source 'https://rubygems.org'

# Should match the version of jekyll we'll be using
gem "jekyll", "~> 3.8"

group :jekyll_plugins do
   gem "jekyll-paginate", "1.1.0"
   gem "jekyll-sitemap", "1.4.0"
   gem "jekyll-redirect-from", "0.16.0"
   gem "github-pages", "214"
end
