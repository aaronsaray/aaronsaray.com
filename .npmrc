# Supply-chain hardening. See README "Tooling" for the reasoning.
# Refuse package versions published less than 7 days ago.
min-release-age=7
# Never run dependency lifecycle scripts (preinstall/postinstall).
ignore-scripts=true
# New deps get pinned to exact versions in package.json.
save-exact=true
