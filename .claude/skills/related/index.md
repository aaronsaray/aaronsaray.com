# Post index

| URL | Title | Summary |
| --- | --- | --- |
| /2007/5-things-this-php-programmer-learned-from-system-ias400-programmers/ | 5 Things this PHP programmer learned from System-i/as400 programmers | Lessons a web developer took from IBM System i (AS/400, iSeries) programmers: store user-facing text in database tables, drive per-environment logic with database rule records, separate small service programs from display, validate data before sending it downstream, and split analyst from programmer roles. |
| /2007/a-better-understanding-of-error-reporting-in-php/ | A Better Understanding of Error Reporting in PHP | Where PHP error reporting is configured (php.ini, Apache httpd.conf or .htaccess, error_reporting() at runtime), how the error level constants combine as bitwise flags, why E_ALL plus E_STRICT belongs in development, and how to silence E_NOTICE for third-party code per directory with php_value. |
| /2007/a-reminder-about-triggering-errors-not-in-the-user-level/ | A reminder about triggering errors not in the USER level | PHP's trigger_error() only accepts the E_USER levels. Passing E_NOTICE instead of E_USER_NOTICE does not raise a notice; it raises a warning reading Invalid error type specified. A small set_error_handler() proof of concept prints the error type and string to show the difference. |
| /2007/add-event-viewer-to-my-computer-right-click/ | Add Event Viewer to My Computer Right-Click | A Windows XP registry tweak: a .reg file that adds an Event Viewer entry to the right-click context menu of My Computer by creating a shell command key under the My Computer CLSID in HKEY_CLASSES_ROOT that launches eventvwr. |
| /2007/ajax-security-research-and-findings-round-1/ | AJAX Security Research and Findings - Round 1 | Research on securing AJAX: queue non-urgent XMLHttpRequest calls through one worker object and a cache to limit server load, gate the AJAX endpoint on a PHP session flag set by the page, and reuse the CSRF token pattern from forms. None stops an attacker. |
| /2007/ajax-security-research-and-findings-round-2/ | AJAX Security Research and Findings - Round 2 | AJAX security recommendations, after confirming a cross-frame JavaScript Object() override does not work in IE or Firefox: return XML or JSON parsed without eval(), require a session token per request, validate before including files, match the page's SSL, and limit request objects to two. |
| /2007/apd-post-processing-wrapper/ | APD post processing wrapper | A PHP class that parses the APD (Advanced PHP Debugger) profiler's pprofp output for display: splits the trace into function call, summary, and timing sections, extracts per-function time, call count, and memory columns, and builds a nested call tree from indentation. |
| /2007/build-phpdocumentor-into-eclipse/ | Build PHPDocumentor into Eclipse | Running phpDocumentor from Eclipse as an external tool, following the PHPEclipse wiki setup guide, and a plan to point the generated documentation at a release folder so developers regenerate and fix documentation after each production deployment as part of the development cycle. |
| /2007/create-an-rss-feed-of-comments-from-myspace/ | Create an RSS feed of comments from myspace | A PHP cron script that screen scrapes a MySpace profile page over a raw socket, pulls each comment out with a regular expression, and writes an RSS 2.0 file with SimpleXML so comments show up in a feed reader instead of on the site. |
| /2007/create-google-advanced-search-string/ | Create Google Advanced Search String | A small PHP class that builds a Google advanced search URL from its query parameters: any of the words, exact phrase, all on one page, excluded terms, number of results, safe search, and last updated, assembled with http_build_query. |
| /2007/cross-domain-ajax-a-quick-anatomy-of-a-mashup/ | Cross Domain AJAX - A quick anatomy of a mashup | Cross domain AJAX through a shared parent domain also needs DNS and web server changes: point a subdomain at the other server's IP (a hosts file entry works locally) and add a ServerAlias to its Apache virtual host so it answers for that name. |
| /2007/demonstrating-password-manager-almost-vulnerability-in-firefox/ | Demonstrating Password Manager Almost Vulnerability in FireFox | Proof of concept for the Firefox password manager autofill weakness: a site with a cross site scripting (XSS) hole lets an attacker plant a duplicate login form in an iframe, and the browser fills in the saved credentials for JavaScript to read and exfiltrate. |
| /2007/eclipse-integration-with-tortoise-svn/ | Eclipse Integration with Tortoise SVN | Using a third-party Eclipse plugin to expose TortoiseSVN commands in the Eclipse PDT PHP Explorer right-click menu, so Subversion working copies checked out on the file system stay usable outside the IDE, as an alternative to Subclipse. Installation and the TortoiseProc.exe path preference. |
| /2007/eclipse-testing-with-tptp-help-me/ | Eclipse Testing with TPTP - help me? | An open question after reading a tutorial on the Eclipse Test and Performance Tools Platform (TPTP): what does writing Java JUnit tests through it offer a PHP developer over PHPUnit, Selenium, and Apache Bench? A request for better hands-on tutorials. |
| /2007/emulation-of-collections-true-stories-of-data-object-handling/ | Emulation of Collections - true stories of data object handling | Exploring PHP data objects built on `__get` and `__set` magic methods that throw on unknown properties, then adding behavior by inheritance versus the adapter pattern (decorators that compose in any order). Favors explicit adapters. Bonus: the Null Object pattern, with `debug_backtrace` logging and `__toString`. |
| /2007/execute-windows-commands-from-php-without-blocking/ | Execute Windows Commands from PHP without blocking | PHP's exec and passthru wait for a launched Windows program to finish, blocking the script. The COM WScript.Shell Run method starts the program in the background and returns immediately. On Unix, shell_exec with the ampersand operator does the same. Non-blocking process launch, asynchronous execution. |
| /2007/fixing-update-error-in-eclipse-pdt/ | Fixing update error in eclipse PDT | Eclipse PDT updates failing with "Network Connection problems encountered during search" because the zend entry in the update site list pointed at the old downloads.zend.com/phpide URL. Editing that entry to downloads.zend.com/pdt lets the PDT updates and everything below it install. |
| /2007/force-log-messages-using-tortoise-svn/ | Force Log Messages using Tortoise SVN | Requiring a minimum commit message length without server-side Subversion hooks: TortoiseSVN's tsvn:logminsize property, set recursively on the working copy root, greys out the commit OK button until the log message reaches that many characters. A reminder for ticket numbers, not a security control. |
| /2007/friendly-reminders-about-testing-your-php-code/ | Friendly reminders about testing your PHP code | Benchmarking PHP performance: millisecond timings vary between runs and machines, so compare ratios and means across many runs rather than raw numbers; profiling code and extensions like Xdebug add their own overhead; and a development box that differs from production makes ratio-based tests mandatory. |
| /2007/helpful-strtotime-reminders/ | Helpful strtotime reminders | What PHP's strtotime() returns for various input formats: now, +1 day, mm/dd/yyyy, and m/dd/yy parse as expected; a bare year gives today's date in that year; m/yyyy fails, and date() renders the result as the Unix epoch. Negative timestamps work on newer PHP. |
| /2007/highlighting-source-files/ | Highlighting Source Files | Options for syntax highlighting code snippets on a WordPress blog: PHP's built-in highlight_file and highlight_string, the PHP_Highlight script that adds line numbers and more, and GeSHi, the Generic Syntax Highlighter, which supports dozens of languages and is the pick for anything beyond PHP. |
| /2007/how-to-make-a-great-myspace-layout/ | How to make a Great MySpace Layout | Building a custom MySpace profile layout: hide the stock content behind a positioned div, keep the required ad, work around MySpace stripping # from CSS, and use PHP with fsockopen, regex, and GD to render friend count and a top friends collage as images. |
| /2007/how-to-use-php-to-generate-downloadable-content/ | How to use PHP to generate downloadable content | Serving a generated file like a PDF from PHP does not need an .htaccess trick that runs a .pdf file as a script. Send the right HTTP headers instead: Content-type for the MIME type and Content-Disposition attachment with a filename to trigger the download. |
| /2007/i-am-a-zend-certified-engineer/ | I am a Zend Certified Engineer | A brief announcement of earning the Zend Certified Engineer credential for PHP 5, the ZCE, Zend's official PHP certification exam. No details on preparation or the test itself; just the news of passing. |
| /2007/is-it-better-to-write-your-for-loops-backwards-in-php/ | Is it better to write your 'for' loops backwards in PHP? | Benchmarking PHP for loops that count down to zero against loops that count up, a micro-optimization borrowed from a JavaScript internals talk. Ten timed runs of 100,000 iterations with microtime() showed no consistent winner, so loop direction does not matter for PHP performance. |
| /2007/live-combined-error-reporting-for-apache-and-php-during-development/ | Live Combined Error Reporting for Apache and PHP during Development | Tail the PHP error log and the Apache error log together while developing, using the logtail Perl script on Windows, and register it as an Eclipse External Tool so errors hidden by redirects or inside HTML tags surface live in the IDE console. |
| /2007/load-time-analyzer-for-firefox/ | Load Time Analyzer for Firefox | A review of the Load Time Analyzer Firefox add-on for measuring page load performance: it records per-resource timings and draws a graph, showing browser caching cut a fresh load from 1750ms to 437ms, but it has no documentation and accumulates results until cleared. |
| /2007/make-interfaces-useful-not-featureful/ | Make interfaces Useful, not featureful | UI and UX design should balance feature rich RIA against simplicity: skip widgets nobody would use like a calendar picker for a birth date, keep labels consistent across pages, do not push needed steps to later, and give interface work to your best designers. |
| /2007/master-the-run-as-option-in-eclipse-pdt-with-php/ | Master the 'Run As' option in Eclipse PDT with PHP | A comparison of the Eclipse PDT Run As options (PHP Script, PHP Webpage, Web Browser) with a script dumping PHP_SAPI, $_SERVER, and $_ENV. They only work with the Zend Debugger loaded, duplicate Debug As, and add little over a browser or the command line. |
| /2007/myspace-bulletins-to-rss/ | MySpace bulletins to RSS | A PHP screen-scraping script that logs into MySpace with cURL and a cookie jar, scrapes the token and bulletin list with regular expressions, fetches each bulletin, and writes an RSS 2.0 feed with SimpleXMLElement. Reading bulletins in a feed reader instead of logging in. |
| /2007/namespacing-is-important-even-in-php/ | Namespacing is important - even in PHP | Why PHP needs namespaces, written before the language had them: every function shares one global namespace, so a new built-in date() broke a PEAR class with a method of the same name. Interim workarounds: prefix everything with short project initials or roll Drupal-style namespaces. |
| /2007/network-site-jemdiarycom-updated/ | Network Site JEMDiary.com updated | A release announcement for version 3.0 of an online diary hosting site that had been growing for years: a face-lift, newer technologies, new features, and existing accounts and entries carried over intact. |
| /2007/odbc-for-udb-and-php-how-i-increased-performance-by-400/ | ODBC for UDB and PHP - How I increased performance by 400% | Speeding up PHP queries against an IBM DB2 UDB (iSeries) database over ODBC: DB2 does not support the dynamic scrollable cursor and downgrades to a keyset-driven one. Passing SQL_CUR_USE_ODBC as the fourth argument to odbc_connect forces a forward-only cursor, four times faster in a benchmark. |
| /2007/pass-php-session-to-a-new-script-using-fsockopen/ | Pass PHP session to a new script using fsockopen | Sharing a PHP session with a second script called over fsockopen on the same server: send a Cookie header with session_name() and session_id(), and call session_write_close() before opening the socket, or the calling request blocks on the session lock and Apache hangs. |
| /2007/pear-installation-woes/ | PEAR Installation Woes | The go-pear.bat installer on Windows XP with PHP 5.2 fails with Cannot use a scalar value as an array warnings from go-pear.phar. Workaround: download the go-pear.php web front end, open it in a browser, and set the install prefix to the PHP directory. |
| /2007/php-command-line-shell-experiments/ | PHP Command Line Shell Experiments | Trying interactive PHP shells (a REPL) on Windows: the built-in php -a printed nothing, the PEAR php-shell package gave verbose var_dump output but exited on any fatal error, and PHP Interactive wrote every command to a script file. The verbose shell won, with reservations. |
| /2007/php-developers-shortcut-for-optimizing-mysql/ | PHP developer's shortcut for optimizing mysql | Few developers run EXPLAIN on every MySQL query. The mysql.trace_mode php.ini directive makes PHP raise errors on unoptimized queries such as full table scans, so paired with an error monitor it flags slow SQL during development. Never enable it in production. |
| /2007/php-script-installer/ | PHP Script Installer | Proof of concept for packaging a whole PHP project into one self-extracting installer file: DirectoryIterator (SPL) walks the tree, file contents are base64 encoded into a generated PHP script that recreates directories and files when run. Pitfalls: permissions, symlinks, memory from base64 bloat, escaping. |
| /2007/php5-object-oriented-properties-tested/ | PHP5 Object Oriented Properties - Tested! | Experiments with PHP 5 object features: a protected constructor plus getInstance builds a proper singleton, `__get` and `__set` magic methods overload property access, but a private `__set` is still invoked from outside, and reference returns through `__get` fail with the indirect modification notice. |
| /2007/planning-for-application-development/ | Planning for application development | Planning a website monitoring project on paper before writing code, after past projects collapsed into unmaintainable code. The idea broken into modules (plugin monitors, logging, alerting, scheduling, self tests) and a research list: refactoring, IDE choice, TDD with PHPUnit, coding standards. |
| /2007/random-user-generation-optimized/ | Random user generation - optimized | Speeding up a slow MySQL ORDER BY RAND() LIMIT 1 random row query: join against a subquery that multiplies rand() by the max id, then take the first row with an id at or above it, ordered ascending. Works when few rows are deleted. |
| /2007/real-world-stories-of-a-prepared-statement/ | Real world stories of a prepared statement | Prepared statements promise SQL injection protection and speed from compiling once. A PHP PDO benchmark of a MySQL join, 100 iterations prepared versus unprepared, showed almost no speed difference. Includes a second test script for ODBC prepared statements against an IBM iSeries (AS/400) database. |
| /2007/security-issue-with-subversion-deployment/ | Security Issue with Subversion Deployment? | Deploying with Subversion leaves .svn/text-base copies of every file ending in .svn-base, and Apache mod_mime treats extra extensions as language or encoding metadata, so those PHP files still execute. Block the .svn directory with a Deny from all Directory directive or a mod_rewrite rule. |
| /2007/send-e-mail-from-outlook-using-php/ | Send e-mail from outlook using PHP | When PHP's mail() cannot reach a locked-down Exchange relay from a Windows development box, send through Outlook instead using PHP's COM extension: create Outlook.Application, CreateItem a mail item, set To, SentOnBehalfOfName, Subject, Body, and attachments, then Display or Send. Outlook prompts for permission. |
| /2007/setting-config-variables-constants-vs-abstract-class-access/ | Setting Config Variables - Constants vs. Abstract Class Access | Two ways to hold PHP application configuration: parse_ini_file feeding a loop that define()s prefixed constants, versus a static config class with a singleton held in a registry that loads a user's details class by reflection. Constants are simpler; the class is bloatier but extensible. |
| /2007/show-the-right-files-in-php-explorer/ | Show the right files in PHP Explorer | Eclipse PDT's PHP Explorer view hides dotfiles by default, so .htaccess and .htpasswd never appear. Unchecking the .* filter shows them but also exposes .svn folders and metadata. Fix: enable name filter patterns with a list of .cache, .settings, .svn, .project, and .projectOptions. |
| /2007/spell-checking-in-eclipse-pdt/ | Spell Checking in Eclipse PDT | Spell checking in Eclipse PDT for HTML converted from Word documents. Eclipse's built-in checker needs a dictionary file, generated on Windows from a GNU Aspell dump; the eSpell Eclipse plugin is better, with check-as-you-type, right-click suggestions, on-demand checking, and a custom dictionary. |
| /2007/symbolic-linking-in-windows/ | Symbolic Linking in Windows? | A warning about emulating symbolic links on Windows with linkd.exe from the Windows Resource Kit: it creates NTFS junction points, which behave more like hard links than symlinks, so deleting the junction deletes the target too. |
| /2007/tamper-data-firefox-extension-is-it-worth-it/ | Tamper Data Firefox Extension - is it worth it? | Review of the Tamper Data Firefox extension: intercept each HTTP request to tamper, submit, or abort it, edit headers and POST variables, then inspect, replay, and graph every request. Worth it for header work and vulnerability testing; for everyday development Load Time Analyzer is better. |
| /2007/test-driven-development-book-review/ | Test Driven Development Book Review | Review of Test-Driven Development by Example by Kent Beck from a PHP programmer new to TDD, PHPUnit, and JUnit. The conversational first section teaches the value of TDD well; the Python and Java pattern sections feel abstract and aimed at Java developers. Worth reading. |
| /2007/the-anatomy-of-a-phishing-attack-advanced-technique/ | The anatomy of a phishing attack - advanced technique | A phishing proof of concept that fooled IT staff: a lookalike promotional domain, a link whose onclick opens a popup without an address bar, a frameset drawing a fake address bar with the real URL, and a cloned site whose login form logs credentials. |
| /2007/the-perils-of-the-at-in-php/ | The Perils of the AT in PHP | The PHP error suppression operator @ (the at sign) should never be used: it silently rewrites error_reporting around each statement, costing performance, and with a custom error handler set it suppresses nothing. Replace it with file_exists, isset, and trigger_error checks. |
| /2007/the-top-17-ways-to-help-eliminate-the-phishing-threat/ | The Top 17 Ways to Help Eliminate the Phishing Threat | Seventeen anti-phishing measures for businesses: standardized outgoing email formats, message verification pages, plain-text email without links, digitally signed mail, per-user passphrases, two-factor authentication, delayed password disclosure, honeypots, trademark monitoring, login notifications, transaction logging, response protocols, and user education. |
| /2007/tools-of-the-trade/ | Tools of the trade | A link list of web development tools for beginners: Firebug for JavaScript, CSS, DOM, and AJAX debugging; Zend PDT and Aptana on Eclipse; MySQL GUI Tools; PuTTY with the PieTTY transparency plugin for SSH; Komodo IDE; and a Windows GUI for phpDocumentor. |
| /2007/two-new-useful-external-tools-for-eclipse/ | Two New useful external tools for Eclipse | Two Windows batch files wired into Eclipse PDT as External Tools: one runs svn update across release checkouts and refreshes the project so new files appear, the other restarts a non-service Apache with taskkill and start, closing the old console window with exit. |
| /2007/type-safe-php-hrm/ | Type-Safe PHP? Hrm... | Pushback on a reflection-and-docblock scheme for type-safe objects in PHP. Loose typing draws beginners; intermediates learn == versus === and empty pitfalls; the advanced want to catch language-generated type exceptions rather than throw them by hand. Forced typing adds overhead and excuses skipping tests. |
| /2007/unobtrusive-js-to-stop-form-submission/ | Unobtrusive JS to stop form submission | Moving form validation out of an inline onsubmit return handler into unobtrusive JavaScript with the Yahoo User Interface library (YUI): YAHOO.util.Event.addListener attaches a submit listener and stopEvent() cancels the submission, keeping behavior separate from markup. |
| /2007/use-mysql-gui-tools-to-securely-connect-to-remote-database/ | Use MySQL Gui tools to securely connect to remote database | Connect MySQL Query Browser to a shared host database that only accepts connections from its web server: configure an SSH tunnel in PuTTY forwarding a local port to the remote MySQL host on 3306, then point the GUI tool at localhost and that port. |
| /2007/website-monitoring-project/ | Website Monitoring Project | Planning an open source website uptime monitoring and broken link checking service in PHP: plugin architecture, MVC, PHP and MySQL version support, coding standards, PHPUnit and test driven development, hosted subscriptions to fund the project, and building a community around it. |
| /2007/when-is-a-php-array-not-an-array/ | When is a PHP array not an array? | Passing a function's return value into array_shift() raises the PHP strict standards notice Only variables should be passed by reference, because array_shift takes its argument by reference and a return value is an expression, not a variable. Fix: return by reference with an ampersand. |
| /2007/when-to-be-oop-and-how-much-lesson-learned/ | When to be OOP - and how much... Lesson Learned! | Lessons from a first PHP MVC app after reading about Strategy, Adapter, and Decorator patterns: view classes extending model classes hit PHP's single inheritance limit, base classes should have been abstract, reinventing PEAR, and preferring public data objects and adapters over protecting everything. |
| /2007/when-your-linkd-causes-you-problems-you-must-convertd-it/ | When your linkd causes you problems, you must convertd it! | A Windows batch file, convertd.bat, that turns a directory junction, a filesystem link made with linkd.exe (from the Windows 2000 resource kit), into a real folder: it validates the argument, removes the link with linkd /D, and runs mkdir in its place. |
| /2007/write-security-triggers-against-sql-injection/ | Write Security Triggers Against SQL Injection | Intrusion detection idea for SQL injection: keep the admin account off ID 1 and away from common usernames like admin, root, or webmaster, then write a MySQL trigger that fires when anything selects those, since only an injection would. Honeypot accounts, tripwire. |
| /2007/writing-a-spelling-corrector/ | Writing a Spelling Corrector | A link to Peter Norvig's essay How to Write a Spelling Corrector, which explains the probability math behind the did you mean suggestions in Google and Yahoo search and shows a short code implementation. Edit distance, Bayesian spell checking. |
| /2008/3d-css-example/ | 3D CSS Example | A proof of concept from the era when people first discussed building 3D games with no images, CSS only: a door rendered in pure CSS, working only in Firefox at the time. A demo page is linked rather than explained. |
| /2008/adobe-air-log-file-watcher/ | Adobe Air Log File Watcher | An Adobe AIR application, built with HTML and JavaScript, that watches one or more log files and pops up a notification on any change, so PHP errors lost between header() redirects and Apache log entries stop going unnoticed. Installer and source attached. |
| /2008/array-merge-is-useful-but-with-a-caveat/ | array_merge is Useful - But With a Caveat | Combining two PHP associative arrays with array_merge versus a foreach loop that assigns each key gives the same elements in a different order, since the loop appends the first array after the second. Do not blindly optimize the loop away; key ordering may be intentional. |
| /2008/automatic-backup-with-svn-on-windows/ | Automatic Backup with SVN on Windows | Using Subversion as a file server backup on Windows: a text file lists directories, batch files loop over them with svn add --force and svn commit, and Windows scheduled tasks run adds weekly and commits daily. Version control doubling as an automatic backup system. |
| /2008/convert-from-vmware-player-to-vmware-server/ | Convert from VMWare Player to VMWare Server | Getting a Gentoo virtual machine built with VMware Workstation to run under the free VMware Server on Windows: hex-edit the .vmx and .vmxd files with XVI32, changing virtualHW.version from 6 to 4. Afterward eth0 became eth1 and networking was switched to NAT. |
| /2008/css-incompatibility-finder/ | CSS incompatibility finder | A proof of concept PHP class that walks a directory tree, pulls CSS out of stylesheets, style tags, and style attributes, and diffs the properties supported by a current browser version against a target one to report incompatibilities, like Firefox 2 versus IE 7. |
| /2008/dig-for-windows/ | Dig for Windows | Running the Linux dig DNS lookup tool on Windows: download the ISC BIND Windows binary, extract dig.exe and its handful of DLLs into a folder or into system32, and query name servers from the command prompt. A cure for Linux tool envy. |
| /2008/directory-separator-is-useless/ | DIRECTORY_SEPARATOR is Useless! | PHP's DIRECTORY_SEPARATOR constant is wasteful and error prone: Windows accepts forward slashes and even mixed slashes in paths, while Unix rejects backslashes, so a hardcoded forward slash works everywhere. Includes a str_replace trick for normalizing getcwd() output on Windows. |
| /2008/does-your-design-leave-an-unknown-size-gap-navigationfillerjs/ | Does your design leave an unknown size gap? navigationFiller.js! | navigationFiller.js, a small JavaScript library that fills the empty space below a sidebar navigation menu with images from a configured list, measuring document height and the target div so the filler never extends past the page. Firefox-only proof of concept with a downloadable zip. |
| /2008/dont-focus-me-bro/ | Don't focus me, bro! | Auto-focusing a login form's username field on window onload steals focus from users who already started typing on a slow page, so half the password lands in the username box. Check document.activeElement against document.body before calling focus(), or track input onfocus for older browsers. |
| /2008/dont-forget-about-class-constants/ | Don't Forget About Class Constants! | Stop polluting the PHP global namespace with define() constants that only one class uses. Declare them as class constants with the const keyword, reference them internally through self::, and access them from outside with the static ClassName::CONSTANT syntax. |
| /2008/dtemplate-dynamic-template-system-for-static-designed-files/ | dtemplate: dynamic template system for static designed files | dtemplate, a PHP tool that turns a designer's static XHTML site into a template: .htaccess rewrites route requests to the original files, then IDs and classes are matched and their content replaced with dynamic output. Includes a verify uploader, controller hook, and download. |
| /2008/eclipse-pdt-include-paths/ | Eclipse PDT - Include Paths | What the Include Paths option on an Eclipse PDT project does: pointing to external libraries such as PEAR or CakePHP so code completion and in-editor error checking work. It is not a substitute for the php.ini include_path. Add Variable versus Add External Folder. |
| /2008/finally-php-has-noindex-on-phpinfo-output/ | Finally - PHP has NoIndex on phpinfo output | Forgotten phpinfo() pages on production servers leak configuration details to attackers, and a Google search turns up huge numbers of them. Since PHP 5.2.1 the output includes a robots meta tag with NOINDEX, NOFOLLOW, NOARCHIVE, so compliant search engines stop indexing them. |
| /2008/firebug-for-ie/ | Firebug for IE? | Firebug Lite brings some of Firebug's debugging features to browsers other than Firefox, including Internet Explorer. A short pointer to the Firebug Lite download for developers who miss the console and inspector when testing in IE. |
| /2008/heredoc-time-for-dead-dead-or-happy-smile-time/ | heredoc - time for dead-dead or happy-smile time? | Pros and cons of PHP heredoc syntax: no quote escaping and editor highlighting for embedded SQL or HTML, versus a closing marker that cannot be indented, no inline expressions, and a crutch for building HTML in logic instead of MVC views. Verdict: personal preference. |
| /2008/how-custom-passphrasespictures-still-dont-protect-against-phishing/ | How custom passphrases/pictures still don't protect against phishing | Two-stage logins that show a personal passphrase or picture before the password can still be phished: a fake page takes the username, a PHP script posts it to the real site, scrapes the passphrase from the response with DOMDocument, and shows it. Man-in-the-middle phishing. |
| /2008/how-desktopini-saved-me-from-myself/ | How desktop.ini saved me from myself | Windows junction points made with linkd.exe look identical to real folders, so deleting a project directory also wiped the linked source repositories. Adding a desktop.ini file with a custom icon to each real folder made them visually distinct from junction points. Windows symlinks. |
| /2008/how-to-throw-an-amazing-meeting/ | How to throw an AMAZING meeting | Tips for running a meeting or technical demonstration: send an outline ahead, rehearse the demo, arrive first, start on time, invite quiet attendees to interrupt, phrase check-ins as no questions (is there anyone who does not understand?), close with questions, thanks, and follow-up. |
| /2008/internet-explorer-css-hacks-transparent-png-and-selectors/ | Internet Explorer CSS Hacks - Transparent PNG and Selectors | Reference notes on targeting Internet Explorer versions with CSS selectors rather than comment hacks: the star html hack for IE6 and below, first-child+html for IE7, html>body for IE7 and modern browsers. Plus the AlphaImageLoader behavior expression that fixes transparent PNG rendering in IE6. |
| /2008/jemgames-launched/ | JEMGames Launched | Announcement that JEMGames, a flash game site, has launched as an experiment comparing how successful a custom programmed website is against an off the shelf open source PHP script. Build versus buy, custom development versus turnkey scripts, side project launch. |
| /2008/js-tool-security-auditing-in-javascript/ | JS Tool - Security Auditing in Javascript | JSTool, an experimental JavaScript security auditing script assembled from open source snippets: browser history viewing, website status reporting, and port scanning from the browser. Very little original code, not production ready, offered as a zip download to learn from. Client-side security research, JavaScript port scanner. |
| /2008/link-checking-module-1st-attempt/ | Link Checking Module - 1st attempt | A first draft PHP link checker that crawls a site with DOMDocument loadHTMLFile, collects anchor hrefs, and marks broken links, presented as a puzzle: untested code full of holes, from relative URL handling to error suppression, showing why code needs testing before trusting it. |
| /2008/meta-tag-generator/ | Meta Tag Generator | A small downloadable JavaScript tool that generates correctly formatted HTML meta tags for the common ones, released from a proof of concept script. Fill in values and copy the generated markup into a page head. Basic SEO and page metadata tooling. |
| /2008/name-css-classes-more-descriptive/ | Name CSS Classes More Descriptive | Name CSS classes for meaning, not appearance: .error instead of .red. When the same shade serves several purposes and a semantic name would force duplicate rules, a compromise like .lowContrastBackground describes the role without tying the class to a color or a logged-in state. |
| /2008/newest-version-of-102-degrees-launched/ | Newest Version of 102 Degrees Launched | A company website redesign announcement: the new site reflects the business model and brings downloadable products, open source projects, the technical blog, and training materials together in one place, with feedback invited. |
| /2008/no-more-the-triangle/ | No More "The Triangle" | A brief note about resigning from a workplace and changing jobs: new opportunities ahead, and reassurance that the blog will keep drawing on real-world experience from a new setting. |
| /2008/ode-to-a-myspace-layout/ | Ode to a MySpace Layout | MySpace changed, leaving an earlier tutorial on building a custom MySpace profile layout out of date. A screenshot preserves what the original custom profile design looked like, in case the old article is retired rather than updated. |
| /2008/pdo-can-you-handle-identical-prepared-statements/ | PDO - can you handle identical prepared statements? | A question about PDO and MySQL prepared statements: MySQL deallocates a named prepared statement when another with the same name is prepared, so does PDO reuse one name and re-prepare every time, or generate unique names? Includes a wrapper caching the last prepared SQL. |
| /2008/php-application-plugins-force-the-interface/ | PHP application plugins - force the interface | Building a PHP plugin system that guarantees third-party plugins follow a contract: declare an interface, then check the loaded object with instanceof before using it. Type-hinted interface parameters force exact method signatures, which also blocks a malicious plugin from demanding a database connection. |
| /2008/php-return-results-of-comparison/ | PHP - Return results of comparison | A comparison expression is a value, so a function can return it directly: return $testVar > 4 gives a boolean without an if statement. A small reminder for writing is*() style predicate functions in PHP with less code. |
| /2008/php-script-configuration-class-with-logic-built-in/ | PHP Script Configuration Class with Logic built in | A PHP configuration class that computes some values at runtime: private static properties, a static get() method, and a private constructor run once as a singleton so conditional logic (environment, database credentials) fills dynamic options while static ones stay preset. |
| /2008/php-script-configuration-options-class-constants-or-mysql/ | PHP Script Configuration Options - Class Constants or MySQL | Choosing where PHP script configuration lives. Requirements: immutable once loaded, quick to edit, always read through a get() method. Class constants work, though self::$item fails and constant("self::$item") is needed. Storing config in MySQL drags in a singleton connection and credential bootstrapping, so constants win. |
| /2008/php-shared-host-session-file-browser-script/ | PHP Shared Host - Session File Browser Script | PHP writes session data to unencrypted flat files by default, which on shared hosting can expose every site's sessions to every other user. A session file browser script lists those files, decodes their serialized values, and audits whether a host's configuration leaks session data. |
| /2008/php-spl-autoload-3-simple-rules-you-must-follow/ | PHP SPL autoload: 3 simple rules you must follow | Three rules for custom autoloaders registered with spl_autoload_register so several can coexist: check is_readable before requiring, never trigger an error when a class is not found so the next autoloader gets its turn, and spl_autoload_unregister loaders you no longer need. |
| /2008/posting-requests-in-php-without-curl/ | Posting Requests in PHP without CURL | Send an HTTP POST from PHP without cURL: file_get_contents with a stream context from stream_context_create, setting the method, a form-urlencoded Content-Type header, and a body built by http_build_query. Requires allow_url_fopen in php.ini. Line by line walkthrough of a login form submission. |
| /2008/programming-without-e-notice/ | Programming without E_Notice | Why PHP raises E_NOTICE for undefined variables and array keys, with register_globals as the historical reason. Fixes: initialize variables to a default before reading them, or guard with isset(), which is a faster language construct than array_key_exists(). Never suppress with the @ operator. |
| /2008/prototype-js-form-elements-need-names-not-just-ids/ | Prototype JS - form elements need names, not just IDs | Prototype's Form.serialize returns nothing when inputs have only id attributes: form elements need a name attribute to be serialized. Under a strict XHTML doctype, the name attribute is deprecated on the form tag itself, not on its inputs. |
| /2008/securing-wordpress-what-my-oops-reminded-me/ | Securing Wordpress - what my 'oops' reminded me | An unpatched WordPress install got compromised by a published exploit and took hours to clean up. Keep WordPress updated; the lesson is that patching is far cheaper than recovery. Links to WordPress security tips, hardening guides, and a vulnerability scanner. |
| /2008/simplephpmailer/ | SimplePHPMailer | SimplePHPMailer is a drop-in PHP form-to-email script for people who do not know PHP: put the file next to your HTML form, open it, follow the commented configuration options, and it mails the submission. Most PHP mailer scripts pile on features; this one stays small. |
| /2008/spl-documentation-standard-php-library/ | SPL Documentation - Standard PHP Library | A link to a documentation site for the SPL, the Standard PHP Library, which bundles iterators, data structures, interfaces, exceptions, and autoloading helpers into PHP. A one-line pointer with a strong recommendation to read it, and nothing more. |
| /2008/svn-pre-commit-duty-lint-your-php/ | SVN Pre-commit duty: Lint your PHP | A Windows batch file that runs before an SVN commit: svn stat filtered with findstr for .php and .phtml files, then php -l on each to report syntax errors. Catches broken code before it reaches the repository when there is no unit test. |
| /2008/the-observer-pattern-in-php-refactored/ | The Observer Pattern in PHP: Refactored | Revisits the observer pattern in PHP with type hints, an abstract observable base class, an observer interface, and a registry mapping observers to hooks. A Twitter transport example fires PREPOST and POSTED notifications so a URL shortener observer rewrites the message. |
| /2008/timetracker-timeclock/ | Timetracker Timeclock | A web-based time tracking package, a timeclock in PHP 5 and MySQL that runs on a spare laptop instead of purchased hardware, with a simple interface, CSV export for Excel, a downloadable zip, and an install script asking for database credentials. |
| /2008/to-clear-up-that-confusion-about-public-and-public-static-access-in-php/ | To clear up that confusion about public and public static access in PHP | Commented PHP example showing which property accesses work: $this-> reaches instance properties, self:: and ClassName:: reach static ones, $this-> on a static property silently fails, and self:: on an instance property throws a fatal undeclared static property error. |
| /2008/understanding-the-observer-pattern-in-php/ | Understanding the Observer Pattern in PHP | A walkthrough of the observer pattern in PHP, the design pattern behind plugin systems and event hooks. An observable class registers observers by type and notifies each before and after posting a message; a URL shortener observer edits the message object by reference. |
| /2008/unofficial-xdebug-ini-with-comments/ | Unofficial xdebug ini - with comments! | A link to an unofficial xdebug.ini file annotated with comments on every setting, written in the style of Apache's commented configuration files. A reference for configuring the Xdebug PHP extension when the bare ini gives no explanation. |
| /2008/update-your-url-filtering-possible-xss-from-data-url-scheme-firefox/ | Update your URL filtering: possible XSS from "Data" URL scheme - Firefox | The data: URL scheme lets a link carry a base64 encoded HTML payload, so cookie stealing JavaScript can run from an href. Firefox executes it; IE 7 does not. URL and HTML filtering routines should reject data: links to prevent XSS, cross-site scripting. |
| /2008/useful-eclipse-plugin-find-a-file-name-in-project-immediately/ | Useful Eclipse Plugin: Find a file name in project immediately | When PHP include paths are computed at runtime, Eclipse PDT cannot jump to a file from an include statement, and grepping for function definitions is slow. The GotoFile Eclipse plugin adds a live search dialog that opens any file in the project by name. |
| /2008/using-eclipse-pdt-check-out-this-wiki/ | Using Eclipse PDT? Check out this wiki | A pointer to a community wiki with a user guide and articles for Eclipse PDT, the PHP Development Tools plugin for the Eclipse IDE. Worth a look for anyone setting up or learning PHP development in Eclipse. |
| /2008/what-do-all-of-those-acronyms-mean/ | What do all of those acronyms mean? | Plain language glossary of web hosting terms for business owners: HTML, CSS, JavaScript, PHP, FTP, MySQL, ASP, Linux, Apache, and IIS, what each does, which are open source, and which need Microsoft licensing on the server. |
| /2008/what-do-i-need-to-get-a-website/ | What do I need to get a website? | Guide for a business owner commissioning a first website: define the need and call to action, budget and timeline, supply your own copy and images, designer versus programmer versus developer, hosting, domain names, SEO guarantees to run from, CMS or update contract, hourly estimates. |
| /2008/what-i-believe-mvc-is-or-mvcfdh/ | What I believe MVC is - or MVCFDH | A personal extension of the MVC design pattern for PHP: front controller as single entry point, per-action controllers, models that return data, data objects (data access layer) holding no business logic, views with only markup, and helpers for shared utilities like database connections. |
| /2008/what-is-a-domain-name/ | What is a Domain Name? | Beginner explanation of domain names: a memorable pointer to a server's IP address, bought through registrars for an annual fee, more a lease than ownership, .com versus .net and .org, registering in your own name, and choosing a short, easy to spell name early. |
| /2008/what-is-website-hosting/ | What is Website Hosting? | Beginner explanation of web hosting: websites live on always-on servers in data centers with backup power and fast connections; shared hosting versus dedicated servers versus free hosting with ads; and why hosting from a home computer fails on ISP terms, bandwidth, uptime, and security. |
| /2008/which-conference-should-i-go-to-help-me-in-2008/ | Which Conference Should I go to - Help me in 2008 | Asking readers to recommend an employer-paid annual web development conference: after MySQL Conference and ZendCon in prior years, the constraints are a United States location (no passport), a web programming, analysis, or tooling focus, and an open source emphasis. |
| /2008/which-fires-first-error-handler-or-shutdown-function/ | Which Fires First? Error Handler or Shutdown Function | In PHP, when a script triggers an error, the error handler registered with set_error_handler() runs before the shutdown function registered with register_shutdown_function(), and the shutdown function still runs after the error. Tested with a division by zero, noting fatal errors bypass the handler. |
| /2008/why-are-there-different-web-browsers/ | Why are there different web browsers? | Beginner comparison of the four browsers for business owners: Internet Explorer, Firefox, Safari, and Opera, how they differ on security and web standards, why the same CSS renders differently, and why a website should be tested in its visitors' top two browsers (cross-browser compatibility). |
| /2008/why-your-company-needs-a-system-architectanalyst/ | Why your company needs a System Architect/Analyst | What a system architect or analyst does and why a software team needs one: an experienced programmer who stays hands-on, knows where the codebase and business are heading, heads off problems, settles peer review disputes, and does analysis first to save work later. |
| /2008/xdebug-and-eclipse-pdt-on-windows-from-start-to-finish/ | XDebug and Eclipse PDT on Windows - From Start to Finish | Installing Xdebug on Windows with Apache and PHP and connecting it to Eclipse PDT: php.ini setup, prettier var_dump and error call stacks, function tracing, cachegrind profiling viewed in WinCacheGrind and KCacheGrind, then remote debugging over DBGp, which hung until output_buffering was turned off. |
| /2008/xdebugs-settings-reminded-me-no-output-to-the-browser-if-sending-headers/ | XDebug's settings reminded me - no output to the browser if sending headers | Turning off output_buffering and enabling implicit_flush for Xdebug exposed stray whitespace between PHP closing and opening tags in legacy code, which sends output to the browser before headers, the classic headers already sent problem, and blocked using the debugger on that codebase. |
| /2008/xss-with-img-onerror-attribute/ | XSS with Img OnError attribute | Cross-site scripting (XSS) through an image's onerror attribute: an img with an empty src fires onerror, and the handler sets src to a remote URL carrying document.cookie, stealing session cookies without anything suspicious in src or href. Sanitize user-submitted HTML, filter every attribute. |
| /2009/another-example-of-csrf-in-css/ | Another example of CSRF - in CSS | A cross-site request forgery (CSRF) attempt submitted through user-supplied CSS: a background url() pointing at a delete endpoint with an id in the query string, so any visitor's browser fires the GET. Two lessons: never use GET for irreversible actions, and filter user input. |
| /2009/auto-failover-for-cdn-based-javascript/ | Auto Failover for CDN based Javascript | Fallback for jQuery loaded from the Google CDN: because a script tag has no onerror, an inline check for typeof jQuery undefined runs after the tag and appends a script element pointing at a local copy. Daily CDN load failures stopped appearing in error reports. |
| /2009/block-and-allow-ip-with-iptables-simple-script/ | Block and Allow IP with iptables - simple script | Two small bash scripts for blocking and unblocking a single IP address or subnet with the Linux iptables firewall: one appends a DROP rule to the INPUT chain, the other deletes it, and both persist the change with iptables-save to the sysconfig file. |
| /2009/bluefish-editor-could-help-you-leak-your-php/ | Bluefish Editor could help you leak your PHP! | The Bluefish editor writes tilde-suffixed backup files such as settings.php~ before saving. Committed and deployed, the web server serves them as plain text, exposing PHP source and database credentials: source code disclosure. Review what you commit and turn off or clean up editor backup files. |
| /2009/bookmarklet-wage-calculator/ | Bookmarklet: Wage Calculator | A JavaScript bookmarklet for comparing salary postings during a job search: enter an hourly or yearly wage, and it converts between yearly, monthly, and hourly using 2080 hours, then shows gross pay, net after a tax bracket, and independent contractor net after self-employment tax. |
| /2009/chaining-methods-in-php/ | Chaining methods in PHP | Method chaining in PHP by returning $this from each method, a fluent interface, with a demo class. The caveat: a method that returns false or depends on a previous method succeeding breaks the chain, since nothing asks whether continuing is valid. |
| /2009/dialing-phone-numbers-from-a-webpage/ | Dialing Phone Numbers from a Webpage | Click to call or save a contact from a mobile web page with WTAI protocol links, the WAP era ancestor of tel: links: wtai://wp/mc; followed by the phone number places a call, and wtai://wp/ap; with number and name adds an address book entry. |
| /2009/disable-md5-now-or-you-will-die/ | Disable md5 now - or you will die | MD5 hash collisions moved from theory to working example code and vendor advisories, so stop using it: switch to sha1() for hashing, add md5 to disable_functions in php.ini so nobody can call it by accident, and keep it out of the database too. |
| /2009/display-unix-timestamp-in-a-date-format-in-mysql/ | Display unix timestamp in a date format in MySQL | Converting a Unix timestamp stored in a MySQL column by PHP into a readable date directly in the query with the built in FROM_UNIXTIME() function, so there is no need to pull the integer out and format it in application code. |
| /2009/displaying-the-most-popular-youtube-videos-with-php/ | Displaying the Most Popular Youtube Videos with PHP | A quick PHP script that loads the YouTube GData API most_popular feed with simplexml_load_file(), loops over the entries, rewrites each watch URL into an embeddable Flash player object, and prints a table of the videos with links back to their source. Requires allow_url_fopen. |
| /2009/document-url-vs-document-location-href/ | document.URL vs document.location.href | In JavaScript, document.location.href is both a getter and a setter, while document.URL is read-only: assigning to it throws an error instead of redirecting. A short demo shows both reading the current URL and only location.href navigating the browser. |
| /2009/eclipse-pdt-integrating-apache-bench-for-load-testing/ | Eclipse PDT: Integrating Apache Bench for load testing | Running Apache Bench (ab) load tests from inside Eclipse PDT as an External Tool: the ab flags for concurrency, keep-alive, request count, and time limit, then Eclipse string_prompt variables so each run asks for concurrency, request count, and URL. Output goes to the console. |
| /2009/eclipse-pdt2-wont-jump-to-functionsclasses-on-ctrl-click-solved/ | Eclipse PDT2 won't jump to functions/classes on ctrl-click: solved | Ctrl-click go to definition stopped working in Eclipse PDT 2 on a project created from an SVN checkout. The fix is adding the project's own folder to its PHP build path (Configure Build Path, Add Folder), then running Project Clean to rebuild the index. |
| /2009/elgg-development-tools-elgg-plugin/ | Elgg Development Tools - Elgg Plugin | A developer tools plugin for the Elgg platform: bundles FirePHP with a mock FB class that logs leftover calls when disabled, adds a switch to turn off view caching, re-enables error display without editing .htaccess, and gathers simple cache settings in one admin page. |
| /2009/elgg-plugin-friend-autocomplete-box-replaces-select-box/ | Elgg Plugin: Friend AutoComplete Box Replaces Select Box | An Elgg plugin that extends the input/pulldown view so adding one class turns a friend select box into a jQuery autocomplete text field. The select is hidden, its options feed the suggestions, and the chosen friend's GUID is written back to the hidden select. |
| /2009/elgg-plugin-generate-groups/ | Elgg Plugin: Generate Groups | An Elgg plugin for seeding test data: bulk creates a chosen number of groups with random names and descriptions, assigns random avatars to two of every three, joins a random set of existing members to each, with per-hook enable, disable, or random options. |
| /2009/email-to-smstxt-address-list/ | Email to SMS/TXT address list | A downloadable CSV of email-to-SMS gateway addresses for mobile carriers, compiled from a website that publishes the list only as HTML with no feed. Meant to make sending text messages by email from code easier, with a request to support the original site. |
| /2009/enabling-javascript-specific-css/ | Enabling Javascript Specific CSS | Styling a page differently for JavaScript on or off without noscript: a nojs class a script swaps to js (progressive enhancement). Putting the class on the html element is invalid under HTML 4; put it on body with the script first inside body. |
| /2009/figuring-out-price-before-tax/ | Figuring out Price Before Tax | A small JavaScript function that takes a tax-inclusive total and a tax percentage and returns the pre-tax price, rounded to two decimals. Reverse sales tax calculation, backing tax out of a total, for quotes that include tax. |
| /2009/firebug-for-internet-explorer/ | Firebug for Internet Explorer | Loading Firebug Lite in Internet Explorer for debugging sites developed in Firefox and tested with IETester: a PHP snippet injects the script only outside production and for MSIE user agents. Its limits versus full Firebug: quirky inspect, full computed CSS, no inline DOM editing. |
| /2009/flash-of-unstyled-content-in-firefox-3/ | Flash of Unstyled Content - in FireFox 3 | A flash of unstyled content in Firefox 3 despite using a link tag for the stylesheet. The cause was a pile of script tags loading before the link; moving the stylesheet link to the top of the head fixed it. FOUC, script loading order. |
| /2009/form-submit-internet-explorer-behaving-badly/ | Form Submit: Internet Explorer behaving badly | Two Internet Explorer form quirks: pressing Enter in an input does not submit the form, fixed with a jQuery keydown handler for keyCode 13 that submits the parent form; and a button element without type="submit" submits in Firefox but not in IE. |
| /2009/how-i-fixed-the-javascript-error-with-wp-codebox/ | How I fixed the Javascript error with wp-codebox | An upgrade of the wp-codebox WordPress plugin broke JavaScript on pages with code boxes because it bundled its own jQuery. The fix: stop the plugin including jquery.js in codebox_header(), delete the file, and replace the $jcodebox noConflict alias in codebox.js with $. jQuery conflict. |
| /2009/html-5-and-me-draft-10-thoughts/ | HTML 5 and Me - Draft 10 Thoughts | Reactions to the HTML5 differences from HTML4 working draft: praise for semantic elements, simpler doctype, autofocus, required, async, and getElementsByClassName; concern over unresolved accessibility issues, the link ping attribute, prefilled form data caching passwords, dropped label focus, and whether XHTML2 or HTML5 will win. |
| /2009/ie-js-error-expected-identifier-string-or-number/ | IE JS Error: Expected identifier, string or number | Internet Explorer throws the JavaScript error Expected identifier, string or number when an object or array literal ends with a trailing comma, code that Firefox accepts. Remove the comma after the last property. A tip for finding the offending line with Firefox's JSView extension. |
| /2009/is-phps-call-used-when-no-construct-is-present/ | Is PHP's __call() Used When No __construct is Present? | Testing whether PHP's `__call` magic method fires when a class is instantiated without a `__construct` method. It does not: constructing an object with arguments but no constructor produces nothing, so `__call` only intercepts inaccessible method calls, not missing constructors. Magic methods, constructor behavior. |
| /2009/javascript-error-handler/ | Javascript error handler | Homemade JavaScript error reporting service: window.onerror receives the message, URL, and line number and fires a request by setting the src of an Image object, a pre-AJAX beacon technique. A PHP script writes the details plus referrer and user agent to error_log alongside server errors. |
| /2009/javascript-snow-fall-with-buildup/ | Javascript Snow Fall with buildup | A jQuery snowfall animation where the snow builds up, unlike a Flash version. The open source snowstorm.js script drops the flakes; three tileable snow line PNGs, offset in Photoshop, are absolutely positioned at the bottom and grow a pixel at a time on a setInterval. |
| /2009/jfbconsole-chainable-firebug-console-jquery-plugin/ | JfbConsole - chainable Firebug Console jQuery plugin | A jQuery plugin that wraps the Firebug console API (log, debug, info, warn, error, assert, dir, dirxml, trace, time, profile) as chainable jQuery methods, so you can log a jQuery object mid-chain during development, for example $('#el').log('msg').clone(). Methods are only defined when window.console exists. |
| /2009/launch-screen-automatically/ | Launch Screen Automatically | Put exec screen -R in .bash_profile so every login reattaches to a detached GNU screen session, or starts a new one when none exists. A one line fix for flaky connections that drop constantly and lose your terminal work. |
| /2009/licensing-of-code/ | Licensing of Code | The license for code published on the blog: originally BSD, later MIT for all code, with written content under Creative Commons. A short note on choosing an open source license for shared snippets and examples. |
| /2009/localized-dates-in-php/ | Localized dates in php | Handling time zones in PHP with date(), gmdate(), and time(): store Unix timestamps in GMT, format with gmdate() to avoid the server's timezone bias, then apply each user's stored GMT offset plus a daylight saving adjustment from gmdate('I') when displaying. |
| /2009/mod-unique-id-error-after-installing-mod_security/ | mod_unique_id Error After Installing mod_security | Apache refuses to restart after installing mod_security with the alert mod_unique_id: unable to find IPv4 address of the machine's hostname. The cause is a hostname that does not resolve; adding a 127.0.0.1 entry for it in /etc/hosts fixes the startup error. |
| /2009/moving-files-from-one-webserver-to-another-using-php-script/ | Moving files from one webserver to another using PHP script | A proof-of-concept PHP script for migrating a site between hosts without manual FTP: it walks a directory with SPL RecursiveDirectoryIterator, uploads every file and folder over ftp_put and ftp_mkdir, then reapplies the original permissions with ftp_chmod. Ideas for credentials, error checking, and database migration. |
| /2009/multi-version-programming-to-successfully-leverage-overseas-programming/ | Multi-Version Programming to Successfully Leverage Overseas Programming | A thought experiment from a paper on N-version programming: independently written programs rarely share the same bug, so running several and taking the majority output filters faults. Applied to outsourcing: four cheap programmers voting could beat one expensive one. An idea, not an endorsement. |
| /2009/my-progression-through-forgot-passwords/ | My Progression Through Forgot Passwords | Three generations of a forgot-password flow and what was wrong with each: emailing plaintext passwords, reset tokens made from md5(time()) that collide and are guessable, then md5 of user ID plus time. The current approach: 300 random bytes, base64 encoded, made URL safe. |
| /2009/password-complexity-class/ | Password Complexity Class | A PHP Password class that validates a new password against rules combined as bitwise flags: minimum and maximum length, lowercase, uppercase, number, special character, differs enough from the old password, does not contain the username, and enough unique characters. Reflection dispatches each rule. |
| /2009/photoshop-touchup/ | Photoshop Touchup | A first attempt at portrait retouching in Photoshop, using a photo found online that turned out to be from a tutorial on the same technique, done with an independent process. Before and after images only, no written steps. |
| /2009/php-and-the-uuid/ | PHP and the UUID | Generating a version 4 UUID (GUID) in PHP by hand: the RFC 4122 field layout, setting the version and variant bits with bitwise OR (0x4000 and 0x8000), splitting the 32-bit fields because mt_getrandmax is smaller, and the sprintf %04x one-liner from the manual. |
| /2009/php-bitwise-user-authorization/ | PHP Bitwise User Authorization | Bitmask user authorization in PHP, like Windows file permissions: each role is a power of two, a user's permissions are their sum, and access checks use bitwise AND. Values carry no hierarchy, so new roles slot in; bulk grants become a MySQL update. |
| /2009/phpini-creator/ | PHP.ini creator | PHPIniCreator, a rough tool that parses a pasted php.ini file, shows a comparison table against suggested values, lets you change directives, and generates an updated php.ini to replace yours. Early version with a long to-do list: full directive database, input validation, per-version defaults. |
| /2009/please-use-public-accessors-in-your-object-oriented-programming/ | Please Use Public Accessors in your Object Oriented Programming | Why public properties on objects backfire: a Book class with public $title, $author, and $pageCount works until author becomes an Author object and page counts need padding. With getter methods (encapsulation) the class changes internally while every caller's printing code stays untouched. |
| /2009/printing-a-small-segment-of-the-page-using-jquery/ | Printing a small segment of the page using JQuery | Print only one section of a web page with jQuery: on click, inject a media print stylesheet that hides the page layout, clone the target container after the body with a visible class, and call window.print(). Built on Yahoo YUI Grids layout. |
| /2009/protect-your-image-from-download/ | Protect Your Image from Download | A CSS trick to deter casual image downloading: the img src is a 1x1 transparent GIF, the real picture is a background-image on that element, sized to match. Right click, save as, or view image yields the transparent decoy. Not foolproof, no JavaScript needed. |
| /2009/quick-reference-simple-mysql-performance-monitoring/ | Quick Reference: Simple MySQL Performance Monitoring | Command-line MySQL performance monitoring with mysqladmin: status for uptime, threads, slow queries, and queries per second; processlist for active connections and what they run; extended-status for every server variable, repeating on an interval with relative deltas. Also mytop, a top clone for MySQL. |
| /2009/rely-on-google-for-your-javascript/ | Rely on Google for your Javascript? | Pros and cons of loading JavaScript libraries from the Google AJAX Libraries API, an early public CDN: proper caching, one shared copy across sites, and easy versioned includes, against the risk that Google goes down or shuts the service off and forces a re-architecture. |
| /2009/remove-internet-explorer-button-border/ | Remove Internet Explorer Button Border | Internet Explorer draws an extra border around every button element unless the border is explicitly set to 0. To get a clean 1px white border, zero the button's border in CSS and wrap it in a span that carries the visible border instead. |
| /2009/scanning-for-unfiltered-content-automatically-with-php/ | Scanning for Unfiltered Content Automatically with PHP | A minimal PHP vulnerability scanner for reflected XSS: load a page with DOMDocument, find every form and its inputs, append a script tag payload to each value, submit with file_get_contents and a stream context, then count how often the payload comes back unescaped. |
| /2009/send-email-from-server-to-gmail-using-postfix/ | Send email from server to gmail using postfix | Postfix on a server inside a domain whose MX record points at Google Apps kept delivering mail for that domain to local mailboxes instead of Gmail. The cause was mydestination in main.cf listing $mydomain; setting mydestination to empty makes Postfix relay the mail out. |
| /2009/trac-bookmarklet-load-ticket-number-easily/ | Trac Bookmarklet: load ticket number easily | A one-line JavaScript bookmarklet that prompts for a Trac ticket number and jumps straight to that ticket's URL, saving the typing or searching when someone hands over only the number. Swap in the root path of your own Trac instance. |
| /2009/trim-down-your-content-it-is-a-must/ | Trim down your content - it is a MUST | Copywriting advice from a usability book: cut your page copy in half, then in half again. Applied to a band-finder landing page by dropping explanations users trust, relying on common sense, a three-second elevator pitch, valuing whitespace, and plain slang over formal words. |
| /2009/twitter-live-search-updates-page/ | Twitter Live Search Updates Page | A self-refreshing page that polls the Twitter Search API with jQuery getJSON (JSONP) every three seconds for several terms or hashtags, dedupes by tweet ID, and prepends new tweets with a slide animation. Running it in the browser avoids server-side rate limits. |
| /2009/unknown-function-_popupcontrol/ | Unknown function _popupControl() | JavaScript error logs filled with an undefined function `_popupControl`(), injected into every page by the CA Personal Firewall. Defining a no-op stub when typeof `_popupControl` is not a function stops Internet Explorer from showing script errors or halting other scripts. |
| /2009/using-php-to-find-distance-between-zip-codes/ | Using PHP to find distance between Zip Codes | Import a CSV of zip codes with latitude and longitude into MySQL, then compute miles between two zips with the spherical law of cosines (great-circle distance) in PHP. For a radius search, a bounding box query narrows candidates before the exact distance filter. |
| /2009/when-phps-dirname-saved-the-day/ | When PHP's dirname() saved the day | Relative paths in require_once break command line PHP scripts as soon as you run them from a different working directory. Build include paths from the script's own location with dirname(__FILE__) so the require resolves the same wherever the script is invoked. |
| /2009/why-the-boss-makes-you-make-a-list-or-proposal/ | Why the Boss makes YOU make a List or Proposal | Why a manager asks for a written proposal or list of ideas: it forces planning, invites review because two heads are better than one, surfaces information only they know, and keeps communication open. For managers: review it, encourage good ideas, give ownership, follow up. |
| /2010/3d-walkthrough-in-basic/ | 3D walkthrough in Basic | A BASIC (QBasic) first-person 3D maze walkthrough in the style of doom.bas: raycasting against a 12 by 12 grid with sine and cosine lookup tables, page flipping to stop flicker, arrow key movement, and a randomly drawn sky, clouds, and building backdrop. |
| /2010/a-better-milwaukee/ | A Better Milwaukee | A photo retouching exercise: a kayaker's photo of Milwaukee, before and after a Photoshop touch-up, with the layered PSD file attached and a request for tips on doing photo touch-ups better or faster. An unusual, artsy entry for a programming blog. |
| /2010/aaron-on-ndas/ | Aaron on NDAs | Why a developer refuses to sign a non-disclosure agreement (NDA) before quoting: ideas are easy and implementation is the value, NDAs rarely hold up in court, a legal document needs a lawyer, and most are too broad to coexist with open source licenses. |
| /2010/book-review-expert-php-5-tools/ | Book Review: Expert PHP 5 Tools | Review of Expert PHP 5 Tools by Dirk Merkel from Packt: coding standards, phpDocumentor, Eclipse PDT versus Zend Studio, Subversion, debugging, frameworks, PHPUnit, Phing, and continuous integration with Xinc and phpUnderControl. Too simple in places and skips Git, but essential reading for newer PHP programmers. |
| /2010/branding-your-tweets-using-anywhere-tweetbox/ | Branding your Tweets using @anywhere tweetbox | Posting tweets branded with your own website using the Twitter @anywhere API: register an application for a consumer key, embed the tweetBox JavaScript widget on a small page, and open that page in the Firefox sidebar so tweets show your site as the source. |
| /2010/change-the-author-tag-default-in-eclipse-pdt/ | Change the @author tag default in Eclipse PDT | The @author value in Eclipse PDT docblock templates comes from the ${user} variable, the logged-in machine account. Override it by launching Eclipse with the -Duser.name vmargs flag, or edit the Comments Types template under Preferences, PHP, Code Style, Code Templates. |
| /2010/combining-print-and-screen-css/ | Combining Print and Screen CSS | YSlow flagged two stylesheet requests where one would do. Merge print and screen styles into a single stylesheet using an @media print block, shown by hiding an input in print while it keeps a border on screen. One fewer HTTP request, no separate print stylesheet. |
| /2010/conditional-comments-fix-blocking/ | Conditional Comments - fix blocking | Internet Explorer conditional comments block downloads until they finish processing, as explained in a phpied.com post. The fix is an empty conditional comment at the very start of the head element so later resources are not held up. Page load performance, IE quirks. |
| /2010/disable-jquery-from-loading-in-custom-wordpress-template/ | Disable jQuery from Loading in Custom Wordpress Template | Stopping WordPress from enqueuing its bundled jQuery in a custom theme so the page can load it from the Google CDN instead: deregister the handle with wp_deregister_script() in the theme rather than deleting the file, which would break the admin and cause a 404. |
| /2010/easiest-form-token-class-to-prevent-csrf/ | Easiest Form Token class to prevent CSRF | A minimal PHP formtoken class for cross-site request forgery protection: generate a SHA1 token from a random number, store it in the session with a timestamp, emit it as a hidden input, then validate on POST with a two-hour expiry and optional single-use clearing. |
| /2010/easy-mcrypt-encryption-class/ | Easy MCrypt encryption class | A static PHP wrapper around the mcrypt extension with encrypt() and decrypt() methods taking key, algorithm, and mode. It caches opened modules, validates the algorithm and mode against mcrypt_list_algorithms and mcrypt_list_modes, and builds the initialization vector by hand to avoid slow mcrypt_create_iv. |
| /2010/excel-wont-handle-utf-8-in-csv-force-a-different-encoding-in-php/ | Excel won't handle UTF-8 in CSV?  Force a different encoding in PHP | Excel misreads a UTF-8 CSV generated by PHP, showing garbled characters. Converting each line with mb_convert_encoding from UTF-8 to Windows-1251 before output makes the file open correctly in Microsoft Excel on Windows. CSV export encoding, character set conversion, mojibake. |
| /2010/exploring-mysql-in-eclipse-pdt/ | Exploring MySQL in Eclipse PDT | Browsing and querying MySQL in Eclipse PDT with the SQL Explorer plugin: installing it from the update site, adding the MySQL Connector/J JDBC driver, creating a connection profile, registering the driver jar when it is missing from the list, and opening the SQL editor. |
| /2010/facebook-message-system-in-php/ | Facebook Message System - in PHP | Building a Facebook-style threaded messaging system in PHP and MySQL: two normalized tables, a message table keyed by thread id and sequence and a recipients table with per-user status, plus inbox, sent, view, reply, compose, and delete pages. Group conversations, soft delete, read status. |
| /2010/fix-firephp-not-showing-output-in-console-in-firebug/ | Fix: FirePHP not showing output in console in Firebug | FirePHP output missing from the Firebug console even with the Console panel enabled for the site. The overlooked requirement: the Net panel must also be enabled for that site before the debug information comes through. Debugging PHP with FirePHP and Firebug. |
| /2010/fixing-fakepath-in-jquery-filestyle/ | Fixing c:\fakepath in filestyle jquery plugin | Internet Explorer prefixes chosen file names with c:\fakepath\ as a security feature, which the jQuery FileStyle plugin then displays. A one-line fix in the plugin's change handler strips the fake path with a regular expression before showing the filename. File input display, IE quirk. |
| /2010/friend-of-a-friend-foaf/ | Friend of a Friend: FOAF | Friend of a Friend (FOAF) is an RDF-based protocol that lets machines read relationships between people on the web. Adding a FOAF link to a home page, plus phoaf, a PHP library for parsing FOAF files. Semantic web, RDF. |
| /2010/guitar-chord-fingering-with-php/ | Guitar Chord fingering with PHP | A small PHP script, rediscovered in an old archive and offered as a zip download, that shows every fingering combination of a chosen guitar chord across the fretboard when you pick the chord from a drop-down. Chord chart generator, music theory in code. |
| /2010/hcard-should-i-care/ | hCard - Should I Care? | Semantic web microformats for online identity: XFN, FOAF, and hCard, an HTML embedding of the vCard contact standard. Many sites publish the data but few consume it, so the value is doubtful, yet a FOAF file and an hCard went on the contact page. |
| /2010/hide-email-addresses-while-still-using-mailto-header-redirect/ | Hide Email Addresses while still using mailto: Header Redirect | Email address obfuscation for mailto links: link to a PHP script with a user parameter that assembles the email from the server name and issues a Location header redirect to mailto. Spam scraper bots miss it; a JavaScript onclick parameter could screen out more. |
| /2010/how-about-profiling-your-mysql-queries-later/ | How about profiling your mysql queries... later? | An idea for retroactive MySQL query optimization: extend the database class to log every unique query the site runs (deduplicating with similar_text for non-prepared statements), then a nightly job runs EXPLAIN on the new ones and emails the results. Query profiling automation. |
| /2010/how-does-google-maps-api-present-location-on-whatscloseto-me/ | How does Google Maps API present location on whatscloseto.me | The location detection on whatscloseto.me comes from the ClientLocation feature of the Google Maps AJAX API, which is why it is sometimes close and sometimes far off. When no location can be determined, the site falls back to Milwaukee, Wisconsin. Approximate geolocation. |
| /2010/how-i-test-email-recipients-when-i-develop/ | How I test email recipients when I develop | Route outgoing email from a PHP app to a developer inbox outside production without losing the real recipient: plus addressing (subaddressing) embeds the original address in the local part, so each test message shows who would have received it. |
| /2010/how-to-handle-ajax-errors-with-jquery/ | How to handle AJAX errors with jQuery | The error callback of jQuery's $.ajax() is often ignored. An ajaxError handler switches on the error type (parsererror, timeout, notmodified, HTTP error) to build a message, adding status code for HTTP failures. Logical failures like a bad login belong in the success path. |
| /2010/how-to-log-php-errors-like-a-pro/ | How to Log PHP Errors like a Pro | A PHP error handling class registered with set_error_handler and set_exception_handler: captures error context and backtrace through output buffering, prints in development, writes to error_log in production with newlines stripped for grepping, and redirects users to a friendly page for anything above a notice. |
| /2010/ie6-warning-on-site/ | IE6 warning on site | Adding an Internet Explorer 6 deprecation notice that only IE6 users see: an IE conditional comment loads a script for lte IE 6, jQuery prepends a banner to the header recommending an upgrade to IE8, Firefox, or Chrome, and a little CSS styles it. |
| /2010/iestandards-xml-requests/ | IEStandards.xml requests | 404 errors for IEStandards.xml in the server log traced to an X-UA-Compatible meta tag set to IE=EmulateIE8, added to stop Internet Explorer 8 blocking AJAX submissions with an information bar. The proposed fix is both the meta tag and an IEStandards.xml file in place. |
| /2010/its-not-shameful-to-cut-your-losses/ | It's not shameful to cut your losses | Most businesses fail, and knowing when to cut your losses is an entrepreneurial skill. Robert Kiyosaki's point that failure buys knowledge, a venture shut down despite sunk costs, and deleting an archived flash game site instead of retrying it. Sunk cost fallacy, failing fast. |
| /2010/javascript-and-css-compression-and-cache/ | Javascript and CSS Compression and Cache | Front-end asset optimization: serving CSS, JavaScript, and images from a separate assets subdomain, CSS sprites to cut HTTP requests, far-future Expires headers for a year, and minifying CSS with CSSTidy at build time. Cache busting by versioning the filename (main.1.css, main.2.css) on each deploy. |
| /2010/jquery-show-password-toggle/ | jQuery show password toggle | A jQuery show password toggle: a checkbox that unmasks a password field. Internet Explorer refuses to change an input's type attribute, so the script builds a replacement input of type text (or password), copies id, name, class, and value, and swaps it in. |
| /2010/jquery-validation-greedy-on-your-forms/ | jQuery Validation - greedy on your forms? | With two forms on one page, calling $('form').validate() with the jQuery Validation plugin makes submitting one form re-validate the other and show its errors. The fix is to loop over each form with .each() and call validate() on each one in its own context. |
| /2010/load-facebook-fanbox-faster-by-caching-it/ | Load Facebook Fanbox Faster by Caching it | The Facebook fan box widget loaded slowly and dragged page load time, so a PHP cron job fetches its HTML with cURL (spoofing the user agent), parses fan count and photos with regular expressions, and writes a cached partial refreshed hourly. |
| /2010/making-friendly-javascript-errors-client-and-server/ | Making Friendly Javascript Errors - Client and Server | Turn logged JavaScript errors caused by leftover spyware on a visitor's machine into a friendly warning. Client side, window.onerror inspects the message and injects a div; server side, the error.php logging endpoint returns an image when the message matches a known bad function name. |
| /2010/milwaukee-php-users-group/ | Milwaukee PHP Users Group | Announcing the founding of the Milwaukee PHP Users Group (MKEPUG), which meets on the second and fourth Tuesday of each month at Bucketworks, after a meeting cancelled by snow. Starting a local PHP user group and developer community meetup in Milwaukee. |
| /2010/modifying-clickheat-to-use-your-authentication/ | Modifying Clickheat to use your authentication | Make ClickHeat, the open source PHP heat map alternative to Crazy Egg, use an existing application's login instead of its own: in index.php after the config include, define CLICKHEAT_ADMIN from your session check and remove the block that reads the clickheat cookie. |
| /2010/my-meetmeme-card/ | My Meetmeme card | A short review of Meetmeme cards, which come with a short profile URL and social stats like tweet count, on baseball-card-sized stock. Neat idea with two flaws: the tweet count was far off, and the oversized cards do not fit in a wallet. |
| /2010/my-nondisclosure-promise/ | My NonDisclosure Promise | Why a PHP developer declines to sign NDAs and offers a public promise instead: open source licenses can require contributing changes back, ideas matter less than implementation, and consulting peers produces better work. The promise covers ethical conduct, protecting the client's investment, and communication. |
| /2010/object-cache-class-in-php/ | Object Cache Class in PHP | A tiny static object cache class in PHP as an alternative to singletons: register an object by type and ID, then check exists and get it back from any method later. Essentially an identity map or registry pattern; SplObjectStorage covers similar ground. |
| /2010/pareto-principle-8020-in-software-developmentsupport/ | Pareto principle - 80/20 in Software Development/Support | Applying the Pareto principle, the 80-20 rule, to software: a hundred complaints from a thousand users means most people are fine, self-testing the 80% of common paths and shipping is an acceptable risk, and a support team aiming for 80% resolution beats chasing 95%. |
| /2010/removing-www-just-not-for-seo/ | Removing WWW - just not for SEO | Serving a site at both www and non-www is no longer a duplicate content SEO penalty, since Google lets you pick a canonical host. Drop it anyway for brevity with a generic Apache .htaccess mod_rewrite rule that 301 redirects www to the bare domain. |
| /2010/restrict-your-git-directory-on-live-site/ | Restrict your .git directory on live site | Deploying by git checkout on the server can leave the .git directory inside the public web root, exposing the repository config and history to anyone. Block it in the Apache configuration with a Directory block for the .git path and Deny from All. |
| /2010/simplephpmailer-now-with-required-fields/ | SimplePHPMailer: now with required fields | Update announcement for SimplePHPMailer, an open source drop-in PHP contact form mailer script for beginners: the new version supports required fields, so blank form submissions are rejected instead of emailed. Links to the original SimplePHPMailer entry with the updated download. |
| /2010/time-off-for-b-day-is-done/ | Time off for B-day is done | A short note that the weekly Tuesday blog posting schedule resumes after a week off for a birthday. A housekeeping announcement with no technical content, of interest only as a marker of the publishing cadence at the time. |
| /2010/twitter-anywhere-platform-web414-presentation/ | Twitter @anywhere platform: web414 presentation | A link to the PDF slides of a talk on the Twitter @anywhere JavaScript platform given at Web414. Nothing beyond the download; the companion proof-of-concept post carries the actual code and explanation. |
| /2010/twitter-anywhere-proof-of-concepts/ | Twitter @anywhere proof of concepts | Proof-of-concept code for the Twitter @anywhere JavaScript API with jQuery: a connect button with authComplete and signOut callbacks, showing the current user's screen name and avatar with hovercards, and looking up another user's follower counts and last tweet via T.User.find. |
| /2010/use-your-own-short-domain-while-waiting-for-bitly-pro/ | Use Your Own Short Domain While Waiting for BitLy Pro | While waiting on a bit.ly Pro invite, temporarily forward a custom short domain to the bit.ly URL shortener at the registrar (GoDaddy) so existing link hashes resolve through the vanity domain. A Firefox bookmarklet rewrites bit.ly URLs to the custom domain. |
| /2010/using-apachetop-with-cronolog/ | Using ApacheTop with Cronolog | Cronolog splits Apache access logs into dated directories and files, which makes pointing apachetop at the current log tedious. A short bash wrapper builds today's log path from date +%Y, %m, and %d and launches apachetop against it for live request monitoring. |
| /2010/using-google-analytics-asynchronously/ | Using Google Analytics Asynchronously | Google Analytics added an asynchronous tracking snippet that loads the tracker without blocking page rendering. An open question: the synchronous method supported two tracker instances for two accounts on one site, and it is unclear whether the async tracking code can do the same. |
| /2010/using-google-charts-to-make-qr-codes/ | Using Google Charts to make QR Codes | The Google Charts API includes a QR code chart type (cht=qr), so no PHP QR library is needed. One line of jQuery sets an image src to the chart URL with window.location.href as the encoded data, so every page gets its own QR code. |
| /2010/when-uniqid-is-too-slow-in-php/ | When uniqid is too slow in PHP | Profiling showed PHP's uniqid() dominating page time when generating six cross-site request forgery (CSRF) form tokens. Benchmarking uniqid against mt_rand and sha1(mt_rand()) found the hash of a random number 140 times faster, raising the question whether it stays unique enough for form tokens. |
| /2010/xfn/ | XFN - XHTML Friends Network | XFN, the XHTML Friends Network, marks relationships between people with rel attribute values on links, part of the semantic web and microformats. Skeptical take: blogrolls are a dying niche, social networks resist public relationship data, and users will mislabel friend, met, or colleague. |
| /2011/a-faster-alternative-to-image-beacons-with-1x1-gif/ | A faster alternative to image beacons with 1x1 gif | Tracking pixels and image beacons load a transparent 1x1 GIF and parse the access logs. Returning HTTP 204 No Content from the tracking URL instead logs the hit while sending no body: less data for the browser, less work for the server. |
| /2011/add-phpunit-listeners-to-watch-for-long-running-tests/ | Add PHPUnit Listeners to Watch for Long Running Tests | A PHPUnit test listener, registered in phpunit.xml, that implements PHPUnit_Framework_TestListener and checks endTest() timing to print a red terminal warning for any test running longer than two seconds. Catching slow tests early keeps the suite fast. |
| /2011/adding-subdomain-routes-for-all-urls-in-zend-framework/ | Adding Subdomain Routes for all URLs in Zend Framework | Zend Framework 1 hostname route examples map one subdomain to one controller and action. For a multi-site CMS on a shared codebase, loop over every registered route after addDefaultRoutes() and add a copy chained to a Zend_Controller_Router_Route_Hostname, so a sitename parameter reaches every controller. |
| /2011/count-lines-of-code-in-eclipse/ | Count lines of code in Eclipse | Counting lines of code (LOC) in Eclipse with the built-in file search: a regular expression that matches lines containing at least one non-whitespace character, scoped to source extensions like .php and .js, so blank lines do not inflate the total. |
| /2011/easy-flash-messenger-messages-in-zend-framework/ | Easy Flash Messenger Messages in Zend Framework | Typed flash messages in Zend Framework: the controller adds a one-element array keyed by success or failure to the FlashMessenger action helper, and a custom view helper pulls them through getStaticHelper and renders a ul with each type as the li class. |
| /2011/facebook-connect-an-unknown-error-occurred-but-why/ | Facebook connect: An unknown error occurred. - but why?? | Facebook Connect's JavaScript API popped up an unknown error after copying working code to a second site with a new app ID. The cause was the app's site domain setting in the Facebook developer portal, which had to match the new domain exactly. |
| /2011/forcing-utf-8-in-zend-framework-with-pdo/ | Forcing UTF-8 in Zend Framework with PDO | Getting a Zend Framework application to talk UTF-8 to MySQL: create the schema with a utf8 character set, declare tables with utf8, and in application.ini set the pdo_mysql charset param plus a driver option issuing SET NAMES utf8 on connect. Character encoding, garbled text. |
| /2011/google-analytics-campaign-link-builder-bookmarklet/ | Google Analytics Campaign Link Builder Bookmarklet | A JavaScript bookmarklet that builds Google Analytics campaign tracking URLs: it prompts for utm_source, utm_medium, utm_term, utm_content, and utm_campaign, defaults the destination to the current page, requires the mandatory fields, URL-encodes everything, and alerts the finished link. UTM parameters, URL builder. |
| /2011/jquery-form-plugin-does-not-submit-submit-buttons-named-submit/ | jQuery form plugin does not submit submit buttons named submit | In Google Chrome, the jQuery Form plugin drops a submit button named submit from the submission; Firefox and Internet Explorer send it. Renaming the button to submitbutton fixes it. The well-known pitfall of naming a form field submit, which shadows the form's submit() method. |
| /2011/jquery-mobile-presentation-from-milwaukee-web-design-meetup/ | jQuery Mobile Presentation from Milwaukee Web Design Meetup | Slides from an introductory jQuery Mobile talk given at a local web design meetup, offered as a PDF download. An introduction to the jQuery Mobile framework for building touch-friendly mobile web interfaces, for anyone starting with mobile web development. |
| /2011/mailchimp-overview-presentation-waukesha-area-internet-marketing-meetup/ | Mailchimp Overview Presentation: Waukesha Area Internet Marketing Meetup | Slides from a meetup presentation giving an overview of Mailchimp as email marketing software, covering the basics for comparison against services like Constant Contact. The PowerPoint deck is available for download. Email newsletters and mailing list software. |
| /2011/milwaukee-php-users-group-1st-year-anniversary/ | Milwaukee PHP Users Group - 1st year anniversary | A short announcement of the Milwaukee PHP Users Group (MKEPUG) one year anniversary meeting, with a link to the event listing and an invitation to attend. Local PHP community, user group meetups, and developer networking in Milwaukee, Wisconsin. |
| /2011/mod-rewrite-to-index-php-file-the-easy-way/ | Mod Rewrite to index.php file, the easy way | Replace the usual six line Apache mod_rewrite block that sends every request for a missing file or directory to index.php (the Zend Framework .htaccess pattern) with a single mod_dir directive: FallbackResource /index.php. Front controller routing without RewriteCond and RewriteRule. |
| /2011/php-doc-for-non-declared-properties-in-your-class/ | PHP Doc for non declared properties in your class | Documenting dynamic properties served by `__get` and `__set` magic methods, common in Zend Framework classes: the PHPDoc @property tag declares each virtual property's type and description on the class so IDE autocomplete offers properties that are never declared in code. |
| /2011/phpunit-error-no-release-available-for-package/ | PHPUnit error: No release available for package | Installing PHPUnit through PEAR failed with no release available for package pear.phpunit.de/PHPUnit. Running pear remote-list exposed the real problem: the cache_dir /tmp/pear/cache did not exist and could not be created. Creating that directory with mkdir let pear install phpunit/PHPUnit succeed. |
| /2011/phpunit-error-with-zend-session/ | PHPUnit error with Zend_Session | Running PHPUnit against Zend Framework code threw Zend_Session_Exception: session must be started before any output has been sent, because the test printer already wrote output. Calling ob_start() in the bootstrap works, but setting Zend_Session::$_unitTestEnabled = true is the cleaner fix. |
| /2011/presenting-at-joomla-day-midwest/ | Presenting at Joomla Day Midwest | A news update: a talk on cloud hosting and SaaS at Joomla Day Midwest, with the slides as a downloadable PDF, alongside a Zend Filter presentation, a PHP design patterns talk, Linux+ certification study, PHP user group leadership, and upcoming blog topics. |
| /2011/remove-svn-folders-from-eclipse-project-explorer/ | Remove .svn folders from Eclipse Project Explorer | Hide Subversion's .svn directories and their svn-base copies from the Eclipse workspace and search results: right click the project, open Properties, choose Resource, add a resource filter of type exclude, and enter the .svn folder name. Cleans up Project Explorer clutter in Eclipse PDT. |
| /2011/scoping-your-bookmarklet-correctly/ | Scoping your Bookmarklet Correctly | A bookmarklet runs in the page's global scope, so var counter in a javascript: URL overwrites a global of the same name on the host page. Wrap the bookmarklet body in an immediately invoked anonymous function, an IIFE, so its variables stay in function scope. |
| /2011/service-class-methodology/ | Service Class Methodology | A service class owns a dumb business object's whole lifecycle: loading it from a data source, manipulating it, saving it, and handing it to the view. Models know only themselves, never extending Zend_Db_Table. Service layer example in Zend Framework: User model and UserService. |
| /2011/settimeout-proper-syntax/ | setTimeout proper syntax | JavaScript setTimeout takes a function reference as its callback. Writing setTimeout(myTime(), 5000) calls the function immediately and passes its return value, so nothing is delayed. Pass setTimeout(myTime, 5000) instead. A string argument works but is bad form: it gets evaluated like eval. |
| /2011/setting-php-command-line-colors/ | Setting PHP Command Line Colors | Color terminal output from a PHP CLI script by echoing the ANSI escape character \033 followed by a bash color code such as [34m for blue, then the text, then [0m to reset to the default. Same codes as a colored bash prompt. |
| /2011/shape-the-sales-of-the-better-bachelor-10-dish-cook-book/ | Shape the Sales of The Better Bachelor 10 Dish Cook Book | A request for feedback on the single-page sales page for a bachelor cookbook sold as a download: what to change in the page copy or layout to increase sales, with free coupon codes for the two best tips. Landing page copywriting and conversion. |
| /2011/solving-the-problem-not-the-solution/ | Solving the Problem, Not the Solution | Stakeholders should define the problem and suggest solutions, not demand them; developers should ask why a feature or tool is requested, separate requirements from suggestions, and prove a better approach. Domain knowledge versus technical execution, requirements gathering, being told how to do your job. |
| /2011/testing-outgoing-email-with-zend-framework/ | Testing Outgoing Email with Zend Framework | Redirecting outgoing mail to a developer address in non-production environments when the mail server rejects plus-sign addressing: a Zend_Mail subclass overrides addTo, checks APPLICATION_ENV, swaps every recipient for a test address, and records the original recipient in an X-Test-Original-Email header. No code changes between environments. |
| /2011/testing-protected-and-private-attributes-and-methods-using-phpunit/ | Testing protected and private attributes and methods using PHPUnit | How to test protected and private members in PHPUnit when you choose to: read an attribute with PHPUnit_Framework_Assert::readAttribute, and call a method through ReflectionClass, getMethod, setAccessible(true), and invokeArgs. A helper with a static method keeps the reflection setup from being duplicated across tests. |
| /2011/themetemplate-system-in-zend-framework/ | Theme/Template System in Zend Framework | Building a theme system in Zend Framework 1 like those in WordPress, Drupal, and Joomla: a front controller plugin on routeShutdown swaps the view base path and layout path per theme, and a view helper resolves public asset URLs under /themes/name. |
| /2011/to-www-or-not-to-www/ | To WWW or not to WWW | Whether a site's canonical domain should carry the www prefix. Arguments for www: recognizable as a web address in print, cookieless subdomains for static assets, browser autocompletion. Against: shorter to say and type. Pick one and 301 redirect the other with Apache mod_rewrite. |
| /2011/useful-firebug-tricks-with-zend-framework/ | Useful Firebug Tricks with Zend Framework | Two Zend Framework 1 hooks into Firebug through the FirePHP add-on: enable Zend_Db_Profiler_Firebug in application.ini to profile database queries in the browser console, and add a Zend_Log_Writer_Firebug writer in the bootstrap so application log messages appear there too. Development environment only. |
| /2011/using-mysql-bit-field-as-a-flag-field-not-a-good-idea/ | Using MySQL Bit Field as a Flag Field: Not a Good Idea | An attempt to replace a char(1) flag column with a MySQL BIT(1) field to save space: inserting b'1' and b'0' then selecting returned only the letter b at every width tried. With export and import concerns on top, char(1) stays for boolean Y/N flags. |
| /2011/want-to-come-work-with-me-lets-do-some-php/ | Want to come work with me?  Let's do some PHP! | A job posting for a senior PHP developer to join a web team: five plus years of object oriented PHP, Zend Framework, jQuery, HTML5 and CSS3, REST and SOAP APIs, on-site in Milwaukee, mentoring juniors on long-lived business applications. The opening has since closed. |
| /2011/when-you-create-your-account-please-log-in/ | When you Create Your Account, Please log in. | Sign-up flows that make a new user log in again right after registering are a UX annoyance. A social media poll found 78 percent of respondents expect account creation to log them in automatically, so streamline onboarding by authenticating immediately after registration. |
| /2011/why-ip-authorization-no-longer-works-on-web-apps/ | Why IP Authorization No Longer Works on Web Apps | Authorizing or rate limiting web application actions by IP address fails because addresses don't map to people: dial-up reconnects, ISP content proxies, expiring DHCP leases, NAT at colleges and businesses, shared coffee shop wifi, and proxy browsing. Block subnets at the router instead. |
| /2011/zend-filter-presentation/ | Zend Filter Presentation | Slides from a Zend webinar on Zend_Filter, the Zend Framework input filtering component, shared as a PDF download. A short announcement post with no writeup of the content; data filtering and sanitization in PHP. |
| /2011/zend-framework-authentication-let-the-user-know-if-it-is-their-fault/ | Zend Framework Authentication: Let the user know if it is their fault | Login error messages with Zend_Auth: Zend_Auth_Result's getCode() distinguishes FAILURE_IDENTITY_NOT_FOUND and FAILURE_CREDENTIAL_INVALID, which are the user's mistake, from ambiguous or uncategorized failures, which are the system's. An isFailureUserBased() method picks between an incorrect username or password message and a contact support message. |
| /2011/zend-framework-bootstrap-vs-front-controller-plugin/ | Zend Framework: Bootstrap vs Front Controller Plugin | Keep web-only setup out of Bootstrap.php in Zend Framework 1, because the bootstrap also runs for command line scripts. View configuration such as doctype, encoding, and stylesheets belongs in a front controller plugin, registered in the bootstrap but invoked only on web requests. |
| /2011/zend-framework-front-controllers-vs-bootstrap-round-2/ | Zend Framework Front Controllers vs Bootstrap: Round 2 | Site initialization in Zend Framework 1 front controller plugins: which hook gets what. routeStartup for resources always needed and anything that may redirect (ACL, logging), dispatchLoopStartup for the view, dispatchLoopShutdown for page-only extras. Zend_Test_PHPUnit_ControllerTestCase resets the request and expects a traditional bootstrap. |
| /2011/zend-framework-to-include-or-not-to-include/ | Zend Framework: to include or not to include | Two ways to install Zend Framework: a copy in each project's library folder versus one shared server-wide install on the include path like PEAR. Pros and cons on version pinning, upgrade risk, and portability, landing on vendoring per project. Dependency management before Composer. |
| /2011/zend-framework-view-helper-for-qr-codes/ | Zend Framework View Helper for QR Codes | A Zend Framework view helper that builds a Google Charts URL for a QR code: chart type qr, size from width and height, data URL-encoded by http_build_query. The view wraps it in an img tag; caching through a model is a possible follow-up. |
| /2011/zend_db-fill-in-multiple-placeholders/ | Zend_db fill in multiple placeholders | Zend_Db's where() with several question mark placeholders and a single bound value repeats that value into every placeholder, so a search across email, first name, and last name with one LIKE pattern needs the parameter passed once. Zend Framework 1 database queries, query binding. |
| /2012/a-couple-cool-features-of-phpstorm-5/ | A couple cool features of PHPStorm 5 | First impressions after switching to PhpStorm 5: command line tool integration like Zend Framework's zf tool, file templates with conditionals, intentions that suggest refactors, live templates, live editing in Chrome, automatic saving, GitHub integration, and plugins. One complaint: no multiple projects in one window. |
| /2012/book-review-php-frameworks-and-applications/ | Book Review: PHP Frameworks and Applications | Review of Real-World Solutions for Developing High-Quality PHP Frameworks and Applications: foundations of quality software, testing beyond unit tests (front end, user acceptance, service layer APIs), best practices from well-known companies, architecture, planning, and technical debt. Recommended reading for a mid-level PHP programmer. |
| /2012/create-your-application-with-overtake-user/ | Create your application with "overtake user" | Build user impersonation into every application: let a support specialist or admin become another user's session to reproduce what they see without asking for a password. Two caveats: audit logs record actions as that user, and single sign-on or OAuth links can collide. |
| /2012/creating-a-custom-zend-framework-body-script-helper/ | Creating a custom Zend Framework Body Script helper | Zend Framework's HeadScript view helper only targets the document head, but non-critical JavaScript belongs at the bottom of the body. A BodyScript helper that extends HeadScript with its own registry key gives a separate, semantic placeholder before the closing body tag. |
| /2012/datetime-my-new-best-friend/ | DateTime: My new best friend | An appreciation of PHP's DateTime object: construct two dates from differently formatted strings and compare them directly with a greater than operator, no strtotime or timestamp math needed. A short example to prompt reading through the class's methods. |
| /2012/dont-always-lead-sometimes-just-be-great/ | Don't Always Lead: Sometimes Just Be Great | Career advice against the assumption that promotion into management is the only path forward: a great engineer promoted to boss often becomes unhappy while the department slips (the Peter Principle). Ask for money or seniority directly, and honestly weigh whether people management suits you. |
| /2012/duplicate-request-issued-in-zf-when-using-inline-scriptdocument-writeln-in-view/ | Duplicate request issued in ZF when using inline script/document.writeln in view | A Zend Framework form kept failing CSRF token validation because the page was being requested twice. The culprit was an inline document.writeln injecting a hidden img tag with src="#". Removing it stopped the extra request, much like a missing image src had before. |
| /2012/employed-resumes-vs-unemployed-resumes/ | Employed Resumes vs Unemployed Resumes | Two kinds of resume: the bragging resume full of abstract buzzwords that people write while employed, and the qualifications resume with numbers and concrete facts that gets someone hired. When job hunting, HR and hiring managers need exact skills and experience, not pretty words. |
| /2012/extend-zend-mail-to-use-views/ | Extend Zend_Mail to use Views | Extending Zend_Mail so email bodies render from Zend_View scripts and Zend_Layout templates: one setBody method that takes a type constant, a script name, and view parameters, for both HTML and text. Layout and view paths sit in overridable methods so unit tests can mock them. |
| /2012/filtering-and-validating-discussion/ | Filtering and Validating Discussion | A round-table among PHP developers on filtering versus validating: filter input, escape output; SQL injection and XSS; HTML Purifier over strip_tags; rejecting bad data before storage in data warehouses; distrusting third-party web service responses; where escaping belongs; and checking primitive parameters inside object graphs. |
| /2012/filtering-user-inputoutput-in-zend-framework/ | Filtering User Input/Output in Zend Framework | Two places to filter user input in Zend Framework: the escape view helper when displaying it, and Zend_Filter_Input with filters like StringTrim and Digits plus validators like Alpha before storing it. Essential whenever a form is handled without Zend_Form. Output escaping, XSS prevention. |
| /2012/finished-up-wordcamp-milwaukee-2012/ | Finished up WordCamp Milwaukee 2012 | A recap of presenting on secure WordPress coding at WordCamp Milwaukee, with the slide deck available as a PDF download and a promised link to the recorded talk on WordPress.tv. WordPress plugin security, conference talk. |
| /2012/follow-through-if-youre-trying-to-seduce-a-developer/ | Follow Through if You're Trying to Seduce a Developer | Recruiters and business owners pitching developers for jobs or advice often go silent after the first reply, like the characters in A Night at the Roxbury who cannot close the deal. Follow through: reply to a no, and propose a concrete time and place. |
| /2012/how-to-add-phpunit-to-your-include-path-for-autocompletion-in-eclipse-pdt/ | How to Add PHPUnit to your Include Path (for autocompletion) in Eclipse PDT | Configuring Eclipse PDT to get PHPUnit autocompletion without copying the library into the project: add the PHPUnit install as a User Library under the project's PHP Include Path, then rebuild the workspace. Match the installed version to the one running tests. |
| /2012/how-to-quickly-mask-a-credit-card-number/ | How to Quickly Mask a Credit Card Number | A one-line PHP snippet to mask a credit card number for display: str_pad the last four digits from substr to the original strlen with asterisks on the left, so the masked output matches the card's real length whether it has 15 or 16 digits. |
| /2012/idea-for-web-design-website/ | Idea for Web Design Website | A concept for a web design agency site using parallax scrolling with jQuery and CSS3 animation: frames tell the process as a story, from a client's lightbulb moment and napkin sketch, through designers and programmers, to the finished site and a billion dollar punchline. |
| /2012/ideas-online/ | Ideas Online | A concept for a public idea repository site: anyone submits an idea, others vote it up or down and tag it, a user can claim an implementation and post progress with automated check-in reminders, points reward participation, and duplicates get flagged as already done. |
| /2012/intelligently-combining-javascript/ | Intelligently combining Javascript | Whether to concatenate all JavaScript or keep files separate is a false dichotomy. Load shared libraries from a public CDN; visitors likely have them cached. Apply the Pareto principle (80/20 rule): bundle code used on most pages, split heavy single-page code and logged-in-only features. |
| /2012/jquery-validator-twitter-username-validator/ | jQuery Validator: Twitter username validator | A custom rule for the jQuery Validation plugin that validates a Twitter username: jQuery.validator.addMethod with a regular expression allowing one to 32 word characters, rejecting spaces, a leading @, and full URLs. Empty values pass so the field can stay optional. Custom validator methods. |
| /2012/keep-your-admin-interface-css-separate/ | Keep your Admin Interface CSS Separate | Combining all CSS into one file has a limit: admin styles belong in their own stylesheet. Semantic class names and IDs leak the admin area's structure and features to anyone reading public CSS, and the extra bytes slow visitors who never log in. |
| /2012/lessons-for-managers/ | Lessons for Managers | Early lessons from managing a team rather than holding the title: people are motivated differently (money, comfort, challenge, learning), some employees care and some are disengaged and both need attention, and ROI on tools is often the absence of loss and retaining talent. |
| /2012/php-can-be-easy-but-it-can-be-powerful-but-either-way-it-is-php/ | PHP can be easy, but it can be powerful, but either way, it is PHP | Defending PHP: the language is easy to learn, so novices who stop early call themselves experts, ship messy enterprise solutions, and PHP takes the blame. Facebook and Yahoo run on it. Java programmers who write Java in PHP should learn it properly first. |
| /2012/phpstorm-my-new-editor/ | PHPStorm: My new editor | Switching from Eclipse PDT to the PHPStorm IDE and paying for a commercial license, a hard step for an open source developer. Worth it for better code recognition, completion of one language embedded in another such as HTML inside PHP, and integrated test results. |
| /2012/phpunit-mock-objects-only-mock-what-you-need/ | PHPUnit Mock Objects: Only Mock What You Need | getMock() in PHPUnit replaces every method of the class with an empty stub, so testing legacy code meant stubbing each method by hand. Instead, use getMockBuilder with setMethods, or the second argument to getMock, to mock only the named methods (a partial mock). |
| /2012/read-something-else/ | Read Something Else | Advice from a mentor: read outside your field. Biographies, history, sales books, and fiction help a programmer avoid burnout, refocus, find inspiration, and grow vocabulary eroded by IM and texting. Examples include Richard Branson's Losing My Virginity and Walter Isaacson's Steve Jobs biography. |
| /2012/removing-the-extra-elements-on-zend_form-for-submit-button/ | Removing the extra elements on Zend_Form for Submit Button | A Zend_Form submit button rendered with the default decorators emits an empty dt alongside its dd, because the label sits on the button itself. Instead of hiding it with CSS, reset the decorators to ViewHelper plus an HtmlTag dd with the standard id suffix. |
| /2012/running-phpunit-on-remote-system-from-command-line/ | Running PHPUnit on Remote System from Command Line | A two-line bash script that runs PHPUnit on a remote development server over SSH, using shared keys and an .ssh/config host alias, then wired into Eclipse PDT as an External Tool so the selected test file or folder runs from the IDE. |
| /2012/service-profile-mketweeps/ | Service Profile: mketweeps | A Twitter bot that found Milwaukee people by profile location or keyword and tweeted an hourly shout-out mentioning them, built as a Zend Framework command line cron job using OAuth. Lessons: keep up when APIs change, follow the people you mention, and plan monetization. |
| /2012/services-mappers-models-enterprise-thinking-in-php-presentation/ | Services, Mappers, Models: Enterprise Thinking in PHP {presentation} | Slides from a PHP user group talk on enterprise PHP architecture: separating data sources, data retrieval, and business logic into models, mappers, and services (separation of concerns, the data mapper pattern), with the gains from the approach and demonstration code. Linked on SlideShare. |
| /2012/site-profile-amazondiscount-me/ | Site Profile: amazondiscount.me | Retrospective on a retired Amazon affiliate site listing category discount links with an affiliate tag. Technical notes: deflate compression, one year cache expirations, CSS sprite for category icons. Lessons: shoppers search Amazon instead of browsing, and a page with no text does not rank. |
| /2012/site-profile-honorarybrother-com/ | Site Profile: honorarybrother.com | A retired site that emailed a man an honorary brother certificate for a woman, an early Zend Framework project replacing a homegrown framework: PDO prepared statements, AJAX with Zend_Json, jQuery SimpleModal. Lessons: forging an email from address triggers spam filters; research SEO first. |
| /2012/site-profile-jemdiary-com/ | Site Profile: jemdiary.com | Retrospective on a long-running free online diary site, from procedural PHP4 to a homegrown MVC framework: static home page generation and asset caching for a slow host, diary export to Word, and an automated welcome comment bot. Lessons: keep current, run ads, fight spam. |
| /2012/site-profile-name-tag-me/ | Site Profile: name-tag.me | A retired site that rendered a 'hello my name is' name tag with a handwritten font instead of images. Technical notes: Open Graph image, mod_expires and mod_deflate caching, IE conditional comments, ZeroClipboard, @font-face, PNG download via imagettftext. A hyphenated domain hurt word of mouth. |
| /2012/site-profile-thebetterbachelor-com/ | Site profile: thebetterbachelor.com | Selling a bachelor cookbook PDF from a one page sales site: PayPal IPN emails a download link, Zend Framework stamps the buyer's name and email on every page, coupon codes bypass checkout. Lessons: give away a free chapter; a newsletter funnel beats a pitch. |
| /2012/site-profile-whatscloseto-me/ | Site profile: whatscloseto.me | Retrospective on a local business finder that used Google IP geolocation and the Google Maps API to plot nearby establishments. Lessons: thin content hurt search indexing, no monetization, result lists that looked like ads, and no way to change your detected location. |
| /2012/split-readwrite-connections-in-zend-framework-database-on-the-application-level/ | Split Read/Write Connections in Zend Framework Database on the Application Level | Separating read and write database connections in Zend Framework code instead of MySQL Proxy: a second Zend_Db adapter in the bootstrap stored in Zend_Registry, and a Zend_Db_Table_Abstract subclass that swaps to the write adapter around insert and update. Read replica, master and slave splitting. |
| /2012/store-zend-framework-options-in-the-registry/ | Store Zend Framework Options in the Registry | A Zend Framework bootstrap method, _initOptionsRegistry, that wraps the config file options from getOptions() in a Zend_Config object and stores it in Zend_Registry under the options key, so configuration is reachable anywhere without going back to the front controller or bootstrap. |
| /2012/testing-error-500-pages-in-zend-framework/ | Testing Error 500 Pages in Zend Framework | Unit testing a custom error controller in Zend Framework with Zend_Test_PHPUnit_ControllerTestCase: a real fatal error would break the suite, so set a Zend_Controller_Exception on the response with setException, dispatch any route, and assert the error controller and action rendered the Error 500 heading. |
| /2012/thatconference-slides/ | ThatConference Slides | Announcement of a talk at That Conference at the Kalahari Resort in Wisconsin Dells, Your First Five Design Patterns, an introduction to PHP design patterns, with the slides posted afterward on SlideShare. A short conference note with a link and nothing more. |
| /2012/the-importance-of-adding-auditing-to-your-applications/ | The Importance of Adding Auditing to Your Applications | Auditing as change tracking: full journaling that stores a new revision per edit versus action-based logging of who changed what. Make auditing a requirement even when stakeholders never ask. Logging the browser at login speeds support; failed-login records feed fail2ban. |
| /2012/the-importance-of-catching-your-breath/ | The Importance of Catching Your Breath | Taking a real break from work and side projects, without guilt, restores energy and avoids burnout. Pausing a project has its own payoffs: reflecting on what has been accomplished, and using the breather to fix bugs and shore up the foundation. |
| /2012/the-point-of-custom-exceptions/ | The point of custom exceptions | Why define custom exception classes in PHP that merely extend Exception: catching a specific type lets code degrade gracefully, disabling a failed third-party API feature instead of returning a 500, or throwing a record-not-found exception rather than pre-validating IDs. |
| /2012/trackthings-com/ | TrackThings.com | An idea: a web app for field sales reps to log which businesses they visited, what promotional material they dropped off, and who showed interest, using GPS and Foursquare check-ins to fill in location data and feed a campaign owner's CRM and map. |
| /2012/translate-your-error-messages-in-your-zend-framework-controllers/ | Translate Your Error Messages in Your Zend Framework Controllers | Internationalization in Zend Framework: translating the rare error messages raised in controllers, not just form validator messages. Store a Zend_Translate instance in Zend_Registry from the bootstrap and translate a flash messenger message before it is persisted. Translating in the view is the open alternative. |
| /2012/twitter-book/ | Twitter Book | An idea for a site that curates tweets into shareable books: each book has a title, description, and unique link, filled with tweets matched by hashtag, location, or date range and paged chronologically, with a Google map for browsing location-based books near stadiums. |
| /2012/two-column-background-colors-demo/ | Two Column Background Colors Demo | CSS for equal height columns, the faux columns problem: two columns of unequal content that need a full-height background color. Give the container a width and background, float the fixed-width primary column, set margin-left on the secondary, and clearfix with an :after pseudo-element. |
| /2012/using-custom-view-scripts-for-zend_form-in-zf1/ | Using custom view scripts for Zend_Form in ZF1 | Zend_Form in Zend Framework 1 separates form logic (validators, filters, isValid) from rendering (decorators and view helpers). To add display-only markup like a help link beside an element, use the ViewScript decorator with a .phtml view script instead of stuffing HTML into the form. |
| /2012/web-developers-invest-the-time-to-create-your-page/ | Web Developers: Invest the time to create your page! | Every excuse a web developer gives for not having a personal website or portfolio, answered. Too busy, enough word of mouth work, nobody pays for it: a portfolio is a career investment like college, and the internet is global. Start now and blog. |
| /2012/what-i-look-for-in-a-code-review/ | What I Look For in a Code Review | What a code review checks: bugs a lint tool like PHP lint or JSLint would catch, coding standards (PEAR, Zend Framework), consistent architecture, agreed patterns followed, and legibility: reinventing the wheel over PHP's array functions, methods doing too much. Reviews are teachable moments. |
| /2012/when-writing-unit-tests-test-against-absolutes/ | When Writing Unit Tests, Test Against Absolutes | A unit test for XML generation should assert against a literal XML string, not against output rebuilt with DomDocument the same way the code under test builds it. Testing through the library under test proves nothing; assertEquals against a known absolute value. |
| /2012/zend-framework-acl-inherits-including/ | Zend Framework ACL: Inherits Including | Zend_Acl's inheritsRole() in Zend Framework 1 returns false when the role being tested is the role itself. A subclass adds inheritsRoleIncluding(), which falls back to an equality check so a role counts as inheriting from itself. Role based access control, ACL role hierarchy. |
| /2012/zend-framework-add-html-to-form-element-label/ | Zend Framework: Add HTML to form element label | Zend_Form escapes element labels by default, so a link in a Terms of Use checkbox label renders as literal HTML. Fix: call setOption('escape', false) on the element's Label decorator, with a warning to be careful about turning escaping off. Zend Framework 1 forms. |
| /2012/zend-framework-static-router-urls-not-found-in-zend-navigation/ | Zend Framework Static Router URLs Not Found in Zend Navigation | Static routes added with Zend_Controller_Router_Route_Static for CMS article pages matched fine but never showed as active in Zend_Navigation, since the URL helper could not tell which route to reassemble. Adding 'route' => 'default' to the route defaults fixed it. Zend Framework 1 routing. |
| /2012/zend-framework-use-filter-with-paginator/ | Zend Framework Use Filter with Paginator | Zend_Paginator returns plain arrays unless the database row has a class. Its setFilter() takes any Zend_Filter_Interface object and applies it to every item, so a filter that constructs a domain object from each array turns paginated results into objects. Zend Framework 1 pagination. |
| /2013/beware-of-javascript-frameworks-extending-the-array/ | Beware of Javascript frameworks extending the Array | MooTools extends the Array prototype, so a for-in loop over an array in a third-party widget also iterates the added methods and passes functions where strings were expected. Widgets cannot assume which framework is loaded, so never use for-in to iterate a JavaScript array. |
| /2013/car-analogy-to-explain-old-browser-support/ | Car analogy to explain old browser support | An analogy for explaining outdated browser support to non-technical people: supporting old browsers is like a gas station keeping a whole tank of leaded gasoline for the few cars that still use it, when the effort belongs on the unleaded demand everyone else has. |
| /2013/conways-game-of-life-in-javascript/ | Conway's Game of Life - In Javascript | A first attempt at Conway's Game of Life, the cellular automaton, in plain JavaScript on an HTML5 canvas: a random 500 by 500 grid, a neighbor count for each cell, the live and dead rules, and a setInterval loop that repaints every generation. |
| /2013/developers-how-to-get-your-estimates-right/ | Developers: How to get your estimates right | Estimation in three steps: estimate every task, asked or not, and track actuals to learn your speed; plan in chunks with probable (+20%) and worst case (+40%) figures plus 20% for unknowns; say no to scope creep, or put a number on the yes. |
| /2013/enable-your-blog-comments-already/ | Enable your blog comments already! | A rant at technical blogs that disable comments or push discussion to Twitter: 140 characters cannot hold a code correction or a real question. Spam is a weak excuse; deleting a few pending comments costs less than losing reader feedback. |
| /2013/equal-distance-menu-items/ | Equal distance menu items | CSS for a horizontal navigation menu whose items spread evenly across the full width and still wrap instead of overflowing: text-align justify on the ul, inline li, inline-block anchors, and a full-width inline-block :after pseudo-element to force the last line to justify. |
| /2013/feedly-background-tab/ | Feedly Background Tab | A Chrome extension that opens the current Feedly story in a background tab with the b key, for people who moved from Google Reader and missed middle-click behavior on the keyboard shortcut. Source is on GitHub. |
| /2013/firephp4chrome-chrome-extension/ | FirePHP4Chrome Chrome Extension | A Chrome extension bringing FirePHP server-side debug output into the Chrome developer console, written because the existing extensions were broken or outdated. Since removed from the Chrome Web Store, with the GitHub repository archived. PHP debugging, console logging from PHP. |
| /2013/google-map-markers-distorted-with-bootstrap/ | Google Map Markers Distorted with Bootstrap | Twitter Bootstrap's default image max-width rule squashes Google Maps marker images so they render distorted. The fix is a CSS override scoped to the map container that sets max-width: none on images from maps.gstatic.com, using an attribute selector on the src prefix. |
| /2013/group-coding-results-tech-on-tap-version/ | Group Coding Results: Tech on Tap Version | A group coding challenge in vanilla PHP, no framework: a contact form that emails the site owner with mail(). It validates and sanitizes with filter_var, caps field lengths, stores errors in the session, and uses post/redirect/get so the browser never shows Webpage Expired. |
| /2013/how-to-discover-talent-not-just-skill/ | How to discover talent, not just skill | Talent is natural ability, skill the practiced mechanics of it. Signs of a talented programmer when hiring: artistry in code structure, talking about creating rather than completing tasks, compulsive tinkering with new technology, refactoring unfinished work, thinking in abstractions and recursion. |
| /2013/how-to-structure-your-controller-service-and-mapper-method-names/ | How to structure your controller, service and mapper method names | A naming heuristic for layered applications: controller actions are user speak, named for what a person would say they want; service methods are programmer speak, what the code does; mapper methods are data speak, what happens to the data going in or out. |
| /2013/html5-css3-javascript-only-photobooth-with-image-download/ | HTML5, CSS3, Javascript ONLY Photobooth with Image Download | A browser-only webcam photo booth with no Flash, server, or jQuery: getUserMedia streams to a video element, CSS3 filters apply, a click draws the frame to canvas, HTML5 drag and drop places decorations, and toDataURL with a Blob download saves the PNG. Chrome only. |
| /2013/idea-bible-verse-a-day/ | Idea: Bible Verse A Day | An idea for a daily Bible verse email service: a single-field signup page, a choice between random verses or a sequential reading plan from Genesis, double opt-in through Mailchimp, settings links in every email, and the World English Bible to avoid translation copyright restrictions. |
| /2013/idea-cms-agnostic-cloud-storage-plugin/ | Idea: CMS agnostic cloud storage plugin | After surveying cloud storage and CDN extensions for Drupal, Joomla, and WordPress, the idea: build one core library (something like SimpleCloud) for storing and retrieving assets in Amazon S3 and other providers, then ship a thin plugin per CMS so the core evolves independently. |
| /2013/idea-codereviewwith-me/ | Idea: CodeReviewWith.Me | A shelved idea: paid one-on-one code review over screen share, booked through a PayPal button that redirects to a Google Calendar appointment slot, conducted on Skype or Google Hangout, covering PHP, MySQL, JavaScript, and front-end code. Dropped because it would need too much marketing. |
| /2013/idea-extra-analytics-information/ | Idea: Extra Analytics Information | An idea for a tiny JavaScript analytics library on window.performance.getEntries() and the Resource Timing API: record each visitor's connection timing to answer how many arrive with an empty cache, when DNS lookups run slow, whether a CDN helps, and whether logged-in users load faster. |
| /2013/idea-voice-controlled-paginator-rss-reader/ | Idea: Voice Controlled Paginator / RSS Reader | An idea for a hands-free RSS reader: parse feeds into list items, page between them with CSS transitions, and drive it with voice commands through the annyang.js speech recognition library (say next, previous, or open), with optional text to speech reading articles back. |
| /2013/integrating-php-qa-tools-with-phing/ | Integrating PHP QA Tools with Phing | A Phing build.xml that runs PHP QA Tools and writes reports: phploc, PHP_CodeSniffer (phpcs), phpcpd copy paste detection, PHPMD mess detection, and PHPUnit, each wrapped in trycatch so a missing tool prints install instructions. phpcs and phpmd get a separate exit code 127 check. |
| /2013/it-is-important-to-use-die-after-a-header-redirect-heres-why/ | It is important to use die() after a header redirect - here's why | Why a PHP header('Location') redirect must be followed by die() or exit: the script keeps running, so an unauthenticated request still executes actions like deleting records. Demonstrated by fetching the page with a PHP stream context that ignores redirects and reading the protected output. |
| /2013/make-sure-not-to-too-future-proof-your-code/ | Make sure not to "too" future-proof your code | Over engineering for hypothetical future needs (YAGNI, premature abstraction) can triple a project's timeline. Balance modularity against deadlines: ship working code with a pluggable skeleton, like a factory method that returns the one implementation you need today, and add others when they are real. |
| /2013/midwestphp-2013-talk-slides-and-files/ | MidwestPHP 2013 Talk: Slides and Files | Slides and sample code from a MidwestPHP conference talk on enterprise PHP architecture through design patterns and modularization. The PDF deck and a zip of the example code are both available for download. Conference presentation on structuring large PHP applications. |
| /2013/my-stackoverflow-rant/ | My StackOverflow Rant | Frustrations with contributing to Stack Overflow as an occasional visitor: questions get closed as off topic after you have written an answer, lazy one-line answers get upvoted, and hard questions go unanswered. The site has value, but the barrier to giving back feels high. |
| /2013/mysql-explain-rows-column-really-is-an-estimate/ | MySQL Explain rows column really IS an estimate | A small demonstration that the rows column in MySQL EXPLAIN output is an estimate, not a count: a two-row city table shows rows: 3 during a full table scan on a three-way join. Adding an index on the name column made the estimate exact. |
| /2013/phpstorm-project-launchers-for-ubuntu/ | PHPStorm Project Launchers for Ubuntu | The Unity Dash icon PHPStorm installs on Ubuntu always reopens the last project. Since phpstorm.sh takes a project directory as its argument, a .desktop file per project in ~/.local/share/applications lets you type the project name into the Dash and launch straight into it. |
| /2013/quick-performance-test-with-apaches-ab/ | Quick performance test with apache's ab | Load testing with ab, the Apache Benchmark tool: run concurrent requests against a page and read requests per second, time per request, and the percentage served within a given time. A contrived PHP sleep() example shows the numbers before and after a fix. |
| /2013/release-the-quality-youd-expect/ | Release the quality you'd expect | Do not ship a feature that breaks an existing one with a promise to fix it later. Imagine a site you rely on leaving a feature broken for weeks: would you stay, would you still trust it? The golden rule for software releases. |
| /2013/review-learning-javascriptmvc-by-packt/ | Review: Learning JavaScriptMVC by Packt | Book review of Learning JavaScriptMVC from Packt, 8.5 out of 10: a clear todo-list walkthrough of the framework's parts, StealJS, DocumentJS, FuncUnit, and jQueryMX, plus a closing case study on planning and building an app. StealJS gets too few pages. |
| /2013/should-you-use-software-you-are-making-new-again/ | Should you use software you are making new again? | When rewriting an old application, use the old version just enough to understand the goal it solves, not enough to become a power user, so its legacy interface assumptions do not shape the new design. Incremental changes need deep familiarity; a rewrite needs distance. |
| /2013/slides-from-thatconference/ | Slides from thatConference | Download link for the slides of a conference talk, 7 Things I Wish Someone Would Have Told Me, career lessons for programmers, presented at That Conference. A short announcement with a PDF and nothing more. |
| /2013/slowing-down-my-blog-frequency/ | Slowing Down My Blog Frequency | Ending a years-long commitment to post every Tuesday, trading a rigid weekly schedule for posting only when there is something worth saying, to protect quality. Lists what takes the time instead: health, managing a team, user groups, a magazine column, a book. |
| /2013/speaking-at-milwaukee-wordcamp-2013/ | Speaking at Milwaukee Wordcamp 2013 | Announcement of a talk at Milwaukee WordCamp, 7 Things I Wish Someone Would Have Told Me, lessons from years as a programmer aimed at newer developers and the mentors teaching them, following a WordPress plugin security talk at the previous WordCamp. |
| /2013/testing-mysql-user-info-passwords/ | Testing MySQL User Info Passwords | Auditing a user table for the most common passwords with SQL alone: compare stored hashes against MD5 of password, 123456, qwerty and friends, and for a salted SHA1 scheme compute sha1(concat(guess, salt)) per row. Unsalted MD5 and weak passwords both turn up matches. |
| /2013/the-very-bad-things-that-javascript-can-do-part-1/ | The Very Bad Things That Javascript Can Do: Part 1 | Why a CMS should not let clients paste custom HTML and JavaScript into their sites. Rebuts the usual arguments: clients are not programmers, botnets and random attackers target small sites too, and a hacked client site is always the host's reputation problem. |
| /2013/the-very-bad-things-that-javascript-can-do-part-2/ | The Very Bad Things That Javascript Can Do: Part 2 | Proof-of-concept attacks hidden in a fake award badge snippet: a PNG that is really PHP logging visitors and user agents, cookie theft of PHPSESSID for session hijacking, obfuscated JavaScript, denial of service via hidden image requests, and CSRF-style form submission as the victim. |
| /2013/theoretical-thoughts-on-data-object-validation/ | Theoretical Thoughts on Data Object Validation | Argues for dumb data objects (an anemic domain model) with validation in separate domain objects. A data object can check sanity bounds like non-zero size, but business rules like stocking only one-terabyte drives or wholesale discounts belong in a domain or service object. |
| /2013/treat-arrays-right-iterating-properly/ | Treat Arrays Right: Iterating Properly | PHP array iteration best practice: use a for or foreach loop when only reading values, and array_walk or array_map when changing them or building a new array, since the C-implemented core functions beat a hand-written loop. Printing from array_walk misuses the function. |
| /2013/ubuntu-lightdm-login-manager-does-not-display-properly-on-boot/ | Ubuntu: lightdm login manager does not display properly on boot | Fix for Ubuntu 11.10 and 12.04 hanging at boot after the checking battery state message instead of showing the LightDM login screen, caused by the nvidia driver and boot timing: edit /etc/init/lightdm.conf and add sleep 1 before exec lightdm. |
| /2013/usb-chrome-debugging-and-local-subnet-surfing-on-android-emulator/ | USB Chrome Debugging and Local subnet surfing on Android Emulator | Testing sites on a private subnet from the Android emulator: install Chrome through adb, enable USB debugging on the device and in Chrome, then a bash script pushes a custom hosts file and forwards port 9222 to chrome_devtools_remote for desktop Chrome DevTools remote debugging. |
| /2013/ux-on-date-range-mark-only-one-in-error/ | UX: On date range, mark only one in error | Form validation UX for a start and end date pair: when the end date falls before the start, flag only one field as invalid, not both, since only one is wrong. Mark the end date, because people think of events by when they begin. |
| /2013/video-post-enterprise-php-through-design-patterns-and-modularlization/ | Video post: Enterprise PHP through Design Patterns and Modularlization | A seven part screencast, with slides and source code, of a conference style presentation on enterprise PHP: applying design patterns and modularization to structure larger applications. Originally recorded as a Udemy course that was rejected for length and audio quality. |
| /2013/vmware-script-to-toggle-vmware-workstation-startsuspend/ | VMWare Script to Toggle VMWare Workstation start/suspend | A bash script that toggles a VMware Workstation virtual machine between running and suspended: vmrun list checks whether the .vmx is running, then vmrun suspend soft or vmrun start. A .desktop launcher entry on Ubuntu makes it a one-click project switch. |
| /2013/why-more-creators-are-not-entrepreneurs/ | Why more creators are not entrepreneurs | A demo met first with why doesn't it do this prompts a theory on why programmers rarely become entrepreneurs: software must not fail, so they hate failure, while business accepts losses; and creators believe a thing should sell itself, so they can't sell. |
| /2013/wmcpa-2013-this-means-that-presentation/ | WMCPA 2013: This Means That Presentation | Slides from a conference talk translating between PHP and IBM System i (AS/400, iSeries) terminology, mapping concepts from one world to the other for midrange computer professionals learning PHP. Downloadable PDF only, no writeup. |
| /2013/wmcpa-intro-to-php-talk-presentation-notes-links/ | WMCPA: Intro to PHP Talk Presentation, Notes, Links | Slides and links from an introduction to PHP talk for midrange computer professionals: local development environments (WAMP on Windows, MAMP on Mac, tasksel lamp-server on Linux), the php.net manual with its function name URL shortcut, and beginner PHP tutorials. |
| /2013/wordcamp-milwaukee-2013-talk-slides/ | WordCamp Milwaukee 2013 Talk + Slides | Slides from a WordCamp Milwaukee talk, a seven things list, linked as a PDF download along with the Joind.in speaker feedback page. Short announcement with no writeup of the talk content; WordPress conference speaking. |
| /2013/would-you-find-value-in-a-one-on-one-video-code-review-session/ | Would You Find Value in a One on One Video Code Review Session? | Proposal for a paid one on one code review service over Skype or Google Hangout: application form, payment, a 30 minute screen share walking through the code, ratings afterward. Open questions on scalability, pricing, and disagreement with the advice. Mentoring, consulting business idea. |
| /2013/zend-framework-1-and-jquery-validate-plugin-how-to-create-passwordconfirm-easily-in-zend_form/ | Zend Framework 1 and jQuery Validate Plugin: how to create password/confirm easily in Zend_Form | Password and confirm password fields in a Zend Framework 1 Zend_Form that validate on both sides: StringLength and Identical validators with a token pointing at the password element, plus required class and equalTo attributes that the jQuery Validate plugin picks up client side. |
| /2013/zend-framework-1-form-captcha-idea/ | Zend Framework 1 Form Captcha Idea | A honeypot CAPTCHA for Zend Framework 1 forms: a hidden field that humans never see but bots fill in. Implemented by extending Zend_Captcha_Word so render() returns nothing and the generated word is empty, so any submitted value fails validation. Anti-spam, hidden field CAPTCHA. |
| /2013/zend-translate-plurals/ | Zend Translate Plurals | Plural translations in Zend Framework 1: Zend_Translate's translate() accepts an array of the singular form, the plural form, and the count, and picks the right one. A reminder to read the manual (RTFM) before fighting the framework; internationalization, i18n, localization. |
| /2014/3d-printing-makes-manufacturing-more-like-programming/ | 3D Printing Makes Manufacturing More Like Programming | Programming builds most of a product from a blank page, while traditional manufacturing assembles mostly standard parts, an inverted 80/20 split that explains why developers ask so many detailed questions. 3D printing hands manufacturers a blank slate too, which should bridge that requirements gap. |
| /2014/android-emulation-in-virtualbox-400-faster/ | Android Emulation - in VirtualBox - 400% faster! | The stock Android emulator is slow for QA browser testing. Install Android-x86 ISOs into VirtualBox instead, add phone screen sizes like 360x640 with vboxmanage CustomVideoMode and UVESA_MODE entries in the GRUB menu.lst, and turn on natdnshostresolver so the VM honors the host's hosts file. |
| /2014/dont-underestimate-the-importance-of-work-space/ | Don't Underestimate the Importance of Work Space | An office reshuffle revealed how much uncertainty about desks, seating, and neighbors stresses a team. Workspace takes a third of an employee's day, and studies tie personalized space to higher productivity, so managers should communicate more and heed what staff want from their space. |
| /2014/html5-placeholder-not-always-the-best-choice/ | HTML5 Placeholder: Not Always the Best Choice | A search form kept receiving terms with a leading space. The likely cause: the HTML5 placeholder attribute stays visible until the first keystroke, so novice users pressed space to clear it before typing. Consider your audience's computer literacy before adopting new UI patterns. |
| /2014/jquery-code-to-manage-check-all-uncheck-all-checkboxes/ | jQuery code to manage check all / uncheck all checkboxes | A jQuery snippet for a check all checkbox driven by data attributes: data-checkall-control on the master and data-checkall-group on the children. Unchecking any child unchecks the master, checking every child checks it. A select all UX convention so familiar nobody notices it until it breaks. |
| /2014/keep-mentoring-even-if-it-looks-like-no-one-cares/ | Keep Mentoring, Even If It Looks Like No One Cares | Mentoring often feels futile: people ask for resume feedback or career advice, follow up once, and vanish with nothing changed. The occasional person who takes even a fraction of the advice and improves is why to keep going. Focus on the ten percent who listen. |
| /2014/morning-motivational-email-to-text/ | Morning Motivational Email to Text | A PHP cron script that pulls a YouTube playlist through the old GData JSON feed, picks a random video, and emails the link to a phone's email-to-SMS gateway each morning as a motivational nudge. Two classes: a playlist fetcher and a mail sender. |
| /2014/preparing-a-conference-tell-your-presenters-this/ | Preparing a Conference?  Tell Your Presenters This | A checklist conference organizers should send speakers ahead of time: room size and layout, microphone type, projector and laptop connections, WiFi reliability, venue and hotel directions, parking, meals, water, and a place to meet attendees afterward. Reduces speaker stress and travel anxiety. |
| /2015/3-steps-to-getting-great-volunteers/ | 3 Steps to Getting Great Volunteers | Three ways a volunteer organization stops turning away recruits: run a secret shopper program to test onboarding, study or ask successful nonprofits what works, and communicate options, qualifications, and respect clearly on the first phone call. Recruiting volunteers is part interview, part sales. |
| /2015/3-steps-to-the-best-programmers-cover-letter/ | 3 Steps to the Best Programmer's Cover Letter | A programmer's cover letter is a connection letter for the hiring manager, not a resume summary. Open by addressing their need to get work done, tailor experience to the job posting, then close with a confident request to talk. Referrals go in the PS. |
| /2015/ansible-installation-of-postfix-with-no-prompts/ | Ansible Installation of Postfix with No Prompts | Installing postfix on Ubuntu through Ansible stalls on the interactive apt-get prompts. Pre-answer them with the Ansible debconf module, setting postfix/mailname and postfix/main_mailer_type to Internet Site before the apt task, then copy in your own postfix config afterward. Unattended, non-interactive package installation. |
| /2015/composer-based-utility-installs-vs-global-installs/ | Composer-based Utility Installs vs Global Installs | Why per-project Composer installs beat global tool installs: after PEAR, downloaded PHAR files, and apt-get, a globally installed PHPUnit turned out to be an old 3.7 while the docs in use were 4.x. Disk space is cheap; composer.lock pins a known version. |
| /2015/creating-a-google-fusion-table-from-a-google-drive-spreadsheet/ | Creating a Google Fusion Table from a Google Drive Spreadsheet | A step by step walkthrough of turning a Google Drive spreadsheet of Milwaukee building values into a Google Fusion Table: importing the source, geocoding addresses onto a map, picking marker styles, and writing custom HTML for the info window before publishing it. |
| /2015/css3-only-read-more-link/ | CSS3 Only Read More Link | A read more link that expands text with CSS alone: a fixed height section with overflow hidden, a gradient fade at the bottom, and a checkbox whose :checked state, through a sibling selector, sets the section height to auto. The checkbox hack, no JavaScript. |
| /2015/doctrine-error-expected-plain-value/ | Doctrine Error: Expected Plain Value | The Doctrine annotation parser error "Syntax Error: Expected PlainValue, got '''" comes from single quotes inside an entity annotation. Doctrine annotations require double-quoted strings, unlike PHP itself, which accepts either quote style. Swap the quotes and the mapping parses. |
| /2015/documentation-tools-for-phpstorm/ | Documentation Tools for PHPStorm | Configuring PhpStorm Remote SSH External Tools to run phpDocumentor and apiDoc inside a Vagrant virtual machine from the Tools menu, so generating code and API documentation takes one click. Covers installing both tools and the command parameters for source and output directories. |
| /2015/error-validating-seconds-in-html5-time-input/ | Error Validating Seconds in HTML5 Time Input? | Chrome rejects a value with seconds in an HTML5 input type="time" with "Please enter a valid value. The two nearest valid values are..." because the spec's default step for time inputs is 60 seconds, not 1 like other numeric inputs. Adding step="1" allows seconds. |
| /2015/getting-variable-values-from-nested-twig-symfony-custom-template/ | Getting variable values from nested twig / symfony custom template | A custom label passed to `form_row`() in a Symfony form theme never reached `form_label`() inside the overridden `field_row` block, because the form view passed along lacks it. Reading the value from Twig's `_context` array, `form_label`(form, `_context`['label']), renders the custom or default label. |
| /2015/keep-users-scroll-position-in-scrollable-div/ | Keep user's scroll position in scrollable div | Preserving a user's scroll position inside an overflow-y scrollable div across page loads: on the window unload event save the element's scrollTop to localStorage, and on load restore it. Shown with jQuery but not dependent on it; a cookie works in place of local storage. |
| /2015/one-time-scrolling-banner-with-jquery/ | One-time Scrolling Banner with jQuery | A rotating hero effect: a UL of phrases clipped by a fixed-height overflow hidden container, with a recursive jQuery function that animates scrollTop to the next LI every five seconds and stops at the last one. Built for a homepage tagline, shared on CodePen. |
| /2015/os-x-yosemite-android-studio-android-sdk-location/ | OS X Yosemite Android Studio Android SDK location | Where Android Studio puts the Android SDK on OS X Yosemite, since most documentation gives the wrong path: ~/Library/Android/sdk. Add ~/Library/Android/sdk/platform-tools to the bash $PATH to run adb from the command line. |
| /2015/php-pdf-viewer-convert-to-images-and-use-htmljs/ | PHP PDF Viewer - Convert to Images and use HTML/JS | Proof of concept for a browser PDF viewer that scales and works across devices: a PHP class uses Imagick (ImageMagick) to render each PDF page to a cached JPEG at 150 DPI, and a few lines of jQuery fade between page images on click. |
| /2015/should-you-unit-test-your-dependency-container/ | Should you unit test your dependency container? | Whether to unit test a Pimple dependency injection container wrapped in a singleton class. For: every service the app needs should be resolvable, or the app crashes. Against: a DI container is configuration, and nobody writes PHPUnit tests for an ini or yml file. |
| /2015/started-my-own-company/ | Started my own company | A one-line announcement of striking out independently with a newly founded company, and an open invitation to get in touch about working together. Going solo, self-employment, freelancing, consulting, starting a business. |
| /2015/stop-fast-taps-acting-like-double-tap-to-zoom/ | Stop fast taps acting like double-tap-to-zoom | Fast taps on a mobile web number pad triggered the browser's double-tap-to-zoom. A jQuery handler on touchend that calls preventDefault and then fires click on the target makes each tap a plain click. Scope it to the buttons that need it, not the whole body. |
| /2015/vagrant-ansible-config-change-to-vagrant-on-login/ | Vagrant / Ansible config: change to /vagrant on login | Save keystrokes after vagrant ssh by having Ansible write a .bash_login file for the vagrant user with a single cd /vagrant line, using the lineinfile module. The shared folder path could be a variable passed through extra_vars. |
| /2016/8-php-command-line-tips-and-tricks-presentation/ | 8 PHP Command Line Tips and Tricks Presentation | Slides from a Milwaukee PHP Users Group talk on eight PHP command line tips and tricks, shared as a Keynote file rather than the usual PDF export because the presentation embeds video. The download only opens in Keynote on a Mac. |
| /2016/are-all-american-ceos-psychopaths/ | Are All American CEO's Psychopaths? | A capstone paper on corporate psychopaths: defines psychopathy against sociopathy and psychosis, compares American CEOs with European and Asian counterparts, and matches decisions by Maxwell, Madoff, and Ken Lay of Enron to psychopathic traits. Recommends screening with the Hare checklist and ethical mentoring. |
| /2016/cc10-sanity-check-values/ | Sanity Check Values | Sanity checking request bounds to prevent denial of service through resource exhaustion, from over-eager users opening many tabs or from malicious ones. Throttle per session, IP, or account, and when a form allows three email addresses, reject a request with more instead of processing three. |
| /2016/cc11-what-type-are-you/ | What "type" are you? | Type hinting in loosely typed PHP: strong versus weak typing, what PHP 5.5 can hint (classes, interfaces, arrays, callable), making the caller supply correct types instead of casting inside methods, and hinting an interface so every object passed must have the method you call. |
| /2016/cc12-make-the-reasons-overwhelming/ | Make the Reasons Overwhelming | Persuading a boss, client, or team to adopt a best practice means stacking reasons until one connects, like a dentist showing rotting teeth. Dependency injection sells on decoupling and testing, but the winning reason is swapping database adapters to reuse a service for data conversion. |
| /2016/cc2-descriptive-naming-schemes/ | Descriptive Naming Schemes | Descriptive variable and method names in PHP: names should reduce commenting and read like a sentence, since PHP gains nothing from minification. Refactoring lastStep with $p, $ms, and $tmp into addPrimaryToMembersIfMissing with $primary, $members, and $isPrimaryFound. Self-documenting code, naming conventions. |
| /2016/cc3-array-key-accuracy/ | Array Key Accuracy | Checking a PHP array value for truthiness breaks when 0 or NULL are legitimate values, and empty() and isset() share the blind spots. array_key_exists() tests whether the key is present regardless of its value and avoids the undefined index E_NOTICE. Accuracy over good enough. |
| /2016/cc4-commenting-is-more-than-opinion/ | Commenting is More than Opinion | Two reasons to comment PHP: PHPDoc docblocks for automated documentation with phpDocumentor or Doxygen and IDE hints, and explaining complex business logic. Simple methods with good names need no inline comments; over-commenting is a phase. Comments explain business rules, not excuse sloppy code. |
| /2016/cc5-attention-to-detail/ | Attention to Detail | Three bugs from not paying attention to detail: assignment instead of comparison inside an if condition (= versus ==), mismatched nested single and double quotes when building HTML in PHP, and a copied and pasted docblock that no longer describes its method. Accuracy over speed. |
| /2016/cc6-confident-not-cocky/ | Coders be Confident, Not Cocky | The line between confidence and cockiness in programming: there is always someone who knows more, as a first conference and a mentor's code review proved. Imitate the leaders in the PHP field, who write to the point, researched, and accurate without self-promotion. Humility, ego. |
| /2016/cc7-take-care-of-your-resources/ | Take Care of Your Resources | Stewardship of CPU and database resources in PHP: hoist count() out of a for loop condition instead of recomputing it every iteration, and replace a query inside a loop (the N+1 query problem) with a single MySQL JOIN. Efficient code is also faster code. |
| /2016/cc8-filtering-php/ | Filtering PHP | Validating and sanitizing input with plain PHP filter_var(): validate filters return a boolean, sanitize filters return a cleaned value, shown with FILTER_VALIDATE_EMAIL and FILTER_SANITIZE_EMAIL. Do not reinvent the wheel with regular expressions, and do not lean on a framework for everything without reviewing it first. |
| /2016/cc9-trust-but-verify/ | Trust, But Verify | Defensive programming in PHP: guard a foreach with is_array() when a library may return null, check isset() on a json_decode() property before using it, and verify with is_numeric() because a third-party API can change an integer of minutes into a formatted string overnight. Guard statements. |
| /2016/chicken-or-egg-of-hacking-your-software/ | The Chicken or the Egg of Hacking Your Software | Whether to write a proof-of-concept exploit for a theorized weakness in your own software. Tim Cook's FBI iPhone letter argues that once such code exists it cannot un-exist. The counterargument wins: a known risk with a defined mechanism beats a guess. |
| /2016/doctrine-database-fails-in-gearman/ | Doctrine Database Connection Fails in Gearman Worker | Long-running Gearman workers lose their Doctrine database connection after idling (the classic "server has gone away" problem). Fix: a wake-up closure that pings the DBAL connection at the start of every job, closing and reconnecting on failure before rethrowing the PDOException. |
| /2016/drupal-plugin-github-updates/ | Drupal Plugin with Github Updates | Hosting a Drupal 7 module on GitHub with self-served update notifications: the project status url in the .info file, an install zip built with git archive and a prefix folder, tagged GitHub releases, and an orphan release-history branch holding the 7.x release XML. |
| /2016/export-git-project/ | Export Git Project | git archive exports tracked files without the .git folder, the equivalent of svn export, and zips the result when the output name ends in .zip. Useful for packaging a WordPress plugin for upload, and easy to add as a PHPStorm external tool. |
| /2016/first-confident-coder-column/ | First Confident Coder Column | Introduction to the Confident Coder column on core PHP without frameworks, born of code review dread. First lesson: detect a POST request by checking $_SERVER['REQUEST_METHOD'] with isset and a strict comparison, not by testing a form field in $_POST. Republished from PHP Architect magazine. |
| /2016/get-better-at-logging/ | Get Better at Logging | PHP developers log too little, and storage cost or disk bottleneck excuses do not hold. Use two Monolog StreamHandlers: an error log at WARNING and above that is monitored and alerts, plus a full activity log correlated by timestamp. FingersCrossedHandler buffers when writes matter. |
| /2016/google-street-view-animation/ | Google Street View Animation | Animating a full-screen Google Street View panorama with vanilla JavaScript: a StreetViewPanorama with controls hidden, a setInterval that decrements the heading with setPov for a smooth rotation, and a CSS keyframe fade-in over a black body after a setTimeout, since no load-complete event exists. |
| /2016/government-website-idea/ | Government Website Idea | A proposed civic site to make elected officials easy to find: detect location from the browser, IP, or a Google Places search box, overlay congressional district boundaries from Data.gov cartographic files converted to KML on a Google Map, and list the representatives beside it. |
| /2016/how-to-use-the-same-method-in-doctrine-app/ | How to Use the Same Method for Add/Update in a Doctrine App | An abstract service class with a single save() method for Doctrine ORM entities: EntityManager::contains() tells whether the entity is managed (update) or new (add), then persist and flush run, and a PSR-3 logger records Added or Updated with the class name. |
| /2016/i-am-a-failure/ | I am a Failure | A failed year as a solo entrepreneur and startup co-founder: quitting without savings, passion projects over income, a niche too big for a one-person shop, draining a 401k, losing health and friends. Lesson: building great things and leading teams can beat owning the business. |
| /2016/jekyll-mark-old-posts/ | Jekyll Mark Old Posts | Liquid template code for Jekyll that shows an old post warning on entries more than 18 months old: capture now and page.date as Unix timestamps, subtract, and compare to a seconds threshold. The same idea as the Label Old Posts WordPress plugin, without a plugin. |
| /2016/making-the-most-of-the-tostring-method-in-javascript/ | Making the most of the toString method in Javascript | Override toString on a JavaScript constructor's prototype so objects render as something more useful than [object Object]: a Person that prints a title and full name when passed to alert or innerHTML. Keep it to simple string formatting; never return HTML templates from toString. |
| /2016/my-madmimi-api-php-library/ | My MadMimi API PHP Library | An open source PHP library for the MadMimi email marketing API, written because the existing PHP clients were outdated, incomplete, and broken in places. It runs in production and installs through Composer from Packagist, with hopes of a listing on the vendor's support page. |
| /2016/new-blog/ | New Blog | Moving the blog off WordPress onto Jekyll for speed, with open questions about how, or whether, to enable comments on a static site. The borrowed design comes with news: winding down a business to take a full-time job. |
| /2016/password-complexity-v2/ | Password Complexity v2 | Users hate password complexity meters and rules about symbols and capitals, and signup is a bad time to fight them. A gentler alternative: reject only passwords on a blocklist of the most common ones (password, 123456, qwerty, letmein) plus the user's email or username. |
| /2016/php-templating-languages-indicate-a-problem-elsewhere/ | PHP Templating Languages Indicate a Problem Elsewhere | Template languages like Twig, Plates, and Smarty solve the wrong problem: PHP is already a template language, views contain some logic, and auto-escaping assumes discipline. The fix is a fat view model that filters, escapes, and flattens data before the view file sees it. |
| /2016/phproblemlogger-filter-strategies/ | PHProblemLogger Filter Strategies | Filter callbacks for PHProblemLogger, an error-condition logger: closures that return the whole $_SERVER payload, pick one key, unset a sensitive key, or keep a few keys with array_filter and ARRAY_FILTER_USE_KEY instead of reinventing the wheel, plus a pre-5.6 workaround using key() and next(). |
| /2016/reduce-vagrant-code-duplication/ | Reduce Vagrant code duplication by using functions | A Vagrantfile is plain Ruby, so repeated provisioner settings can move into a method (DRY). Define shared_ansible_config(ansible) at the top of the file with the common Ansible playbook, host key, and sudo options, then call it inside each vm.provision block before the per-environment extra_vars. |
| /2016/short-hand-php-dot-env/ | Short-hand PHP Dotenv instantiation | Load vlucas/phpdotenv environment variables in one line with PHP 5.4's class member access on instantiation: (new Dotenv\Dotenv(__DIR__))->load() replaces assigning the object to a variable first. Keeping credentials out of source code in a .env file follows OWASP guidance. |
| /2016/sometimes-more-features-arent-better/ | Startups - take note: Sometimes More Features Aren't Better | Adding features will not fix a startup product that is not selling. The real causes are the wrong customer, the wrong problem, or weak sales material and sales engineering. Study rejections, involve developers in demos, accept the sunk cost, and pivot instead of feature creep. |
| /2016/sync-bitbucket-and-github/ | Sync BitBucket and GitHub | Keep a private Bitbucket repository and a public GitHub mirror in sync with no hooks or third-party service: add two pushURL lines under the origin remote in .git/config so a single git push goes to both. Bitbucket stays the authoritative repo for fetching. |
| /2016/two-quick-tips-for-securing-php-sessions/ | Two Quick Tips for Securing PHP Sessions | Two php.ini settings against session fixation and session hijacking: session.use_only_cookies stops the session ID riding in a URL where a shared link leaks it, and session.use_strict_mode (PHP 5.5.2+) refuses attacker-chosen IDs by generating a fresh one for any unknown session. |
| /2016/using-developer-tools-in-chrome-for-testing/ | Using Developer Tools in Chrome for Testing | A beginner's tour of Chrome DevTools for testing a website without a device lab: open the tools, use device mode to simulate phone and tablet viewports, touch events, and orientation, then throttle the connection in the Network tab to test slow speeds like 2G. |
| /2016/using-json-patch-in-swagger/ | Using JSON Patch in Swagger | How to describe an HTTP PATCH endpoint in a Swagger (OpenAPI) YAML spec using JSON Patch (RFC 6902): a PatchRequest array of PatchDocument objects with op, path, value, and from. Compares it with JSON Merge Patch (RFC 7386), the simpler choice for flat objects. |
| /2016/using-your-namespace-for-optimum-readability/ | Using Your Namespace for Optimum Readability | When two classes share a short name, like a User service and a User form, importing them by full class name leaves method signatures ambiguous. Import the parent namespace instead (use MyApp\Service; use MyApp\Form;) and write Service\User and Form\User for readable PHP type hints. |
| /2016/wordpress-meetup-presentation/ | WordPress Meetup Presentation | PDF slides from a WordPress meetup talk titled My Site is Broken, Now What, on diagnosing and fixing a broken WordPress site. Short announcement post with a download link and no writeup of the content. |
| /2017/33-things-book/ | 33 Things I Wish Somebody Would Have Told Me | A free eBook, PDF and ePub, of career advice for programmers on quality code, work relationships, and respect. Grew out of a conference talk on the same theme. Agents and publishers passed, so it is given away open source instead of self-published. |
| /2017/add-case-sensitive-disk-in-macos/ | Add a Case-Sensitive Disk in MacOS | Shrink the main macOS partition in Disk Utility and add a second one formatted case-sensitive journaled for development files, since abc.php and AbC.php collide on the default filesystem. It mounts under /Volumes; symlink it to ~/Sites. Faster than the earlier SD card approach. |
| /2017/always-keep-learning/ | Always Keep Learning | Career growth comes from constant learning: books, experiments, weekend side projects that succeed or fail, instead of hours of TV. Learning other languages improves your main one; ideas cross fields. A VueJS, Bulma, and Webpack toy project showed how alike front-end frameworks are. |
| /2017/anatomy-of-a-php-hack/ | Anatomy of a PHP Hack | Decoding an obfuscated PHP backdoor found on a hacked website: string offsets assembled with strtolower() and strtoupper() spell out base64_decode and $_POST, so the payload is eval(base64_decode($_POST['n89024b'])), a remote code execution web shell. Test unknown code only in an offline sandbox. |
| /2017/anonymous-self-execute-php-js/ | Anonymous Self-Executing Functions in JavaScript and PHP | The JavaScript immediately invoked function expression, or IIFE, wraps an anonymous function in parentheses and calls it immediately. Rather than the same syntax in PHP, call_user_func() with an anonymous function gets the same effect. A question of can, not should. |
| /2017/assert-same-vs-equal/ | AssertSame vs AssertEqual in PHPUnit | The difference between assertSame and assertEquals in PHPUnit is the difference between identical (===) and equal (==) comparison in PHP. Prefer assertSame whenever you can, because strict comparison also catches type mismatches that a loose equality check would hide. |
| /2017/composer-dry-run/ | Composer Dry Run | Preview what a Composer dependency update would change before running it with composer update --dry-run, which resolves everything and reports the packages that would move, helping gauge how large a third-party library upgrade will be. It caches the calculation, so the real update runs faster. |
| /2017/customer-always-right-but-not-with-everything/ | The Customer is Always Right, But Not With Everything | Unpacking the customer is always right: their concerns are always valid and deserve empathy, their requests are always useful as a window into their business, but their proposed solutions are usually not the answer. Read between the lines to find the real problem. |
| /2017/easily-test-json-keys-in-laravel-api/ | Easily Test JSON Keys in Laravel API Response | Feature testing a paginated Laravel API endpoint with PHPUnit: assertJson for exact pagination values, then assertJsonStructure with a wildcard whose expected keys come from array_keys on a fresh model's toArray(). Checks response shape without duplicating values already covered by unit tests. |
| /2017/evolution-of-php-programmer/ | The Evolution of PHP Programmers | Stages a PHP developer passes through: crappy procedural code, crappy OO, misusing a framework, writing your own framework, following one rigidly, then using one as a tool. Online training lets people skip stages. The end is quality code that solves the business problem. |
| /2017/fake-it-till-you-make-it/ | Fake It Till You Make It | Fake it till you make it, ethically, while job hunting as a programmer: treat the search like a job with a schedule, apply during meeting blocks instead of spamming, and build portfolio projects or nonprofit work to show. Never inflate resume experience. |
| /2017/find-code-review-fodder/ | How to Find Sections of Code to Review | Signals for where to focus a code review in a large codebase: spelling mistakes, sloppy formatting that hints at copy and paste, very short variable names, TODO comments, long methods, complexity reports from PHP Metrics, and gut feeling about code described as clever. |
| /2017/fixing-laravel-5.4-dependency-on-phpunit-5/ | Fixing Laravel 5.4's Dependency on PHPUnit 5 | Queue::fake() and assertPushed in Laravel 5.4 fail with class PHPUnit_Framework_Assert not found under PHPUnit 6, which dropped the non-namespaced aliases PHPUnit 5 kept. The fix: class_alias calls in the test bootstrap mapping the four PHPUnit_ names Laravel's testing fakes import to their namespaced classes. |
| /2017/fun-with-giant-integers-in-php/ | Fun with Giant Integers in PHP | Comparing an integer one larger than PHP_INT_MAX on 64-bit PHP: the overflowed literal fails a less-than comparison, the same values as strings compare correctly, and casting the string with (int) silently clamps to PHP_INT_MAX. Integer overflow, validating large API input, type juggling. |
| /2017/in-php-false-is-sometimes-true/ | In PHP, False is Sometimes True | Casting the string "false" to a boolean in PHP gives true, because a non-empty string is truthy regardless of its content. A flag posted as JSON and cast with (bool) turned every value into true. Type juggling, string to boolean conversion, validating boolean input. |
| /2017/investing-in-quality-reduces-expenses/ | How Writing Good-Quality Code Reduces Expenses | The business case for code quality: rushed code piles up technical debt, like a rickety house, until small changes cost hours of regression checking. Start quick to prove the idea, then shift to solid architecture. Quality codebases attract and keep great programmers, lowering turnover. |
| /2017/issue-404-after-last-page/ | Issue 404 Not Found Middleware After Pagination Limit | A Laravel middleware that returns 404 Not Found when a JSON API request asks for a page beyond the paginator's last page. It decodes the response body, reads LengthAwarePaginator's last_page, and sets the status code, because Laravel pagination serves empty pages past the end. |
| /2017/laravel-api-validate-boolean-value/ | Laravel 5.4 API/Request Validate Boolean | A custom Laravel validation rule, any_boolean_value, because the built-in boolean rule rejects values like yes, no, on, and off. It uses in_array with strict comparison against an allowed list, is registered with Validator::extend, and gets its own error message in the validation language file. |
| /2017/laravel-factory-skip-hash-generation/ | For Performance, Skip Generating Hashes in Laravel Factories | Laravel model factories run many times per test suite, so calling Hash::make('secret') in a User factory pays for bcrypt hashing on every generated model. Replace it with a precomputed hash string and a comment noting how it was made. Faster tests get run more often. |
| /2017/laravel-log-db-events-from-environment/ | Laravel Log Database Queries Based On Environment Variable | Log every SQL query Laravel runs by registering a DB::listen callback in AppServiceProvider::boot(), gated on a config value fed by a LOG_QUERIES environment variable in .env. Toggle query logging per environment without commenting code in and out; version control should not carry debug toggles. |
| /2017/laravel-pretty-print-middleware/ | Laravel Pretty Print JSON Middleware | A Laravel middleware that pretty prints JSON responses on demand: when the query string has pretty=true, it calls setEncodingOptions(JSON_PRETTY_PRINT) on any JsonResponse on the way out. A drop-in alternative to editing every response()->json() call or relying on Postman's preview. |
| /2017/laravel-queue-console-with-injection/ | Use Dependency Injection in Laravel Console Commands | Unit testing Laravel console commands is easier without the Artisan facade. Inject the console Kernel contract through the constructor and call queue() or call() on it, so nested commands can be mocked instead of faked, with no application reset between tests. Dependency injection over facades. |
| /2017/laravel-string-studly-words/ | Studly Words in Laravel | Extending Laravel's Str helper with a studlyWords() method that turns a hyphenated or snake_case slug like something-is-here into title case words with spaces, Something Is Here, where Str::studly() would concatenate to SomethingIsHere. Implemented as a Str subclass with the same static cache pattern. |
| /2017/laravel-swagger-generation-command/ | Laravel Command to Generate Swagger Documentation | A minimal artisan command, swagger:generate, that scans the app directory with swagger-php and writes swagger.json to the public directory, for documenting a Laravel API with Swagger (OpenAPI) annotations without the bloat of full-featured packages. Commit the generated file to version control. |
| /2017/laravel-throttle-and-jwt-vuln-to-cache-attack/ | Laravel Default Throttle Configuration and Common JWT Blacklist Vulnerable to Cache Attacks | Laravel's ThrottleRequests middleware and the tymon/jwt-auth blacklist both keep state in the default cache, which is ephemeral: file caches get reaped, Redis evicts under memory pressure, and an attacker can flood the cache to evict a throttle or blacklist entry. Bind a persistent driver instead. |
| /2017/laravel-validate-parameter-not-present/ | Validate Request Parameter Not Present in Laravel 5.4 | A custom Laravel validation rule, not_present, that rejects a request when a field is included at all, registered with Validator::extendImplicit so it runs on missing fields. Useful for rejecting deprecated API parameters or fields allowed on POST but not PATCH. |
| /2017/mac-app-keep-slack-active/ | Mac App to Keep Slack Active | An abandoned Mac app that pings the Slack API users.setActive method on a timer so you never appear away. Electron was too heavy, MacGap promised tiny binaries, and the real lesson was that nobody was watching the status anyway; cutting losses is fine. |
| /2017/my-site-redesign-simpler-faster-but-less-user-friendly/ | My site re-design: Simpler, faster, but less user-friendly? | A blog redesign driven by Google Analytics: most visitors land on one post from search and leave, and other links go unclicked. Navigation shrank to home and contact, built-in serif fonts and a capped line width improved readability, and pages got smaller and faster. |
| /2017/php-interfaces-extend-multiple/ | PHP Interfaces Can Extend Multiple Interfaces | PHP classes can extend only one parent (use traits for shared code), but an interface can extend several interfaces at once. A Human interface extending Talks and Eats forces implementers to define every method from all three, a form of multiple inheritance for contracts. |
| /2017/phpstorm-file-watcher-error-logs/ | PHPStorm File Watcher for Immediate Error Log Alerts | Warnings and logged errors during local development go unnoticed when nobody opens the log. A PHPStorm File Watcher scoped to logs/error.log runs terminal-notifier on macOS (or notify-send on Linux) whenever the file changes, popping a desktop notification the moment an error is written. |
| /2017/phpunit-helpers-mockery-non-public-properties/ | PHPUnit Test Helpers for Mockery and Non-Public Properties | Three global helpers for a PHPUnit bootstrap: a mock() shortcut proxying to Mockery::mock with a custom interface so PHPStorm stops flagging shouldReceive arguments, plus callMethod() and getProperty(), which use ReflectionClass and setAccessible to invoke protected or private methods and read non-public properties. |
| /2017/phpunit-listener-for-long-running-tests-update/ | PHPUnit Listener for Long Running Tests Update | An updated PHPUnit test listener that flags slow tests: registered in phpunit.xml.dist, it extends BaseTestListener, and in endTest compares the elapsed time against a 2000 millisecond limit, printing the test class and method in red console output when a test runs too long. |
| /2017/phpunit-stop-on-failure/ | PHPUnit Stop On Failure | On a project with dozens of broken unit tests, the scroll-back fills before you find the first failure. Setting stopOnFailure="true" on the root phpunit element in phpunit.xml halts the suite at the first error so you fix failures one at a time. |
| /2017/pitch-to-programmers-like-investors/ | Pitch to Programmers Like Investors | Founders build investor pitch decks, then recruit developers with a two-line equity-only post. A programmer forgoing salary is betting on your startup like an investor, so pitch them the idea: they bring experience that can refine it, and their name rides on its success. |
| /2017/professional-connection-made-now-what/ | I Made A Professional Connection: Now What? | Networking etiquette after a LinkedIn connection request is accepted: do not connect and disappear. Keep the conversation going, offer free help on something the other person needs, and ask for help without abusing it. Also a rant at people who complain about recruiter spam. |
| /2017/quick-and-easy-case-sensitive-drive-in-macos/ | Quick and Easy Case Sensitive Drive in MacOS | Git cannot see a filename case change on a case-insensitive macOS filesystem. Fixes: a case-sensitive disk image or partition, or a microSD card in a Nifty MiniDrive formatted Mac OS Extended (Case-sensitive, Journaled) and symlinked over the projects folder, at some cost in speed. |
| /2017/rockstars-are-ruining-your-business/ | Rockstar Coders are Ruining Your Business | Job ads asking for a rockstar programmer or code ninja hurt team culture. Rockstars burn out and bring drama; ninjas write hidden, convoluted code nobody can explain later. Hero worship breeds ego and demotivates everyone else. Hire solid, well-rounded developers instead. |
| /2017/run-only-one-phpunit-test/ | Run A Single PHPUnit Test Only | Run one test method rather than a whole file by passing PHPUnit's --filter option with the method name after the test file path, instead of commenting out the other tests. Omit the file and the filter pattern is matched across the entire suite. |
| /2017/track-done-things-with-alfred/ | Track the Things You've Done Today with Alfred | An Alfred workflow for logging what you did for agile stand-up check-ins: type did plus a task and it appends a bullet to a text file named for today's date, using keyword, args and vars, and write-file actions instead of AppleScript and Notes. |
| /2017/tracking-work-accomplishments/ | The Importance of Tracking Work Accomplishments | Keep a running log of your work accomplishments, a brag document, for motivation, resume material, and performance reviews, since nobody remembers what they did months ago. Set a monthly calendar reminder, and record the project, your role, one measurable result, and the customer impact. |
| /2017/use-anonymous-classes-to-test-traits/ | Use Anonymous Classes to Test Traits | Unit test a PHP trait with an anonymous class instead of a stub class at the bottom of the PHPUnit test file. For protected trait methods, add a public proxy method on the anonymous class that forwards its arguments with call_user_func_array and func_get_args. |
| /2017/use-the-fail-method-with-mockery-on/ | Use the $this->fail() method with Mockery::on() | When a Mockery::on() closure returns false, PHPUnit reports only NoMatchingExpectationException with no hint which check failed. Calling $this->fail() with a message inside the closure gives a clear failure, and type hinting the closure parameter replaces instanceof checks with a TypeError. |
| /2017/using-namespaces-in-laravel-tinker/ | Using Namespaces in Laravel Tinker | Typing fully qualified class names like App\Models\Facebook\Account in Laravel Tinker gets tedious. A use statement works inside the Tinker REPL (PsySH), so use App\Models or use App\Models\Facebook sets a working namespace and shortens every model reference in the session. |
| /2017/verify-all-object-properties-are-not-empty/ | Quick Snippet to Verify All Object Properties are Not Empty | A one-line PHP check that every property on an object is non-empty: array_filter without a callback drops falsy values, so comparing array_filter(get_object_vars($object)) against get_object_vars($object) tells whether any property was empty. Handy for validating a fully populated object. |
| /2017/whats-important-in-life/ | Deciding What's Important in Life | A health scare forces a reckoning with what chasing success cost: lost friendships, weight gain, poor diet, no exercise, alcohol abuse, and an ER visit. Announcing a leave from blogging to reset physical and mental health; being great at your work is good enough. |
| /2017/why-didnt-my-friend-hire-me/ | Why Didn't My Friend Hire Me? | When a friend is the hiring manager and passes you over: their duty is to the company, you may not be the best fit or the trusted kind of friend. It isn't personal. Thank them, ask carefully for interview feedback, and don't burn bridges. |
| /2017/why-use-private-in-oss/ | Why Use Private in Open Source Software | Questioning private methods and properties in open source PHP: private is not security, only security through obscurity. A plugin base class shows a child bypassing a wrapped PDO connection, risking SQL injection. Private makes sense for fast-changing third-party libraries whose interfaces are the contract. |
| /2018/2-slack-status-shortcuts-alfred/ | 2 Ways to Use Alfred to Set Status on Slack | Two Alfred automations for setting a Slack status from a Mac: a text snippet that expands to the /status slash command with an emoji, and a workflow keyword that runs a PHP script calling the users.profile.set API with a legacy token. |
| /2018/adding-csv-to-laravel-using-macros/ | Adding CSV Responses to Laravel Using Macros | Extend Laravel with macros so a CSV endpoint reads like the JSON one: a Response::macro('csv') that builds the document with League CSV and sets the text/csv content type, and a Request::macro('wantsCsv') modeled on wantsJson(). Macroable magic needs PHPDoc @method annotations for IDEs. |
| /2018/corrective-feedback-given-privately-summarized-publicly/ | Give Corrective Feedback Privately, Then Summarize Publicly | Praise in public, correct in private: giving negative feedback in a group setting reads as shaming. Have the specific conversation one on one, then, once the same problem shows up more than once, share an anonymized summary with the whole team. |
| /2018/do-not-badmouth-ex-employees/ | Why Complaining About Bad Ex-Employees Never is Good | Management advice: never badmouth a former employee, even one who did poor work or backstabbed the team. Venting keeps the wound open and teaches remaining staff you hold grudges. Instead, summarize the situation honestly, then move on to the next topic. |
| /2018/explicit-javascript-function-parameters/ | Be Explicit with Your JS Function Parameters | A stateless React component that takes the whole props object hides what it needs. Declaring a function with explicit named parameters and destructuring only the needed props at the call site makes the function reusable, self-documenting, and independent of component context. |
| /2018/get-all-imessage-attachments/ | Get All iMessage Attachments | A PHP script that flattens every iMessage attachment on a Mac into one folder: attachments live under ~/Library/Messages/Attachments in nested hex and UUID directories, so RecursiveDirectoryIterator and RecursiveIteratorIterator walk them, filter for jpg, and copy each under a uniqid name. |
| /2018/hardest-part-of-mentoring/ | The Hardest Part of Mentoring is Knowing What and When to Share | Mentoring programmers works best as a safety net: share research pointers rather than answers, step in slightly after the mentee needs it and mostly when asked, and let them wander so they become themselves, not a mini-you. Their pushback sharpens the mentor too. |
| /2018/honey-pots-in-laravel/ | Quick Honey Pots in Laravel | Honey pots and canaries for intrusion detection in Laravel: an Eloquent observer on retrieved fires an event and throws AuthorizationException whenever the admin user (id 1) is hydrated; a global after middleware does the same when a route-bound user holds a restricted system role. |
| /2018/host-site-github-pages-with-ssl/ | Host Static Website on GitHub Pages with SSL and WWW redirect | Free static hosting on GitHub Pages with HTTPS and a www redirect, which Pages alone cannot do: DNS on Cloudflare, publishing with the gh-pages npm package and a CNAME file, CNAME flattening, full SSL mode, and page rules forcing HTTPS and stripping www. |
| /2018/how-developers-should-delegate/ | How Developers Should Delegate | Developers should delegate outward by building interfaces: never be the only person who can change a config or pull a report. Skipping database-edit and command-line stages for a product owner UI avoids context switching, makes testing easier, and puts owners in control. Bus factor. |
| /2018/how-to-learn-programming/ | How to Learn Programming | Resources for learning to code, slanted toward web development: video courses (Udacity, Udemy, Front End Masters, Laracasts), books and blogs, open source contribution on GitHub, meetups, and mentors. A computer science degree is optional; bootcamps are a gamble. Mostly: pick a project and start. |
| /2018/http-only-cookies-are-not-perfect/ | HTTP Only Cookies Aren't Perfectly Secure | The HttpOnly cookie flag hides a cookie from JavaScript but is not a complete defense. A contrived proof of concept steals one through two vulnerabilities: a file upload hole that runs a PHP script mailing $_COOKIE, plus HTML or script injection (XSS) requesting it. |
| /2018/instead-of-coding-interview-do-peer-review/ | Stop Interactive Code Challenges During Interviews, Do This Instead | Live coding challenges in interviews measure stress response, not skill: being watched while typing produces a candidate's worst work, and interview stress is unlike real deadline stress. Better: screenshare a pull request nobody on the team wrote and have the candidate review it aloud. |
| /2018/lessons-from-200-tech-interviews/ | Lessons Learned from Conducting more than 200 Technical Interviews | Hiring is a skill: interview with empathy, look for reasons a candidate could succeed instead of hunting for disqualifiers, be transparent about salary range and process, and replace gut feeling with consistent questions, metrics, and notes that reduce bias and seed an onboarding plan. |
| /2018/make-meetings-5-mins-shorter/ | Make Meetings 5 Minutes Shorter | Schedule 25 minute meetings instead of 30 and 50 instead of 60 so back to back meetings leave time for notes, a walk, or a break. How to set that default: Google Calendar speedy meetings, and defaults write commands for Apple Calendar and Fantastical. |
| /2018/many-motivations-of-money/ | The Many Motivations of Money | Three ways compensation motivates employees, and what each means for managers: money as a reward or goal (commission driven, willing to defer payout), money just to pay the bills (steady, not to be undervalued), and money as a measure of the value they bring. |
| /2018/mockery-on-better-failure/ | Better failing tests with Mockery::on() | Instead of returning false or calling $this->fail() inside a Mockery::on() closure, use PHPUnit assertions like assertInstanceOf and assertEquals directly in the closure and return true. Failures then report the exact mismatch instead of a vague no matching handler error, without reinventing the wheel. |
| /2018/musings-on-github/ | Musings on Github | GitHub features people overlook: archive a repository instead of tagging it abandoned, star repositories as bookmarks instead of forking them (a profile full of untouched forks impresses nobody), host static docs on GitHub Pages, and always write a good README since Packagist shows it. |
| /2018/not-everything-is-impostor-syndrome/ | Not Everything is Imposter Syndrome | Impostor syndrome is real, but the term gets misused like ADHD and OCD. Two designers show the difference: one is qualified yet sabotages her work, the other has a skill gap moving from print to web. Struggling while learning is growth, not a syndrome. |
| /2018/phpunit-dataprovider-before-setup/ | PHPUnit Runs Data Provider Before Setup | PHPUnit runs data provider methods before setUp, so a provider cannot use resources built in setUp. That limitation is a feature: data provider values should be simple, pre-calculated literals, not derived with logic, because testing logic with logic defeats the test. |
| /2018/quick-wifi-scan-default-mysql/ | Quick Local Network Scanner for Default MySQL Installs | A NodeJS script, mysql-connect on GitHub, that scans the local subnet by CIDR for open port 3306 and tries the MySQL default login of root with a blank password. Prompted by developer laptops on coffee shop WiFi; the only good result is finding nothing. |
| /2018/reminder-that-target-blank-is-not-safe/ | Reminder That Target Blank Links Are Not Safe | Links with target="_blank" give the opened page access to window.opener, so a malicious destination can redirect the original tab to a phishing lookalike showing a fake logged out message: reverse tabnabbing. Add rel="noopener" to any new-tab link whose destination you do not control. |
| /2018/teaching-is-a-privilege/ | Teaching is a Privilege, Treat it as Such | Conference and meetup speakers who open with an apology for finishing the slides thirty minutes ago cheapen the audience's investment of time. Teaching is serious: prepare, or delegate the talk to someone who can. Apologizing is not humility; be confident and ask for feedback. |
| /2018/techniques-for-expensive-construction-of-php-objects/ | Techniques to Battle Expensive PHP Constructors | Three ways to defer an expensive PHP constructor until a method needs it, useful with dependency injection and mocking: an explicit start() guard in every method, the __call magic method with protected methods, or a Proxy pattern object for classes you cannot modify. Lazy initialization. |
| /2018/tools-for-measurement/ | Tools You Need for Measuring Everything and Anything | A survey of measurement tools for backing performance claims with metrics: Xdebug profiling, tracing, and cachegrind; Guzzle on_stats transfer times for third-party APIs; MySQL slow query log and EXPLAIN; HAR files from browser dev tools; Zabbix and Nagios for server monitoring. |
| /2018/two-questions-for-new-process/ | Two Questions That Demonstrate if an Experimental Process Should Remain | A test for whether an experimental process should stay: after about six weeks ask two questions. Is it actually useful? Could you live without it? Only yes then no means keep it; anything else means refine or drop it. Habit keeps bad processes alive. |
| /2018/understanding-before-package/ | Understand the Tech Before Getting a Package | Learn a technology or spec before searching Composer or npm for a package rather than reinventing the wheel. Choosing the package first means learning one author's opinionated interpretation, and you cannot judge whether a popular package is bloated, insecure, or wrong for the spec. |
| /2018/what-i-have-learned-in-6-months/ | What I've Learned in 6 Months | Six months after stepping back for physical and mental health, a personal check-in on burnout recovery: why publishing struggles publicly helps others feel less alone and keeps you accountable, exercise and alcohol as battles, and rediscovering hobbies like writing, speaking, guitar, and target shooting. |
| /2018/what-i-learned-from-corp-gig/ | What I Learned from My Most Recent Corporate Gig | Lessons from leading engineering teams: take meeting notes and review them for mentoring, performance improvement plans, terminations, and covering yourself; protect your time from burnout by blocking lunch and personal hours on the calendar; and practice transparency with context, which builds trust. |
| /2019/2019-goals/ | 2019 Goals: What am I Working On? | A mid-year review of professional goals after returning to self-employment: engineering manager training and coaching, a consulting company that augments development teams, validating a startup accountability group, a chicken facts side project, and a joint advisory consultancy. Blogging pauses to make room. |
| /2019/bass-test-app-idea/ | Bass Test App/Website | An unbuilt idea for a bass test website: generate low tones and frequency sweeps in the browser with the Web Audio API AudioContext or Tone.js, and shake a subwoofer image with CSS. Includes sine wave sample code; audio must start from a user gesture. |
| /2019/composer-security-concerns-create-project/ | Composer Security Concerns with Create Project | Composer only runs scripts from the root composer.json, but composer create-project makes the skeleton the root, so a scaffold package can execute arbitrary commands on install, demonstrated with the macOS say command. Custom installer plugins and autoload classmap conflicts are further supply chain risks. |
| /2019/db-migration-separate-from-data-migration/ | Keep Data Migrations Separate from Database Migration | Keep data conversions out of Laravel schema migrations and out of seeders: schema changes are fast and can run in a maintenance window, while data migrations are slow and should run in the background after deploy, with code that tolerates old and new formats. |
| /2019/dev-manager-one-on-one-coaching/ | The Dev Manager One on One Coaching | Announcing one on one coaching for development and engineering managers through The Dev Manager, going beyond one size fits all training, books, and videos to mentoring from someone who has done the job. A later note: the venture was rolled back into this site. |
| /2019/didnt-launch-my-startup/ | I Didn't Launch My Start Up - and That's OK. | A post mortem on StartUp Tribe, an accountability tool for founders that never launched: friends are the worst market validation, a Netlify landing page with an explainer video drew five signups, and lessons on vague feature descriptions, signup form placement, and walking away. |
| /2019/do-not-use-doctrine-with-laravel/ | Reasons Why Not to Use Doctrine with Laravel | Why Eloquent, not Doctrine, belongs in a Laravel project: Active Record versus data mapper and repository pattern, path of least resistance, framework integration, onboarding cost. Oversized models are an architecture problem, not an ORM problem. The one case where porting Doctrine makes sense. |
| /2019/dont-p-when-you-td/ | Don't P When You TD | A p tag dropped inside a table or ul to show "no results" when a list is empty is invalid HTML. Decide whether there are items before opening the list, in PHP and in Vue with v-if and v-else, never v-for mixed with v-if. |
| /2019/expression-1-not-in-group-by/ | Expression #1 of SELECT list is not in GROUP By clause | MySQL error 1055, nonaggregated column not in GROUP BY, appears when sql_mode only_full_group_by is on, the default since 5.7.5. Selecting columns outside the grouping is ambiguous. Wrap them in any_value() rather than disabling the mode, since the error signals a query or data smell. |
| /2019/have-informed-opinions/ | Have Informed Opinions | Try a tool before judging it. Dismissing PHPUnit for Codeception, calling Laravel slow next to Symfony, or scorning IDEs for Vim only counts if the opinion rests on current hands-on use, real measurements, and business goals, not a trusted tweet or premature optimization. |
| /2019/how-to-get-more-feedback/ | How to Get More Feedback from Your Boss | One closing question draws out feedback a manager may be holding back: is there anything else you think I should know? Asking again at the end of a one-on-one gives them a prompt to reflect, and often opens the most important discussion. |
| /2019/laravel-filter-user-input-before-validation/ | Filter User Input Before Validation in Laravel | Filtering user input before validation in a Laravel Form Request with prepareForValidation(): strip empty rows the UI sends from an items array and merge the cleaned array back, so validation rules stay strict and the logic lives in the request class instead of the controller. |
| /2019/laravel-middleware-requires-json/ | Laravel 5 Middleware that Requires JSON | A Laravel middleware that rejects any request without an Accept: application/json header by throwing NotAcceptableHttpException, a 406 Not Acceptable response. Uses the built-in wantsJson() request helper. Add it to all API routes to force JSON-only clients. Content negotiation, JSON API enforcement. |
| /2019/live-reloading-simple-webserver/ | Live Reloading HTML Webserver | The live-server npm package as a lightweight static file server with live reload for HTML, CSS, and JavaScript projects, replacing Vagrant boxes, Docker LAMP images, or php -S when no backend is needed. Install globally, run in the docroot, get automatic browser refresh. |
| /2019/managers-should-welcome-side-projects/ | Managers Should Welcome Side Projects | Managers who forbid employee side projects are wrong: a side project does not steal work time if the employee is engaged, innovation is not a fixed quantity that drains away, and a competing project is something to learn from or acquire, not destroy. |
| /2019/many-ways-to-remove-composer-package/ | The Many Ways to Remove a Composer Package | Three ways to remove a package from a PHP Composer project: composer remove also updates every other dependency, the --no-update flag edits composer.json but leaves files on disk, and deleting the line by hand then running composer update for that package does both. |
| /2019/mark-features-wip/ | Mark WIP Features for Better UX/Customer Acceptance | When demoing work in progress to a client, unfinished navigation links confuse testers and produce false bug reports. Give placeholder links an href of #wip and a jQuery click handler that shows an alert saying the feature is not done, keeping context without frustration. |
| /2019/put-salary-in-job-posting/ | Put Your Salary Range in Your Job Posting, Please! | Why job postings should list a salary range: candidates need context to self-select, transparency builds trust and wins better hires, holding back a number is adversarial, and skilled people asking for more push the market up. Also micro versus macro market competition and negotiation anchoring. |
| /2019/review-your-packages-for-security/ | Remember to Review Your Package Code for Security | Read third-party package code before installing and upgrading: a contrived Laravel Blade package whose auto-discovered service provider, after an update, pushes a Monolog StreamHandler writing logs into the public directory, then replaces the auth middleware to log in an attacker. Supply chain attack. |
| /2019/run-npm-in-docker/ | Run NPM in Docker | A bin/npm shell script that runs npm inside a node:10-alpine Docker container, so a local Node upgrade cannot break older projects' build tooling. Explains each docker run flag: interactive terminal, remove on exit, the TERM environment variable for ANSI colors, and the volume mount. |
| /2019/same-password-not-new-attempt/ | Stop Considering the Same Password a New Password Attempt | Watching a focus group log in showed users retype the same wrong password several times and get locked out before trying a second one. Account lockout should count unique wrong passwords, not entries: repeating one adds nothing to a brute force or dictionary attack. |
| /2019/search-goodreads-bookmarklet/ | Search Goodreads Bookmarklet | A JavaScript bookmarklet that, on an Amazon book page, reads the product details table, pulls the ISBN-13 with a regular expression, and opens a Goodreads search for it in a new tab. Written as a self-executing function; it alerts when no ISBN is found. |
| /2019/share-your-results-with-mentor/ | Share Your Results with Your Mentor | How to repay a mentor: report back on what happened after you acted on their advice. Mentors are invested in the outcome, are not too busy to hear it, and often only know the wrong answers, so your results sharpen their future advice. |
| /2019/spa-plus-api-no-framework-no-db/ | SPA No Framework, API No Database | Building chickenfacts.io as a single page application in plain JavaScript with fetch, no Vue or React, and a JSON API that is static files generated by Jekyll posts with a json layout. Hosted free on Netlify with a catch-all redirect and base 32 share URLs. |
| /2019/stop-using-sqlite-in-laravel-unit-tests/ | Stop Using Sqlite in Laravel Unit Tests | In-memory SQLite in Laravel tests gives false positives when production runs MySQL: no column type enforcement, ignored string lengths, foreign keys possibly off, no user permissions, and raw queries that differ by engine. Test against the same database engine as production. |
| /2019/tell-shimo-vpn-connect-with-applescript/ | Tell Shimo VPN to Connect with Applescript | Automatically connect the Shimo VPN client on macOS before pushing to a git repository: an AppleScript that tells Shimo to connect a named account if not connected, wrapped in a vpn-git-push shell script. A git pre-push hook fails because remote refs are updated first. |
| /2019/test-coverage-bad-metric/ | Test Coverage is Not as Good of a Metric as You Think | Code coverage is a weak measure of test quality. A PHPUnit example reaches 100% coverage on a class while never asserting anything about the method that generates a random value. Use coverage as a last-step reminder of untested code, and write tests with TDD instead. |
| /2019/the-end-of-possessor/ | The End of Possessor | Retiring Possessor, a planned service for verifying website and domain ownership by uploaded HTML file or DNS record, with periodic re-verification and an API so a security scanner could confirm permission before scanning. The idea still seems sound, but monetization never justified the build. |
| /2019/track-done-things-with-alfred-update/ | Track Things Done with Alfred (update) | An update to the Alfred did-this workflow: besides appending each task to a dated text file, typing did lists today's entries and offers a copy-to-clipboard option via a PHP script, producing a bullet list ready to paste into a Basecamp daily check-in. |
| /2019/truncate-does-implicit-commit/ | Truncate MySQL Causes Implicit Commit | MySQL TRUNCATE TABLE triggers an implicit commit, so inside a Laravel test using the DatabaseTransactions trait, Model::truncate() escapes the transaction and corrupts the seeded data for later tests. Use Model::getQuery()->delete() instead, which stays inside the transaction and rolls back. |
| /2019/try-process-before-tooling/ | Try Your Process Before Tooling Your Process | Developers who build polished products often have garbage internal tooling because they script an annoying process before understanding it (premature automation). Live with the manual process first, document it, quantify how often it hurts and whether it is consistent, and only then automate. |
| /2019/two-gotchas-in-laravel-unit-tests/ | Two Gotchas in Laravel Unit Testing | Two Laravel testing pitfalls. Eloquent's is() compares only key, table, and connection, so models with different attribute values or different classes count as the same. Factory-created models on a fresh database start at ID 1, so ID assertions yield false positives. |
| /2019/what-to-license-private-software/ | What License to Use for Private Software | Closed source or proprietary software needs no license file: copyright already reserves every right to the author, and no license means nobody else may use, copy, or distribute the code. For package.json or composer.json license fields, write Unlicensed or No-License, not the SPDX Unlicense. |
| /2019/what-version-of-laravel-is-this/ | What Version of Laravel is This? | Ways to find the Laravel framework version from the command line: php artisan prints it on the first line, or pass the version flag for only that; composer show piped through grep, or composer show laravel/framework for the version plus license, description, and dependencies. |
| /2019/why-some-bosses-hate-remote-work/ | Why Some Bosses May Hate Remote Work | Reasons a manager may resist remote work: team camaraderie is harder to build, so plan meetups and require video conferencing; crunch time is invisible, so set an escalation plan; and empire builders want to see their team, so share more and thank them publicly. |
| /2020/code-coverage-image-github-actions/ | Host PHPUnit Code Coverage Image in Your Repo | A self-hosted code coverage badge without external services: a GitHub Actions workflow runs PHPUnit with Clover output, a PHP script reads coverage.xml through SimpleXML and draws a PNG with GD, an action commits the image back to the branch, and the README embeds it. |
| /2020/dev-manager-1-3-books/ | 3 Important Management Books to Read This Quarter | Opens the Dev Manager Crash Course: three books for an engineering manager, one a month. Extreme Ownership, by two Navy SEALs, on absorbing blame and forwarding praise, and Michael Lopp's Managing Humans and Being Geek on leading developers and judging when to leave. |
| /2020/dev-manager-2-3-tough-convos/ | 3 Tough Conversations from Stakeholders and Upper Management | Three conversations a dev manager gets from above: a demand for more features without moving the date becomes a scope question (what would you like to slip?), criticism of your team is a failure you own, and hiring time is an investment in velocity. |
| /2020/dev-manager-3-3-types-of-devs/ | 3 Types of Developers and How to Manage Them | Three developer personalities and how to manage each: the hero, a bottleneck to move into research and teaching without wounding their ego; the martyr, whose negativity spreads and needs a direct, calm talk; and the insubordinate, who is often just bored and under challenged. |
| /2020/dev-manager-4-make-face-to-face-work/ | Make Face-to-Face Work | Trolls feel safe because distance removes the face to face; the same distance lets remote developers drift from their manager. Citing Mehrabian's finding that 93% of communication is nonverbal: turn on the camera for video calls, and walk over instead of sending an IM. |
| /2020/dev-manager-5-ask-why/ | Sometimes Just Ask Why | Asking a programmer why is fact finding, not an accusation. A dev manager should understand the reasons behind decisions without knowing every implementation detail, and saying a vague reason aloud forces the programmer to think the choice through and opens the door to feedback. |
| /2020/dev-manager-6-manager-must-be-perfect/ | A Manager Must Always Be Perfect | A team expects its manager to be flawless as it never expected of a programmer, so act as if every move is on display: spell check emails, look up words and acronyms before using them, and treat the impossible standard as motivating accountability. |
| /2020/developer-time-vs-manager-time/ | Developer Time vs Manager Time | Developers are producers who need uninterrupted blocks to warm up, focus, and wind down; managers are operators whose fluid day hops between many things. Maker's schedule versus manager's schedule: pushing or cancelling a developer's meeting wastes a block, so move your own commitments instead. |
| /2020/first-portfolio-project-ideas/ | Coming Up With Your First Portfolio Project (with coding walkthrough) | How a junior developer picks and scopes a first portfolio project: a weather page with a hard-coded location, plain HTML, CSS, and JavaScript, fetch against the weather.gov API, error handling, a full-screen gradient background, hosting the demo, and iterations to add later. |
| /2020/full-page-screenshot-in-chrome/ | How to Take Full Page Screenshots in Chrome | Capturing a full-height screenshot of a long web page in Chrome without stitching images: open developer tools, switch on the device toolbar, choose Responsive, then pick Capture full size screenshot from the three-dot menu. Caveat: elements sized with 100vh stretch to the viewport height. |
| /2020/homekit-notify-door-open-too-long/ | Homekit Notify If Door Open Too Long | A HomeKit automation for a door left open too long, built with Convert to Shortcut: when an Eve door sensor opens, wait 30 seconds, then an If checks the contact sensor is still open and flips a Homebridge switch that sends a Pushover message. |
| /2020/ifttt-twitter-blog-hashtags/ | Tweeting Blogs with Hashtags Automatically with IFTTT Pro | Auto-tweet new blog posts with hashtags using an IFTTT Pro applet: an RSS feed trigger, a Twitter post action, and a JavaScript filter that loops over chosen keywords, prefixing whole-word matches with # through a case-insensitive word-boundary regex, then appends the post URL. |
| /2020/leave-zoom-with-alfred/ | Leave Current Zoom Meeting with Alfred App AppleScript | An Alfred workflow with the keyword lz that leaves the current Zoom meeting on macOS. The AppleScript activates Zoom, sends Command+W through System Events, then clicks the first button so a host ends the meeting for all. Downloadable workflow included. |
| /2020/motivational-message-when-you-wake-up/ | Get a Motivational Message You Set on iPhone to Wake Up | Using the iOS Shortcuts app to build a personal automation that runs after an alarm is dismissed: set the volume to full, then have Siri speak a text you wrote the night before. Turn off Ask Before Running so it starts on its own. |
| /2020/phpunit-coverage-helps-while-writing-tests/ | PHPUnit Code Coverage Can Help While Writing Tests | Code coverage is not only a final check: run PHPUnit with --coverage-html on one test class to spot untested branches. Plan tests from public methods, if statements, loops, and ternaries (cyclomatic complexity), then the HTML report reveals missed branches. Covered is not tested. |
| /2020/pitfalls-of-buying-software/ | The Pitfalls of Buying Pre-Made Software | Buying pre-made software or a template to hit a deadline rarely saves time. A mansion-kitchen metaphor: a nicer kitchen where nothing is where your team expects slows the 20-minute meal. Purchased software must fit the team's plan, not be a quick fix. |
| /2020/plan-9-in-vmware-fusion/ | Run Plan 9 in VMWare Fusion (on Mac) | Step-by-step install of Plan 9 from Bell Labs in VMware Fusion on macOS: download the ISO, choose Other with Legacy BIOS, fall back to inst/textonly when rio fails, then run partdisk, fmtfossil, copydist, and bootsetup before logging in as glenda. |
| /2020/redirect-domain-with-ssl-on-cloudflare/ | Redirect Domain with SSL on Cloudflare | Redirect a whole domain, HTTPS and www included, to one URL with a free Cloudflare account: an A record at reserved IP 192.0.2.1 with proxying on, a CNAME for www, nameserver change, SSL on Full, then two Page Rules using 301 Forwarding URL. |
| /2020/redirecturl-experiment-is-done/ | The RedirectURL Experiment is Done | Shutting down RedirectURL, a Laravel side project that offered free SSL redirects with Let's Encrypt certificates via Caddy, after almost no users. Born when registrars would not redirect HTTPS. Lessons on failure: know your limits, partners must equally want it, know your customer. |
| /2020/rewrite-redirect-url-apache/ | Rewrite or Redirect URL with Apache | Setting up a permanent 301 redirect with Apache mod_rewrite: check the module is loaded with apachectl -M, enable it with a2enmod or by uncommenting LoadModule in httpd.conf, then add RewriteEngine, RewriteBase, and a RewriteRule with the R=301 flag in the Directory block or .htaccess. |
| /2020/showing-css-variable-values-in-browser/ | Seeing Calculated Values of CSS Variables in Browsers | Browser dev tools show a CSS custom property (variable) as the var() reference rather than its computed value. How to see the value in each inspector: Chrome's color swatch and picker, Safari's equals sign that expands the value, and Firefox's hover tooltip and swatch. |
| /2020/stop-assert-database-has-laravel/ | Stop Using Assert Database Has in Laravel | Why assertDatabaseHas is a poor test assertion: it forces tests to know table names Eloquent is designed to hide, gives false positives when an update touches every row, and assumes models live in a database. Assert on models and relationships instead, and check untouched records. |
| /2020/the-end-of-quickpic/ | The End of QuickPic | Post-mortem on QuickPic.dev, a browser-based image editor for crops, resizes, and meme text, built in Vue on the canvas API with no backend: images arrive by clipboard paste, drag and drop, or URL and download as blobs. Retired for lack of monetization; source on GitHub. |
| /2020/web-developer-vs-web-programmer/ | Web Developer vs Web Programmer | What job titles like web designer, web developer, web programmer, and software engineer mean: prefixes mark the realm, nouns mark the discipline. A developer assembles a site or blog; a programmer builds custom software for a unique business process. Which to hire, and when. |
| /2020/when-to-use-stoppropagation-vs-preventdefault/ | When to use stopPropagation vs preventDefault | JavaScript event handling: preventDefault() stops the browser's default action, like following a link or submitting a form, while stopPropagation() stops the event bubbling up to parent element handlers. Reach for preventDefault most of the time, adding stopPropagation when nothing else should run. |
| /2020/zabbix-test-notification/ | How to make a Zabbix Test Notification | Testing a Zabbix media, action, and notification setup without polluting history: create an item with key vfs.file.exists[/tmp/test], a trigger on last()=1, then touch the file to fire the alert and delete it to clear. A throwaway trigger for end to end notification testing. |
| /2020/zabbix-web-scenario-template/ | Website Up Web Scenario Template for Zabbix | Building a Zabbix template for website uptime monitoring: a web scenario fetching the homepage every minute, a Disaster trigger on web.test.fail min(#2) to avoid single miss false positives, a speed and failure graph, and hosts that stand for websites. Downloadable XML template. |
| /2021/an-argument-for-larger-dev-scopes/ | An Argument for Larger Dev Scopes | Splitting a project into one-developer silos creates a bus factor of one, quietly slows everyone down, and produces worse code with no one reviewing. Put two or three developers on each task and rotate the pairs: cross training, higher quality, and faster integration. |
| /2021/ansible-reboot-if-required/ | Ansible Reboot Only If Required | After an apt dist upgrade in an Ansible playbook, stat /var/run/reboot-required, register the result, and run the built-in reboot module only when that file exists. Ansible controls the reboot, so later tasks wait instead of timing out. Tested on Debian-based hosts. |
| /2021/better-alternative-to-gitkeep/ | A Better Alternative to .gitkeep | Git tracks paths, not directories, so keeping an empty folder needs a file. Instead of the .gitkeep convention, place an empty .gitignore to version a folder, or a .gitignore containing * and !.gitignore to keep the folder while ignoring everything inside it. Self-documenting and standard. |
| /2021/capture-and-redirect-all-laravel-email/ | Capture and redirect all Laravel email | Redirecting every outgoing email in a Laravel staging environment without Mailtrap: a listener on the MessageSending mail event strips the To, Cc, and Bcc addresses, sends the message to one catch-all address from config, and preserves the originals as JSON in an X-Original-Emails header. |
| /2021/css-fallback-for-redirect/ | CSS fallback for failed Javascript redirect | Showing a manual continue link on an interstitial redirect page only after a delay, with no JavaScript timer: a CSS keyframe animation holds opacity at 0 through the 99% mark and flips to 1 at 100%, so it works even when the script crashes. |
| /2021/disable-pihole-from-ios/ | Disable PiHole from iPhone 1-click | Building an iOS Shortcut that disables (or re-enables) a Pi-hole DNS ad blocker with one tap: grab the API token from the admin settings, put it in a Text action, and call the admin/api.php?disable endpoint with a Get Contents of URL action. |
| /2021/do-not-calculate-answers-to-tests/ | Do Not Calculate Answers to Your Unit Tests | PHPUnit tests should assert hard-coded expected values, never values computed by a helper that mirrors the logic under test. Duplicating the same condition on both sides tests nothing. Magic numbers, string constants, and copy-pasted setup are fine in test code. |
| /2021/do-not-use-tinker-in-production/ | Do Not Use Laravel Tinker in Production | Keep Laravel Tinker in require-dev and off production servers: REPL changes are unversioned, unreviewed, untested, and can fire events or delete data by accident. Write one-time Artisan console commands wrapped in database transactions for data conversions, and use Metabase for ad hoc reports. |
| /2021/finding-slow-tests-in-phpunit-9/ | Finding Slow Tests in PHPUnit 9 | Flagging slow tests in PHPUnit 9 with an extension class implementing AfterTestHook: executeAfterTest gets each test's duration and writes a warning to STDERR past a threshold, registered in phpunit.xml. Notes on Laravel database setup, migrations, and skipping the test that pays for them. |
| /2021/fix-my-fucking-project/ | Bye Bye FixMyFuckingProject | Retiring the fixmyfuckingproject.com brochure site, a profane single-page marketing pitch to clients with projects spiraling out of control. It drew contact forms and Calendly bookings, but most leads were low-budget clients already burned by contractors. Screenshots of the page and its Open Graph previews. |
| /2021/github-actions-pages-scheduled-data-updates/ | Using Github Actions & Pages to Publish Static Pages Based on Dynamic Data | Auto-updating a static GitHub Pages site on a schedule: a cron-triggered GitHub Actions workflow fetches a silver spot price API, writes the JSON to data.json, and commits it, triggering a Pages rebuild; the page reads the file with fetch() and fills the DOM. |
| /2021/have-lunch-and-walk/ | Having Lunch With Employees Isn't Enough | Managers bond with their team over lunch, which connects through the heart. Walking with an employee to solve a problem connects through the head, like pacing when stuck or an escape room. Both connections build a happier, more effective team. Team building, one-on-ones. |
| /2021/homebridge-notify-garage-open-too-long/ | Homebridge Homekit Notify if Garage Open Too Long | Alerting when a HomeKit garage door stays open over three minutes: homebridge-delay-switch plugin provides a delay switch and motion sensor; automations turn the switch on when the door opens, off when it closes, and fire a Pushover notification through homebridge-messenger when the sensor trips. |
| /2021/illustrating-why-fillable-and-validated-matter/ | Illustrating Why Fillable and Validated Matter in Laravel | A mass assignment vulnerability demonstrated in Laravel: with only id guarded on an Eloquent model and $request->all() in the controller, a hidden owner_user_id field added through dev tools reassigns a record to another user. Use a $fillable whitelist and $request->validated(), ideally both. |
| /2021/install-composer-package-from-local-zip/ | How to Install a Composer package from a local zip | Install a Composer package such as Laravel Nova from a local zip instead of committing its files or configuring private repository credentials: drop the zip (containing a composer.json) in a packages directory, add an artifact repository pointing at it in composer.json, then composer require. |
| /2021/its-not-lazy-its-reducing-cognitive-load/ | Programmers Aren't Being Lazy: They're Reducing Cognitive Load | The programmer joke about being lazy enough to automate a task is really cognitive load theory at work: intrinsic, extraneous, and germane load compete for limited working memory. Automating trivial repetitive tasks frees capacity for decision making, which is the real job. |
| /2021/laravel-quick-show-config/ | Command to Quickly Show Config in Laravel | An artisan command, config:show, that dumps Laravel's config to the terminal, optionally a single key like services, for checking a cached config without opening Tinker. Handy after forgetting to re-cache; risky anywhere someone can see the screen, since it prints secrets. |
| /2021/lessons-from-code-review-interview/ | What I Learned From Giving a Few Interactive Code Review Interviews | A retrospective on replacing live coding interviews with an interactive code review of an unfamiliar repository. Candidates froze, stayed silent, or were unprepared; the chosen projects were above their level; and it is unclear whether reviewing code measures the ability to write it. |
| /2021/managers-have-a-team-not-a-family/ | 3 Reasons to Stop Calling Your Dev Team a Family | Three reasons managers should stop calling a development team a family: families love unconditionally while teams bench members who cannot perform, the word becomes cover for overwork and poor treatment, and not everyone's family is a happy comparison. Build a team with its identity. |
| /2021/mass-slack-status-update-app-idea/ | Mass Slack Status Update App Idea | An app idea: sign in to several Slack workspaces through OAuth and set the same status on all at once, perhaps as an Ionic app. Challenges include custom emoji, the ten app limit on free workspaces, monetization, and whether anyone would look for it. |
| /2021/mirror-git-repository/ | Mirror a Git Repository Without Forking | Keep a backup of a third party Git repository on GitHub when forking is not allowed: git clone --mirror into a bare repo, add the destination remote, git push --mirror, then git remote update and push to sync. Protects against a paid dependency disappearing. |
| /2021/must-embrace-conflict/ | Engineering Managers Must Embrace Conflict | Managers who try to eliminate all workplace conflict avoid the hard decisions, as the Colin Powell quote puts it. Conflict is a byproduct of criticism, feedback, and challenging the status quo, not a goal to manufacture. Healthy disagreement signals an evolving team and product. |
| /2021/old-school-fancy-resume-style/ | Old School Fancy Resume | Recreating a heavy-paper, gold-ribbon resume in HTML and CSS: a cream background with base64 noise texture as a data URL, and a ribbon built from nested elements sharing an angled linear-gradient with tight gold stops for a reflective look. CodePen demo and PDF. |
| /2021/php-constant-visibility/ | PHP Class Constant Visibility | Class constant visibility in PHP: since PHP 7.1 a constant can be declared public, protected, or private. A short video walks through how each visibility works and why you might hide a constant from code outside the class or its children. |
| /2021/phpunit-todo-tests/ | The Importance of ToDo Tests | TODO comments get forgotten, but todo tests in PHPUnit earn their keep: write empty test shells while the use cases are fresh and mark them incomplete so every run shows an I for untested areas. A base-class todo() helper calls markTestIncomplete. |
| /2021/play-tic-tac-toe-page/ | Shutting Down PlayTicTacToe.page | Retiring a tic-tac-toe website built in Vue, then rewritten on Firebase with Firestore and cloud functions for two-player games over a shared series link. Lessons: cloud functions boot slowly, Firestore rules could have handled scoring, and Firebase has no spending cap. Traffic never came. |
| /2021/poor-mans-javascript-error-reporting/ | Poor Man's Javascript Error Reporter | Client-side JavaScript error tracking without a service: assign window.onerror, build the message, source, line, column, and user agent into URLSearchParams, and fire a GET by setting an Image src to your endpoint. A blunt fallback; use Bugsnag or similar error monitoring instead. |
| /2021/publish-bugsnag-laravel-config-only/ | Publish Bugsnag's Laravel Configuration Only | Bugsnag's Laravel docs say to run php artisan vendor:publish, which can publish every package's assets. Limit it to the Bugsnag config file alone by passing the --provider flag with BugsnagServiceProvider, so only config/bugsnag.php lands in your app. A one-command tip. |
| /2021/quick-tip-to-decide-type-of-unit-test/ | 1 Quick Tip to Decide Which Type of Unit Test to Create | Choosing between unit, integration, and end-to-end tests: a definition of each and one heuristic. Code whose behavior depends on user choices, like CRUD and workflows, wants E2E tests; code driven by data and algorithms, like report generation, wants unit and integration tests. |
| /2021/redirect-rewrite-url-nginx/ | Rewrite or Redirect URL with NGINX | Redirect an entire domain to another URL with a permanent 301 in NGINX: find nginx.conf, locate the server block for the domain, add a rewrite rule matching ^/(.*)$ to the destination with the permanent flag, then reload NGINX. Originally published on RedirectURL. |
| /2021/securing-laravel-from-hackers/ | Securing Laravel from Hackers | Laravel is secure by default, with prepared statements and environment configuration, but one coding mistake or misconfiguration defeats it, and security education rarely goes deeper than a top 10 list. Announces Laravel Hacker: a seven question security quiz and a seven day email course. |
| /2021/understanding-company-productivity-with-amoebas/ | Understanding Company Productivity, High Performers and Amoebas | A theory of why high performers leave slow companies: picture the company as an amoeba moving at the average worker's speed. A star enters from the back, lifts nearby coworkers' pace, then reaches the front wall, strains against it, and breaches out. |
| /2021/what-have-i-done-lately/ | What Have I Been Working on Lately? | Roundup of current projects: Laravel consulting focused on team practices and tooling, the No Compromises podcast, the paid e-book Master Laravel Validation Rules with PHP exercises, the free tips e-book A Little Bit of Laravel, and a Laravel security brand in progress. |
| /2021/what-is-a-cto/ | What is a CTO? What do they do? | A Chief Technology Officer has two concerns, communication and technology, and communication is the larger: translating technical risk and cost for stakeholders, advocating for engineering team's limits, and using deep expertise in one discipline to ask questions and hire people more talented than you. |
| /2021/what-non-it-startup-founders-need-to-know/ | What Non-IT Startup Founders Need to Know | Why a non-technical startup founder needs a technical cofounder: contractors do what you say and leave, a cofounder protects product and eases funding. Meanwhile do wireframes and focus groups, own every account, use a password manager, keep source control and data access, question relentlessly. |
| /2021/who-to-tell-i-dont-know/ | Who Do I Tell "I don't know" To? | Admitting you don't know is essential to learning and avoiding mistakes, and interviews probe for it. With business stakeholders, pair the admission with an expectation, a date to return with a proposal or referral. With peers, just say it; they will spot a bluff. |
| /2021/why-ui-ux-matters/ | Why Does UI/UX Even Matter? | Small UI inconsistencies like misaligned spacing and typos add up the way noises at night do: the subconscious pattern matching that detects danger reads them as signs of a scam or careless product. Design consistency keeps users trusting and completing a call to action. |
| /2022/add-interfaces-to-add-verbosity/ | Add Interfaces to Laravel to Increase Code Readability and Verbosity | Code shows what it does, not what the programmer intended, which makes review harder. Laravel's empty ShouldQueue marker interface is the model: declaring an empty interface such as ShouldNotQueue on a listener states intent instead of leaving an omission ambiguous. Comments versus self-documenting code. |
| /2022/are-surveys-accurate/ | Are programming surveys accurate? | State of the language surveys with around 3,000 self-selected respondents get cited as authoritative, but sample size, selection bias, and who has time to answer all skew toward the new and shiny. Survey results can become self-reinforcing hype, like the PHP is dead chorus. |
| /2022/backup-github-repos/ | Backup All Your Github Repos Script | A Node.js script that backs up every GitHub repository you own: Octokit paginates the repos API, git clone over SSH into a date-named folder, cli-progress shows a progress bar, token and folder come from environment variables. Removes a single point of failure for your code. |
| /2022/be-careful-with-prepareforvalidation/ | Be careful with prepareForValidation in Laravel | In a Laravel form request, prepareForValidation that merges ucfirst of get() on a missing nullable field injects an empty string, so has() returns true and the value may wipe model data. Guard each key with has() before adding it to the merge array. |
| /2022/breaking-down-laravel-unit-tests-into-types/ | How to Break Down Laravel Unit Tests into Types | Four kinds of automated tests in Laravel: unit tests of the smallest code, integration tests touching the database (gates, Eloquent scopes, factories), feature tests of a full endpoint with mocked services, and external tests hitting real third parties in a separate PHPUnit suite. |
| /2022/deleting-vs-fixing-old-code/ | Why is Deleting Code OK but Fixing is Not? | Ugly or fragile code that runs can be left alone when the business judges fixing it not worth the opportunity cost. Dead code is different: unreachable or abandoned features carry old security assumptions and can still be found and executed. Delete it immediately. |
| /2022/do-you-even-sprintf/ | Do you even sprintf? | Introduction to PHP sprintf() as a replacement for messy string concatenation and interpolation: printf format strings, the %s, %d, and %f specifiers, integer truncation versus float output, and custom padding characters for building a dotted table of contents line. |
| /2022/helper-method-in-php-global-vs-anonymous/ | Named function or anonymous function for PHP helper | Named global function versus closure in a variable for a PHP helper: the named function is callable anywhere, including inside class methods, while the anonymous one needs a global declaration there. Limited scope suits a helper that should stay local to one file. |
| /2022/highlight-laravel-logs-in-phpstorm/ | Highlight Laravel Logs in PHPStorm | Using the JetBrains Ideolog (idealog) plugin to color Laravel log files in PhpStorm: three regex highlight rules for stack trace lines, .ERROR entries, and .WARNING entries, with colors, bold, and show-on-stripe scrollbar markers, so errors stand out in a wall of log text. |
| /2022/idea-quick-api-proxy/ | Idea - Quick API Proxy service | An idea for a cheap API proxy service: front-end developers hit a UUID-keyed URL that forwards to an API lacking CORS headers or requiring an API key, keeping the key server-side. Built on Cloudflare Workers, KV, and Pages with Auth0 login and freemium tiers. |
| /2022/one-tap-track-weight-daily-ios-no-app/ | One-Tap Track Weight Daily in iOS Without an App | Log daily weight into Apple Health without a third-party app: an iOS Shortcut that asks for a number, then uses Log Health Sample with type Weight in pounds and the provided input, added to the home screen so it launches like an app. |
| /2022/phpdoc-in-blade-files-helps/ | Use PHPDoc in Laravel Blade files for autocomplete in PHPStorm | PHPStorm offers no autocomplete for model methods inside a Blade @foreach over a paginator or collection, even with Laravel Idea. Adding a PHPDoc @var annotation for the loop variable inside the @foreach parentheses tells the IDE its type and autocomplete works. |
| /2022/quick-trick-for-laravel-requests/ | Quick trick for Laravel requests saves tons of time | Avoid duplicating validation rules between store and update in a Laravel CRUD controller: make UpdateRequest extend StoreRequest so both form request classes share the same rules. Only works when create and edit require identical validation, not for partial PATCH updates. |
| /2022/state-of-laravel-security-2021/ | The State of Security of Laravel Apps in 2021 | A whitepaper PDF compiled from over 1,000 self-reported answers to the Laravel Hacker seven question security quiz, showing how secure Laravel developers believe their applications are and which security topics need attention. Preserved from the Laravel Hacker site; current material lives at Mastering Laravel. |
| /2022/use-netlify-functions-to-proxy-api-for-cors/ | Use Netlify functions to proxy an API without CORS | When a browser fetch to a third party API fails with a CORS error (no Access-Control-Allow-Origin header), a Netlify serverless function can proxy the request. Build one with node-fetch, install its dependencies through netlify.toml, and call it at /.netlify/functions/ from vanilla JavaScript. |
| /2022/using-traits-in-laravel-policies/ | Use Traits in Laravel in Policies | Laravel authorization policies can intercept every check with a before() method. When only some policies should let admins do anything, put before() in a trait (AllowAdminToDoAnything) and use it in just those policy classes, instead of bloating a shared abstract parent policy. |
| /2022/why-care-about-privacy-with-analytics/ | Why Care About Privacy with Web Analytics? | Web analytics matter beyond vanity metrics: funnels, A/B testing, content interest. Google Analytics is free because it profiles visitors across sites and feeds ad targeting, and nothing to hide is a weak defense. Fathom Analytics is a recommended privacy-focused, GDPR compliant alternative without cookie banners. |
| /2022/your-team-is-not-the-only-special-one/ | Your team isn't the only special one | Staying at a bad job for the team: a Bar Rescue bartender and a programmer gave the same reason for not quitting. Great teams exist everywhere, so loyalty to coworkers should never trap you in a toxic workplace. When to leave a job. |
| /2023/actions-should-not-have-tightly-coupled-input/ | Actions Should Not Have Tightly Coupled Input | A Laravel Action class whose make() type hints one FormRequest is single-use code moved out of the controller for no benefit. Options: accept a validated array or a data transfer object, or expose named static constructors per request that funnel into one protected make(). |
| /2023/an-argument-for-keeping-your-project-simple/ | An Argument for Keeping Your Project Simple | Interfaces and database-agnostic code are over-engineering and YAGNI (you aren't gonna need it): skip contracts for services never swapped, do not abstract Braintree because Stripe might come later, use MySQL-specific SQL, keep the ORM. Interfaces pay off for the strategy pattern and plugin systems. |
| /2023/create-an-orphan-or-empty-git-branch/ | Creating an Orphan or Empty Git Branch | Making a completely empty branch in an existing Git repository with git checkout --orphan followed by git rm -rf to clear the inherited working tree. Useful for starting a v2 or a full rewrite while keeping the old history in the same repo. |
| /2023/css-only-display-element-after-click/ | CSS Only Display Element After Click | Revealing a hidden form when a link is clicked using only CSS: the :target pseudo class matches the element whose id equals the URL fragment, so form is display none and form:target is display block. No JavaScript, fewer bytes, and it pairs with transitions. |
| /2023/explain-wtf/ | Explain WTF | Archive of explain.wtf, a profane glossary site that explained tech terms to non-technical people: Angular, Apache, full stack, JavaScript, LAMP and LEMP stacks, Linux, MySQL, NGINX, NodeJS, open source, PHP, React, servers, Vue. The content is reproduced after the site was shut down. |
| /2023/get-laravel-auth-user-in-404-blade/ | Get Laravel Auth User in 404 Blade | Laravel's built-in 404 handler renders the published errors/404.blade.php without the authenticated user. Registering Route::fallback at the end of routes/web.php with an invokable NotFoundController that returns response()->view with status 404 lets the error page extend the normal layout and show the logged-in user. |
| /2023/google-geolocation-zip-only-not-found/ | Google Geo-location Zip Only Not Found Fix | Geocoding a bare US zip code through the Google geocoder (via the geocoder-laravel package) returned no results for one postal code, even though full addresses in it resolved. Appending "USA" to the zip fixes the lookup: "15705 USA" returns the right location. |
| /2023/hourly-billing-gives-you-the-worst-results/ | Hourly Billing Gives You the Worst Results | Why hourly billing for software work produces the worst outcome: a contractor paid by the hour profits from taking longer, and an agency charging a fixed hourly rate profits by staffing the cheapest developer. Flat rate or fixed price projects align incentives instead. |
| /2023/how-do-i-find-a-programmer/ | How Do I Find a Programmer? | Business owners with an idea and a budget find a developer by asking anyone in IT "do you know anyone who can build this?" rather than "can you help me", which leaves the door open for a referral chain. Networking the way sales does. |
| /2023/how-to-fix-chronically-out-of-date-documentation/ | How to Fix Chronically Out of Date Documentation | Stale wikis and READMEs happen because documentation is treated as lower priority than code. Treat documentation as natural language code: a change is not complete for review until its docs are written. Rebuts the usual excuses: programmers can't write, no time, over-documentation. |
| /2023/job-trader-website-idea/ | Job Trader Website Idea | A startup idea: a marketplace website, JobTrader, where two people who each want the other's job swap positions. Matching search, a private mutual interview sharing the good, the bad, salary and benefits, then a swap package presented to both employers. Unsolved: one-sided interest and monetization. |
| /2023/laravel-blade-component-for-form/ | Is a Laravel Blade Component for a Form a Good Idea? | A Laravel Blade class component, x-form, that wraps the HTML form tag and automatically emits the @csrf token for POST and the @method override for PUT, PATCH, or DELETE, since HTML forms only support GET and POST. Attributes fall through untouched. |
| /2023/meme-image-generator-ios-shortcut/ | Make Meme Image Generator with IOS Shortcuts | Build an iOS Shortcut that appears in the share sheet, prompts for top and bottom text, uppercases it, and overlays it on the image in white Impact with a black outline, then copies or saves the result. Private meme generation without Imgflip attribution. |
| /2023/moved-to-hugo/ | Moved to Hugo | Migrating a blog from Jekyll on GitHub Pages to Hugo on Netlify: builds dropped from thirty seconds to under two, shortcodes replaced template logic, and a new design. The cost was days of content conversion and changed RSS feed URLs handled with redirects. |
| /2023/npm-install-for-hugo-on-cloudflare-pages/ | NPM Install for Hugo Theme on Cloudflare Pages | Cloudflare Pages only runs npm install automatically when package.json sits at the repository root, which a componentized Hugo theme with its own Node dependencies (Bootstrap, esbuild) does not have. Fix: change the Pages build command to npm --prefix=./themes/my-current-theme install && hugo --minify. |
| /2023/obsidian-always-show-markdown-header-level/ | Obsidian Always Show Markdown Header Level | A CSS snippet for Obsidian's live preview that keeps the # marks visible on headings after the cursor leaves the line, so heading levels stay obvious: ::before pseudo-elements on .cm-header-1 through .cm-header-6, hidden on the active line. Remember to enable CSS snippets. |
| /2023/requirements-documentation-examples-guidelines-rationale/ | Requirements Documentation Examples Guidelines and Rationale | How to write a software requirements document and wireframe, with a downloadable example for a fictional Slack app. Sections: executive summary, problem, assumptions, risks, technical requirements, out of scope, milestones. Wireframe tips: same viewport size, clickable workflow, annotations, grayscale. Made in Balsamiq or Pencil. |
| /2023/requirements-gathering-questions-to-ask/ | Requirements Gathering Questions to Ask | A base questionnaire for requirements gathering on software and web projects: goals and a three-year vision, main and nice-to-have features, user accounts and roles, security and privacy, regulation, integrations like payments and CRM, existing tech teams and hosting, phased releases, priorities, and acceptance testing. |
| /2023/run-php-local-server-like-live-server/ | Run PHP Local Server Like live-server | A zsh function that mimics live-server for PHP: find an open port with netcat, start the built-in PHP CLI web server (php -S) in the current directory, and open the browser to it. Begins with a plain .zshrc alias, then explains each line. |
| /2023/self-reflection-as-a-manager/ | Self Reflection as a Manager | A dev manager answers a consultant's suggestions (pull request reviews, rotating support weeks) with 'we tried that and it didn't work.' Past failure does not predict the outcome; the team's skills and makeup have changed. Reflexively rejecting ideas means the obstacle is the manager. |
| /2023/should-we-use-a-monorepo/ | Should We Use a Monorepo? | Monorepo versus many small repos (polyrepo) is usually argued on technical grounds, but team and management culture matters as much: team size, synchronous or asynchronous communication, and how experienced members are at managing dependencies across projects. Pros and cons of each, plus the cultural questions. |
| /2023/siri-shortcut-to-todo-list-easier/ | Siri Shortcut to Todo List Easier Than Existing Systems | Adding tasks to the Things todo app by voice through Siri's built-in phrasing works poorly for long items. Fix with Apple Shortcuts: a Dictate Text action that stops after a pause feeds a Things Create Todo action, triggered by 'Hey Siri, take a note'. |
| /2023/sort-nova-users-on-spatie-role/ | Sort Nova Users on Spatie Role | Make a Laravel Nova user index sortable by role from the spatie/laravel-permission package. Override indexQuery to left join model_has_roles and roles, select the role name as an aliased column, and expose it as a sortable Text field, assuming each user has zero or one role. |
| /2023/statement-of-work-example/ | Statement of Work Example | What a statement of work or developer contract is and what goes in one: summary, responsibilities of business and developer, technical limitations and scope, intellectual property, payment schedule. Treat it as a flexible guide amended by written addendums. Includes a fictional sample SOW PDF. |
| /2023/stop-vs-code-wrapping-html/ | Stop vs Code Wrapping HTML | VS Code's HTML formatter breaks long lines at a default wrap length even when Prettier and ESLint handle everything else. Setting html.format.wrapLineLength to 0 in settings.json turns off the automatic line wrapping for plain HTML files. |
| /2023/turning-off-syntax-highlighting-one-file-vscode/ | How to Turn Off Syntax Highlighting for a Single File in VSCode | Disable syntax highlighting for one open file in Visual Studio Code (VSCode) without changing global settings: click the language mode indicator in the status bar and choose Plain Text. The change lasts only until the file is closed and reopened. |
| /2023/use-dpi-media-query-for-crisp/ | Use DPI Media Query for Crisp Text and Images with High Density | Antialiased text and a hero background image looked great on a Retina 5K display but washed out on a lower resolution monitor. A min-resolution: 192dpi media query applies font smoothing and the image only on high DPI screens, with a plain CSS gradient elsewhere. |
| /2023/use-github-actions-permissions-with-3rd-party-actions/ | Use Github Actions Permissions with 3rd Party Actions | Third party GitHub Actions run with your repository's token and could exfiltrate source or credentials, whether by malice or a supply chain attack. Scope the job with the permissions key so an action like actions/stale gets only the access it needs: least privilege. |
| /2023/using-github-public-api-for-random-user/ | Using Github Public Api for Random User Retrieval | Redirect an expiring domain to a random GitHub user's profile with client side vanilla JavaScript and the unauthenticated GitHub search API: a random page number with per_page=1, a location and repos query, and a random sort. Rate limits fall on each visitor's IP. |
| /2024/disable-1password-popup/ | How to Disable the 1Password Popup | Two attributes to stop 1Password from interfering with a form field: autocomplete off on the input suppresses the auto popup window, and the extension's data-1p-ignore attribute hides its inline icon in the box. Forms with password fields may still trigger it. |
| /2024/elevator-pitch-website/ | Elevator Pitch Website | A startup idea: a site where founders post a word-limited elevator pitch, visitors vote thumbs up or down before commenting, and a premium tier buys expert critique across three iterations. Moderating trolls is the hard part; a good project for a new programmer. |
| /2024/email-catch-all-forwarding-on-cloudflare-free/ | Email Catch All Forwarding on Cloudflare Free | Step-by-step screenshots for setting up a free catch-all email forward on a domain whose nameservers point at Cloudflare: enable Email Routing, remove old MX records, accept the new DNS records, then edit the catch-all rule to send to a destination address and enable it. |
| /2024/generate-ford-explorer-manual-as-pdf/ | Generate the Ford Explorer 2020-2024 Manuals as PDF | Open source PHP scripts that scrape the online Ford Explorer owner's and service manuals and render them as printable PDFs with mPDF: a URL list per manual, a loop fetching each page, print CSS hiding navigation, string cleanup, and huge memory and time limits. |
| /2024/generate-laravel-seeds-package-idea/ | Generate Laravel Seeds Package Idea | An idea for an open source Laravel package: point Eloquent or DB queries at a read-only production connection and generate seeder files from the results using existing model factories, so new developers get realistic local dev data without copying the whole production database. |
| /2024/git-ignore-without-gitignore/ | Git Ignore Without the .gitignore File | Keep personal tooling files like a docker-compose.yml out of a shared repository without touching its .gitignore: list them in .git/info/exclude, a per-checkout ignore file that is never committed or shared, for files only your own working copy needs. Untracked local exclusions. |
| /2024/laravel-display-markdown-easily-in-blade/ | Laravel Display Markdown Easily in Blade | A Laravel anonymous Blade component, x-markdown, that renders Markdown in a view with one line: Str::of($slot)->markdown()->toHtmlString(). Caveats: four-space indentation inside the slot becomes a code block, and security depends on Laravel's league/commonmark GithubFlavoredMarkdownConverter configuration, especially for user-supplied Markdown. |
| /2024/laravel-log-api-request-and-response/ | Laravel Log Incoming API Request and Response | A terminable Laravel middleware that logs every incoming API request and its response after the response is sent: method, route name, full URL, body, server data, user id, status code, response body, and headers, to a dedicated log channel. Password fields are redacted before logging. |
| /2024/laravel-log-throttled-users/ | Laravel Log Throttled Users | Logging Laravel rate limiting: register a reportable callback for ThrottleRequestsException in the exception handler that writes the request input, IP, and URL to the log. Lets you confirm throttling on login and password reset endpoints works, spot abusive users, or reach out to legitimate ones. |
| /2024/laravel-password-reset-a-little-better/ | Laravel Password Reset a Little Better | Improving the Laravel docs' password reset controller: move validation to a Form Request with Password::default(), drop password confirmation, null the remember token instead of regenerating it, and log the user in with Auth::login honoring a remember checkbox, redirecting to the dashboard, not the login page. |
| /2024/lets-refactor-a-javascript-snippet-adding-class-on-scroll/ | Let's Refactor a Javascript Snippet for Adding Class on Scroll | Refactoring a vanilla JavaScript scroll listener that toggles a scrolled header class on the body: a 10 pixel threshold to stop jitter, a class name parameter for reuse, classList.toggle with its force argument instead of if/else, and knowing when clever code goes too far. |
| /2024/logging-for-laravel-http-client/ | Logging for Laravel Http Client | Log every request and response made through the Laravel HTTP Client without touching existing calls, using Http::globalMiddleware() to attach Guzzle middleware in a service provider. A shared UUID ties each request log line to its response, captured when the promise resolves. |
| /2024/mysql-docker-not-running-entrypoint/ | Fix for Mysql Docker Not Running Entrypoint | The official MySQL Docker image only runs the scripts in docker-entrypoint-initdb.d when the data directory is empty. If a volume in docker-compose.yml still holds data, even after docker compose down, the init SQL never reruns. Remove the volume too, with docker compose rm. |
| /2024/production-merge-in-github-actions/ | Production Merge in GitHub Actions | A GitHub Actions workflow that merges develop into main on demand: workflow_dispatch trigger, contents write permission, actions/checkout with fetch-depth 0 for full history, git user set from the actor, then checkout, merge, and push. Deploy by clicking Run workflow instead of a local merge. |
| /2024/show-alpine-element-after-delay/ | Show Alpine Element After Delay | Reveal an AlpineJS element after a delay with no user interaction: x-cloak it, keep show false in x-data, bind x-show to it, and in x-init call setTimeout to flip show to true. Use case: delaying a 'send again' link after a password reset email. |
| /2024/states-with-abbreviations/ | States With Abbreviations in PHP | Copy and paste PHP arrays of the 50 US states plus the District of Columbia: full state name keyed to two-letter postal abbreviation, and abbreviation keyed to name, each in multi-line and single-line form. Use array_flip if you only want to keep one. |
| /2024/using-laravel-factory-states-to-add-more-configuration-after-creation/ | Using Laravel Factory States to Add More Configuration After Creation | Laravel database factory states normally set attribute values through state(), but relationships like a Spatie Laravel Permission role cannot be set in definition(). A state method can return $this->afterCreating() with a callback that calls assignRole, so User::factory()->admin()->create() yields a user with the role. |
| /2024/web-components-as-decorators/ | Web Components as Decorators | Wrap a form in a custom element (form-single-submit) that prevents duplicate submissions, using a web component as a decorator: connectedCallback attaches a submit listener that calls preventDefault once a submission is in flight, without disabling the button, and opts in per form through markup. |
| /2025/a-deep-dive-into-print-css-headers-and-footers/ | A Deep Dive Into Print Css Headers and Footers | Every way to put repeating headers and footers on printed pages or Chrome-generated PDFs, with output screenshots: @page margin boxes like @top-right with counter(page), hiding them on :first, background images, thead and tfoot repetition, position: fixed, and combinations. Page numbers only work through @page. |
| /2025/adding-laravel-validation-errors-after-form-request-validation/ | Adding Laravel Validation Errors After Form Request Validation | When validation has to happen after a form request passes, such as a payment processor declining a card inside the controller, throw ValidationException::withMessages() keyed by the form field. The error lands in the error bag like any rule failure, with no custom session workaround. |
| /2025/custom-php-exceptions-for-unimplemented-code/ | Custom PHP Exceptions for Unimplemented Code | A NotImplementedException extending SPL RuntimeException as a pattern for work in the todo state or for methods an extended class should not support: throwing it stops the code and tells the next programmer, more loudly than an @todo comment, that this is not done. |
| /2025/customize-laravel-ide-helper-to-autocomplete-blade-files/ | Customize Laravel Ide Helper to Autocomplete Blade Files | Getting PhpStorm autocomplete and click-through for Blade view names passed to a custom method, here the render() call on a Yajra DataTables class: an ide.json completions entry for the Laravel Idea plugin that targets the parent class, method name, and parameter position. |
| /2025/docker-healthcheck-values-for-laravel-projects/ | Docker Healthcheck Values for Laravel Projects | Docker HEALTHCHECK commands for the containers in a typical Laravel stack: pgrep for the PHP-FPM pool on Alpine, mysqladmin ping for MySQL in docker-compose.yml, redis-cli incr for Redis, and wget against robots.txt for Nginx, with start periods, timeouts, and retries. |
| /2025/exploring-sprintf-in-the-browser/ | Exploring Sprintf in the Browser | An archived browser tool for experimenting with sprintf format strings, using a JavaScript port of PHP's function. Showing it revealed that developers unfamiliar with sprintf cannot explore what they do not know, so a playground was the wrong teaching tool. Source remains on GitHub. |
| /2025/github-workflow-for-production-merge/ | Github Workflow for Production Merge | A GitHub Actions workflow, run manually with workflow_dispatch, that merges develop into main for a production deploy: explicit contents write permissions, actions/checkout with full history and a personal access token secret, git user config from the triggering actor, then checkout, merge, and push. |
| /2025/how-i-launch-a-laravel-testbed/ | How I Launch a Laravel Testbed | Spinning up a throwaway Laravel instance on a Mac in a few keystrokes: two zsh aliases, sail for vendor/bin/sail and new-laravel for composer create-project, then new-laravel, artisan sail:install, and sail up -d run the fresh app in Docker (OrbStack) with Laravel Sail. |
| /2025/livewire-3-reactive-event-dispatching/ | Livewire 3 Reactive Event Dispatching | Livewire treats each component as an island, so a child that dispatches an event to its parent does not re-render when the parent's property changes. The #[Reactive] attribute on the child's prop, paired with #[On] listeners, gives the reactivity React or Vue developers expect. |
| /2025/phpstorm-docker-not-starting-fix/ | PHPStorm Docker Not Starting Fix | PHPStorm could not find a running Docker Compose container that the docker CLI and OrbStack saw fine. The cause is a named compose project plus a deprecated API PHPStorm uses to monitor containers. Fix: add COMPOSE_PROJECT_NAME to the CLI interpreter's environment variables. |
| /2025/phpstorm-terminal-lcall-error-fix/ | Phpstorm Terminal LC_ALL Error Fix | Opening a PHPStorm terminal on macOS printed bash: warning: setlocale: LC_ALL: cannot change locale. A pyenv init line in .zshrc spawned bash, which rejects the macOS BCP 47 locale format. Fix: unset LC_ALL in .zshrc when it contains -u- so a UTF-8 locale applies. |
| /2025/rendering-json-exceptions-in-laravel-before-version-11/ | Rendering Json Exceptions in Laravel Before Version 11 | Force JSON error responses for API routes even when the request does not send an Accept header. Laravel 11 offers shouldRenderJsonWhen in withExceptions; in Laravel 10 and earlier, override shouldReturnJson in app/Exceptions/Handler.php, return true for routes matching api.v1.*, and fall back to the parent. |
| /2025/reusing-dotenv-configuration-with-the-shell/ | Reusing DotEnv Configuration with the Shell | Share the .env file that phpdotenv and Laravel read with your shell scripts instead of defining secrets twice: a bin script sources the .env relative to its own location using dirname $0, and every variable such as APP_URL becomes available to commands like ping. |
| /2025/showing-utc-in-the-users-timezone-easily/ | Showing UTC in the Users Timezone Easily | Display a UTC timestamp in the visitor's local timezone: put it in an HTML time element's datetime attribute (Carbon in a Laravel Blade view), then JavaScript builds a Date, gets the browser timezone from Intl.DateTimeFormat().resolvedOptions(), and writes toLocaleString. AlpineJS x-init version too. |
| /2025/stopping-laravel-sql-injection-with-sole/ | Stopping Laravel SQL Injection with sole() | Eloquent's sole() as defense in depth against SQL injection: a whereRaw query built from user input such as OR 1=1 returns the first user with firstOrFail, but sole() throws MultipleRecordsFoundException when the injected condition matches every row. Parameterized queries remain the real fix. |
| /2025/what-type-of-contractor-are-you-hiring/ | What Type of Contractor are You Hiring? | Three types of software contractor: just do the work, hired hands; trying what's best, who take pride and learn the business over time; and all in, who analyze requirements and push back. None is best; decide which you want and ask which you're getting. |
| /2025/why-you-might-want-to-hydrate-models-to-delete-them-in-laravel/ | Why You Might Want to Hydrate Models to Delete Them in Laravel | Cascading deletes in Laravel through Eloquent model events: a deleting hook loads the HasMany relationship and deletes each model instead of a bulk query, so child models fire their own deleting events. Hydration cost versus never forgetting cleanup down the hierarchy. |
| /2026/fixing-phpstorm-when-git-stops-responding/ | Fixing PHPStorm when Git Stops Responding | PHPStorm stops seeing a git repository while git works in the terminal, because macOS has two gits: Apple's at /usr/bin/git, which PHPStorm uses by default and halts until the Xcode license is accepted, and Homebrew's. Point PHPStorm's Git executable path at the Homebrew binary. |
| /2026/laravel-201-created-json-with-location-header/ | Laravel 201 Created JSON With Location Header | Returning a 201 Created response from a Laravel API without a body: response()->json(data: null, status: 201) plus withHeaders() to add a Location header built from a named route to the new resource. REST best practice for create endpoints that skip returning the full object. |
| /2026/reinventing-the-wheel-is-how-you-learn/ | Re-inventing the Wheel Is How You Learn | Reinventing the wheel is worth it with AI generated code available. Building your own version of an existing solution teaches the decisions and trade-offs; an LLM gives the most popular answer, not the best, and code that looks right hides the failure that teaches. |
| /2026/strava-ios-shortcut-to-start/ | Strava iOS Shortcut to Start Last Activity | Strava's iOS widgets only show progress and the app is absent from Shortcuts, but it exposes custom URL schemes. An iOS Shortcut that opens strava://record/new/start launches Strava and starts recording the last activity type from a home screen widget, removing friction before a morning walk. |
| /2026/the-cto-is-a-communicator-first/ | The CTO Is a Communicator First | A CTO should spend about 20% of the time hands on, writing code and reviewing pull requests to stay current, and 80% communicating: guiding without dictating, removing roadblocks, prioritizing across the company, and translating technical risk and consequence to stakeholders. Technical leadership, engineering management. |
| /2026/use-php-named-parameters-for-readability/ | Use PHP Named Parameters for Readability | A method with too many parameters is a code smell, but when it cannot be refactored, PHP 8 named parameters (named arguments) make the call site readable: each value is labeled with its parameter name, with no temporary variables and no fixed order. |
| /2026/using-a-laravel-factory-state-to-add-a-spatie-role/ | Using a Laravel Factory State to Add a Spatie Role | Laravel Eloquent factory states usually call state() to set attributes, but a state method can instead register an afterCreating lifecycle hook. An admin() state that calls assignRole on the created user attaches a Spatie Laravel Permission role fluently: User::factory()->admin()->create(). |
| /2026/validate-everything-including-pagination/ | Validate Everything Including Pagination | Pagination query parameters are user input, so validate them like everything else. An abstract Laravel Form Request with rules for page (nullable integer, min 1) and per_page (nullable integer, between 1 and 100) blocks absurd page sizes and non-numeric values; index requests extend it. |
| /2026/why-do-i-use-plain-text-password-for-my-initial-user-record/ | Why Do I Use Plain Text Password for My Initial User Record? | Seeding the first admin user in a Laravel migration with a plain text password like change-this-asap: no hash can match it, login is impossible until a password reset, the database makes the placeholder obvious, and required data stays in a migration, not a seeder. |
| /2026/why-im-not-writing-about-ai-yet/ | Why Im Not Writing About AI Yet | Not anti AI, anti hype. The same watch, assess, and revisit pattern that once looked like anti React, Laravel, Livewire, or Pest: use AI as a daily development tool, but wait to publish guidance until experience runs as deep as with PHP and JavaScript. |
